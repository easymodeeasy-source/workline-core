# Workline Improvement Backlog

## Authority

- このファイルはWorkline仕様の正本ではない。
- normative authorityは `registry.md` と、registryがroutingするcanonical Skills（`.claude/skills/<name>/SKILL.md`）だけである。
- backlog itemは未採用の候補であり、ここに記載されているだけでは仕様・rule・behaviorにならない。
- ある項目が実装済みになっても、canonical authority（`registry.md` / canonical Skills）へ反映されるまではnormative ruleとして扱わない。
- このbacklogはWorklineのRoadmap / Phase / Workではなく、Workline lifecycleでは管理しない。

## Conventions

- 各項目は `BL-001` 形式のstable IDを持つ。表示順を変えてもIDを振り直さず、再利用もしない。
- `Status` は現在分かっていることの区分であり、ticket lifecycleではない。
  - `OPEN`: 追加・改善したい能力。欠陥の再現を前提としない。
  - `VERIFIED`: 問題（欠落・欠陥）の実在を、現行実装または実運用で確認済み。解決済みという意味ではない。
  - `INVESTIGATE`: 問題の実在・原因・規模・方針のいずれかを、先に調査する必要がある。
  - `DEFERRED`: 実例や前提条件が揃うまで、意図的に保留している。
  - `RESOLVED`: 対応がcanonical authority（`registry.md` / canonical Skills）と実装へ反映済み。以後の正本はcanonical authorityであり、この項目は経緯の記録として残す。
- `Evidence class`:
  - `real-project migration`: 既存の実ProjectをWorklineへ移行した際に観測した。
  - `smoke test`: Workline検証用Projectでのsmoke testで観測した。
  - `code inspection`: 現行のworkline-core（実装・canonical仕様・repository状態）の確認で特定した。
  - `design deferral`: 設計段階で意図的に後回しにした論点。
  - `self-hosting assessment`: workline-coreのself-hosting診断で特定した。
- このrepositoryはpublicである。private Projectの名称・識別子・固有仕様・ファイル名・commit、ローカルの絶対パス、AI sessionログからの引用、個人情報・認証情報は書かない。private Projectでの発見は `real-project migration` 程度に一般化する。

## Notes

- remoteを持つProjectで、承認済みpush destinationへのpushを伴うoperationは、real-project migrationで実走済み（real-project path verified）。open itemにはしていない。

## Index

| ID | Title | Status |
|---|---|---|
| BL-001 | Foreign Project mutation boundary | RESOLVED |
| BL-002 | Work completion automatic review | OPEN |
| BL-003 | Phase completion automatic review | OPEN |
| BL-004 | Review finding classification / convergence | OPEN |
| BL-005 | Review / achievement evidence durability | VERIFIED |
| BL-006 | Read-only status/context command | OPEN |
| BL-007 | Cold-start recovery performance | INVESTIGATE |
| BL-008 | Python interpreter resolution | RESOLVED |
| BL-009 | Historical deleted Related / must_read semantics | RESOLVED |
| BL-010 | Generated artifact hygiene | RESOLVED |
| BL-011 | Reusable migration procedure | OPEN |
| BL-012 | Unsupported self-hosting guard | RESOLVED |
| BL-013 | Self-hosting readiness | DEFERRED |
| BL-014 | Deterministic tie-break among startable candidates | RESOLVED |
| BL-015 | Silent no-op on Phase re-entry | RESOLVED |
| BL-016 | Default result commit message type | RESOLVED |
| BL-017 | Declared write scope broader than actual writes | RESOLVED |
| BL-018 | Unused assignment in Phase expansion | RESOLVED |
| BL-019 | Runtime recovery record retention and ignore policy | RESOLVED |
| BL-020 | Correction of mis-recorded historical facts | DEFERRED |
| BL-021 | Concurrent operation exclusion | RESOLVED |
| BL-022 | ProjectSTART abandoned pre-effect recovery | RESOLVED |
| BL-023 | Phase expansion cannot resume after an interruption | RESOLVED |
| BL-024 | Resume invocation does not bind decided content | RESOLVED |
| BL-025 | Lifecycle mutation cannot resume after its own event is applied | RESOLVED |
| BL-026 | Phase cancel publishes an unreplanned structure before its postcheck | RESOLVED |
| BL-027 | Registration applies its effects before refusing them | RESOLVED |
| BL-028 | Replan registers new Works before its removals and fails on the intermediate structure | RESOLVED |
| BL-029 | Plan exclusion resume after its own terminal event | RESOLVED |
| BL-030 | START Work cancel resume requires a durable cancel decision | RESOLVED |
| BL-031 | START closes an unfinished terminal mutation without committing it | RESOLVED |
| BL-032 | Recorded commit cannot resume after an independent commit advances HEAD | RESOLVED |
| BL-033 | Recorded commit is taken as made when another commit has the same message | RESOLVED |
| BL-034 | Bootstrap backfill cannot converge its own interrupted pending mutation | RESOLVED |
| BL-035 | Recorded commit can resume on the wrong branch when HEAD still equals base | RESOLVED |
| BL-036 | Resumed operation can commit its decided effects on another branch before its commit effect is recorded | RESOLVED |
| BL-037 | Own applied commit is not recognized when its stored message differs from the recorded message | RESOLVED |
| BL-038 | Resume re-applies a mutation's committed effects on another branch before refusing it | RESOLVED |
| BL-039 | Recovery record becomes unreadable when recorded text contains Unicode line separators | RESOLVED |
| BL-040 | Cancel resume cannot reconcile a relation added earlier in the same START and removed by the cancel decision | RESOLVED |
| BL-041 | Operation performs effects before refusing a pre-existing dirty overlap, leaving an unrecoverable mutation | RESOLVED |
| BL-042 | START destroys a pre-existing dirty result path before refusing it, then leaves a mutation that cannot converge | RESOLVED |
| BL-043 | Operation commits, erases or adopts a change another subject makes to an owned path after it begins | RESOLVED |
| BL-044 | START resume re-decides a recorded hold and grows duplicate lifecycle effects on every retry | RESOLVED |
| BL-045 | Replan resume refuses after another Work changes only the recomputed display numbering | RESOLVED |

## Items

### BL-001 Foreign Project mutation boundary

- ID: BL-001
- Title: Foreign Project mutation boundary
- Status: RESOLVED
- Kind: design, spec, implementation
- Problem: あるrepository（Workline rootや別のProject）を開いたsessionから、別のWorkline Projectへのmutation（Project開始、Roadmap操作、bootstrap backfill等）をそのまま実行できる。現行の仕様・実装にはsessionとProjectの対応（session/Project affinity）という概念がなく、各operationは渡された任意のProject rootへ書き込む。別Projectをreadすることは許容し得るが、readとwriteの境界、別Projectへのwriteに明示的なoverrideを要求するかどうかが定義されていない。
- Why it matters: 意図しないProjectへの書込み、operation ownerや実行文脈の取り違え、canonical implementationを使わない手作業の構造再現につながる。real-project migrationでは、別のrepositoryを開いたsessionから移行対象Projectへのmutationが行われ、手作業での構造再現（後に巻き戻し）も発生した。
- Likely scope: readとwriteの境界、session/Project affinity、明示的overrideの要否の設計。`rules/git`（operation ownerと書込範囲）、Project側bootstrapと `skills/project-router` の入口規則、Workline root側から他folderへ行うpre-project操作（`skills/project-start`）との整理、CLI / Python APIのentry check。設計段階で保留したmulti-repo Project / 1 repositoryに複数Project（現行原則は 1 Project ≒ 1 repository）の論点もここで扱う。
- Cross-project impact: あり（全Projectのoperation entry）
- Backfill likely: 可能性あり（Project側bootstrapや入口規則を変える場合）
- Human confirmation likely: yes（共通ルールの変更、書込可能範囲の変更）
- Self-hosting prerequisite: yes（BL-013 の session / Project context rule）
- Evidence class: real-project migration, code inspection, design deferral, self-hosting assessment
- Resolution: 成立済みProjectへのstate-changing operationを、top-level operation開始時の作業directoryから1回だけ解決するinvocation Project contextがtarget Projectと一致する場合だけ許可した（session identityは使わない）。一致しなければProject execution lockより前に `foreign_project_mutation` でSTOPし、lock・mutation intent・event・relation・entity・Gitのいずれにも書き込まない。照合の後にexecution lockを取り、その後でmutationを行う。別Projectのread-only参照は許可する。Project開始は、別の成立済みProjectのcontextからでなく、成立済みProjectの配下でもないtargetに限るpre-project例外とし、そのmutationはProject開始の実行が対象rootに与えた許可の内側でだけ受け付ける（owner名だけによる免除は廃止）。overrideは設けない。作業directory由来のcontextは誤操作を防ぐmechanical guardであり、security sandboxではない。multi-repo Projectの設計は扱っていない。`rules/git` とcanonical Skillsへ反映済み。

### BL-002 Work completion automatic review

- ID: BL-002
- Title: Work completion automatic review
- Status: OPEN
- Kind: spec, implementation
- Problem: Work完了時の確認は、機械的に検査できるcompletion precheck（must_update / realizes / result path / dependency / 構造validation）に限られる。成果・diff・verification結果・「このWorkで成立させる状態」との整合を自動でレビューする仕組みがなく、レビューはWorklineの外で都度行われている。
- Why it matters: 構造上PASSでも成立状態を満たさないWorkがcompletedになり得る。completedはterminalで戻せないため、見逃した欠陥は後からfix Workとして扱うしかない。real-project migrationでは、completedになったWorkの欠陥が、後続の第三者レビューで初めて見つかった。
- Likely scope: `skills/start` のcompletion precheckとSTART実装の完了経路、レビュー対象（成果 / diff / verification / desired state）と判定の扱い、BL-004（分類と収束）・BL-005（証跡の保存）との接続
- Cross-project impact: あり（全ProjectのSTART完了挙動）
- Backfill likely: no（既存のcompleted Workは再判定しない想定）
- Human confirmation likely: yes（START completion semanticsと自動実行境界の変更）
- Self-hosting prerequisite: no
- Evidence class: real-project migration, code inspection

### BL-003 Phase completion automatic review

- ID: BL-003
- Title: Phase completion automatic review
- Status: OPEN
- Kind: spec, implementation
- Problem: Phase completeはeventではなく生成状態であり、条件はeffective current-plan Workの完了、integrationの存在、構造validation等の機械的条件に限られる。単体Workの再レビューではなく、Phase全体としてintegration・authorityの整合・lifecycleの妥当性・欠落した責務（どのWorkも担っていない成立条件）を確認する仕組みがない。
- Why it matters: 個々のWorkがPASSでも、Phaseの「成立させたい状態」を満たさないままRoadmapへ戻り得る。real-project migrationでは、Phase単位のレビューで、個々のWork完了時には検出されなかった欠陥が見つかり、追加Workで対応した。
- Likely scope: Phase completionの生成条件、`phase_integration_check` Workの意味と確認範囲、`skills/start`（outer continuation）と `skills/roadmap`（Phase完了後の復帰）
- Cross-project impact: あり（全ProjectのPhase完了判定）
- Backfill likely: no
- Human confirmation likely: yes（Phase completion semanticsの変更）
- Self-hosting prerequisite: no
- Evidence class: real-project migration, code inspection

### BL-004 Review finding classification / convergence

- ID: BL-004
- Title: Review finding classification / convergence
- Status: OPEN
- Kind: procedure, spec
- Problem: review結果の分類と収束条件が定義されていない。findingを少なくとも `VALID DEFECT` / `IMPROVEMENT` / `FALSE POSITIVE` / `REQUIREMENT CHANGE` 等に分類しないため、改善提案や誤検出まで成果物の修正要求として扱われ、review → 修正 → 再reviewのloopが収束しにくい（Review Loopの非収束）。
- Why it matters: 不要なartifact revisionが増え、完了判定が遅れる。分類が場当たりになると、同じ種類のfindingの扱いがreviewごとに変わる。real-project migrationでは分類をその都度定義しており、reviewが報告した該当箇所数と実際の該当数が食い違う場面もあった。移行に伴うWorkline側の修正でも、同じ論点で複数回のreview往復が発生した。
- Likely scope: 分類の定義と各分類の扱い（例: `VALID DEFECT` だけを修正必須とする、`IMPROVEMENT` は候補として記録する、`FALSE POSITIVE` は根拠付きで棄却する、`REQUIREMENT CHANGE` は `rules/human-confirmation` へ接続する）、収束条件、BL-002 / BL-003 での利用、分類結果の記録（BL-005）
- Cross-project impact: あり（review手順を共通化する場合）
- Backfill likely: no
- Human confirmation likely: yes（review手順を共通ルールやcanonical Skillへ入れる場合）
- Self-hosting prerequisite: no
- Evidence class: real-project migration

### BL-005 Review / achievement evidence durability

- ID: BL-005
- Title: Review / achievement evidence durability
- Status: VERIFIED
- Kind: spec, implementation
- Problem: `work_completed` / `roadmap_achieved` 等のeventは id / type / entity / at だけを持ち、なぜPASS / achievedと判定したのか、どのverification / evidenceを使ったのかを保存しない。Roadmap achievement判定の判断内容も永続化されない。mutation recovery recordはruntime領域にありcommit対象外のため、証跡の保存先にはならない。
- Why it matters: 判定の根拠がWorklineの記録の外にしか残らず、fresh sessionや第三者が完了・達成の妥当性を後から検証できない。eventの時刻は記録時刻であり、検証を実施した時点と一致しない場合もある。real-project migrationでは、完了判定に使ったverification結果や第三者レビューの結果がWorklineの記録に残らなかった。
- Likely scope: 何を・どこまでdurableに残すか（判定理由、使用したverification、review結果と分類）、event / record schema、`skills/start` と `skills/roadmap`（achievement）の証跡規則、derivation detailに類する証跡文書の置き場、public repositoryに残さない情報の扱い
- Cross-project impact: あり
- Backfill likely: no（過去の判定根拠は再構成しない想定。schema互換の検討は必要）
- Human confirmation likely: yes（canonical schema / 共通ルールの変更）
- Self-hosting prerequisite: no（self-hosting時の記録価値には直結する）
- Evidence class: code inspection, real-project migration

### BL-006 Read-only status/context command

- ID: BL-006
- Title: Read-only status/context command
- Status: OPEN
- Kind: interface
- Problem: 現在の状態を取得するcanonicalなread-only interfaceがない。CLIはregistry / Project validation、Project開始、push destination pin、bootstrap backfill、standalone Work作成だけを提供し、Roadmap / Phase / Workの状態取得にはPython APIを直接呼ぶ必要がある。Claude等のAIは、その都度sourceを読みながら状態取得方法を再構築している。
- Why it matters: 状態確認のたびに実装の読解が必要になり、recoveryが遅くなる（BL-007）。読み方の誤りが起きやすく、read目的の手組みコードがmutationを伴うAPIに触れる余地もある。smoke testとreal-project migrationの双方で、状態確認のためのコードをその場で書いていた。
- Likely scope: 例えば次をread-onlyで返すcanonical interface（CLI subcommand等）: Project、Roadmap / Phase、current target、next startable、pending mutation、validation結果、execution authority（どのregistry / canonical Skill / 実装で動作しているか）、Git branchとapproved push destination。mutationを開かないこと、networkへ接続しないことを保証する。
- Cross-project impact: あり（全Projectが使う入口になる）
- Backfill likely: no（Project側bootstrapから案内する場合は可能性あり）
- Human confirmation likely: no（read-onlyで、実行能力・外部接続・書込範囲を広げない場合）
- Self-hosting prerequisite: no
- Evidence class: smoke test, real-project migration, code inspection

### BL-007 Cold-start recovery performance

- ID: BL-007
- Title: Cold-start recovery performance
- Status: INVESTIGATE
- Kind: implementation, interface
- Problem: fresh sessionでProjectの現在地を回復するまでに長い時間がかかる。bootstrap → registry validation → router → Skill読込 → 状態取得の各段と、実装内で繰り返される全entity読込・構造validation・Git status呼出しのどこに時間を要しているかは計測されていない。
- Why it matters: 「fresh sessionが正本だけから現在地を回復できる」というWorklineの前提の実用性を下げ、長い回復時間は手順の省略や推測を誘発する。real-project migrationでは、fresh sessionからのrecoveryに要した時間の長さが（欠陥ではないが）改善候補として挙がった。
- Likely scope: performance measurement（段階別の所要時間、Project規模・record数との関係）、bootstrap / routing手順、状態読込とvalidationの重複、Git呼出し回数、BL-006との統合
- Cross-project impact: あり（全Projectのrecovery）
- Backfill likely: no（Project側bootstrapの手順を変える場合は可能性あり）
- Human confirmation likely: no（routingの意味やauthorityを変えない高速化の場合）
- Self-hosting prerequisite: no
- Evidence class: real-project migration, code inspection

### BL-008 Python interpreter resolution

- ID: BL-008
- Title: Python interpreter resolution
- Status: RESOLVED
- Kind: interface, procedure
- Problem: 実装は `requires-python >=3.11` を要求するが、環境の既定 `python` がそれ未満の場合がある。READMEの起動手順は `python -m workline.cli` だけで、install方式や使うinterpreterを定めていないため、実行のたびに対応するinterpreterの探索とimport path（`PYTHONPATH` 等）の指定が必要になる。Roadmap / START等の操作にはCLI entryがなく、Python APIを直接呼ぶ必要もある。
- Why it matters: Claude等のAIが毎回interpreter探索とimport path設定をやり直すことになり、誤ったinterpreterでの失敗や、どの実装コードが実行されたかの曖昧さが生じる。canonical implementation firstの原則上、実装を起動できなければSTOPになるため、起動手順の不安定さがそのまま作業停止につながる。smoke testとreal-project migrationの双方で都度の起動設定が必要になり、既定interpreterでは実装をimportできない失敗も発生した。
- Likely scope: canonical runtime / launcher / interpreter policy（install方式、entry point、interpreter選択規則、version検査）、READMEやcanonical Skillに書く起動手順、CLI entryの範囲（BL-006と連携）。smoke testで挙がった「PYTHONPATHとCLIの使い勝手」の指摘はこの項目へ統合した。
- Cross-project impact: あり（全Projectからの実装起動方法）
- Backfill likely: 可能性あり（Project側bootstrapから起動手順を案内する場合）
- Human confirmation likely: yes（実行環境・launcher等のtooling構成の変更）
- Self-hosting prerequisite: partial（BL-013 の runtime / development separation と関係する）
- Evidence class: smoke test, real-project migration, code inspection
- Resolution: Workline root直下のcanonical source launcher（`run-workline.py`）から起動する形を正式にした。installせずWorkline rootの `src/workline` を実行し、Python 3.11以上（exact versionは固定しない）とisolated invocation（`-I`、bytecodeを書かない）を要求する。launcherと実装内の照合は、実際にloadされたimplementationのoriginとconfigured Workline rootを比べ、不一致・証明不能は成立済みProjectのexecution lockより前に何も書かずSTOPする（Project開始・validate-projectも同様）。CLIの無いoperationは、同じprocessでactivateした後にだけAPIをimportする。ambient install・editable install・PYTHONPATHには依存せず、project.yaml schema・bootstrap・intent / event形式は変えず、既存Projectのbackfillは不要。committed / released versionやruntime / development separationは扱っていない（BL-013）。security sandboxではない。`rules/git` とcanonical Skills、READMEへ反映済み。

### BL-009 Historical deleted Related / must_read semantics

- ID: BL-009
- Title: Historical deleted Related / must_read semantics
- Status: RESOLVED
- Kind: spec, implementation
- Problem: completed Workの `must_read` 等のRelated targetが、後続のWorkで意図的に削除された場合の意味が定義されていない。historical evidence（そのWorkが当時何を読んだか）と、現在の参照が有効か（current validity）を区別する規則がなく、構造validationはRelated targetの存在を検査しない。意図的なtracked file削除はWork resultとして扱えるようになり、must_update / realizesでの扱いは定義済みだが、must_readを含む履歴側Relatedの扱いは未定義のまま。started / completed等のWorkに対するRelated maintenanceも、現行仕様では定義していない。
- Why it matters: 削除済みtargetを指す履歴参照を「壊れた参照」と誤判定するか、逆に死んだ参照を現在の読取計画に残すかの判断が場当たりになる。旧authorityを退役させる移行では、履歴Relatedのtarget削除が必ず起きる。real-project migrationでは、completed Workのmust_read targetが後続Workで意図的に削除され、その扱いを都度判断した。
- Likely scope: historical evidenceとcurrent validity validationの分離、`rules/information-tracing`（読取順とhistorical参照）、`skills/start` / `skills/roadmap` のRelatedの意味、構造validation、reading planの生成
- Cross-project impact: あり
- Backfill likely: no（意味を定義するだけの場合）
- Human confirmation likely: yes（canonical semanticsの変更）
- Self-hosting prerequisite: no
- Evidence class: real-project migration, code inspection
- Resolution: Related edgeを、from Workが実行し得る間の現在のread obligationと、terminal Work（completed / cancelled / plan_excluded）のhistorical evidenceに分けた。historical側は記録のまま保持し、後続Workがtargetを正式に削除してもedgeを削除・書換えせず、targetの不在だけでstructure validationをfailさせない。現在側は、STARTがWorkを実行する前にmust_read / 条件が現在成立しているconditional_must_read / obey chainのtargetを解決し、解決できなければlifecycle event・executor実行・Git書込みより前に `related_target_missing` でSTOPする（推測で無視せず、filename / mtime / 意味類似での代替も自動修復もしない）。Workline自身がこの状態を作らないよう、deletion resultが他のstarted（in_progress / held）non-terminal Workの現在のread targetを消す場合はcompletion precheckで失敗させる。削除するWork自身はtargetが存在する状態で開始しているため対象外。未開始Workは従来どおりRoadmapのRelated maintenanceで計画修正し、started Work用のRelated編集経路は新設していない。既存Projectへも即時適用し、grace periodやlegacy modeは設けない。historical record自体の訂正はBL-020へ要件として追記した。relation type・record schema・project.yaml・event・backfillの変更なし。`rules/information-tracing` / `rules/ai-decision` と `skills/start` / `skills/roadmap` へ反映済み。

### BL-010 Generated artifact hygiene

- ID: BL-010
- Title: Generated artifact hygiene
- Status: RESOLVED
- Kind: hygiene
- Problem: workline-coreに `.gitignore` がなく、test実行やpackage metadata生成で作られる `__pycache__/`、`*.egg-info/` 等がuntrackedとして作業ツリーに残る。
- Why it matters: `git status` のdirty noiseになり、意図した変更の確認を読みにくくし、誤ってcommitされる余地を残す。
- Likely scope: workline-coreの生成物policyと `.gitignore`（Python bytecode、packaging metadata、test cache等）
- Cross-project impact: no（workline-core repositoryのみ。Project側のruntime recordはBL-019）
- Backfill likely: no
- Human confirmation likely: no
- Self-hosting prerequisite: no
- Evidence class: code inspection, self-hosting assessment
- Resolution: repository rootへ `.gitignore` を追加し、実測で生成を確認した `__pycache__/`、`.pytest_cache/`、`*.egg-info/` の3 patternだけをignore対象にした。genericなPython templateは使わず、このrepositoryが生成しない `build/`、`dist/`、`.coverage`、`.mypy_cache/` 等は入れていない。READMEのdevelopment test例も `py -3 -B -m pytest tests -q` にし、`-B` はworking treeへbytecode cacheを残さないためのdevelopment hygieneであって、Runtimeの起動条件ではないことを明記した。ただし主たる修正は `.gitignore` である。test suiteはcanonicalでない起動形を検証する子processを起動するため、`-B` を付けたfull runでも `src/workline/__pycache__` に23件が残ることを実測した。tracked file全件がignoreされないことを機械的に確認しており、正本である `registry.md` とcanonical Skillsが隠れることはない。既にworking treeにある生成物は削除していない。このrepositoryの履歴で一度もtrackedになっていないためindexから外す必要がなく、手元のcopyは各開発者の持ち物だからである。`pyproject.toml`、`registry.md`、canonical Skillsは変更しておらず、canonical runtimeのsemanticsは変わらない。canonical runtimeが `-B` の有無にかかわらずbytecode cacheを書かないのは、launcherがWorkline importの前に `sys.dont_write_bytecode` を設定するためであり、既存のcanonical記述が正しいことを実測で確認した。workline-core repository自身のdevelopment hygieneであり、Projectへ及ぶ影響はない（Workline operationがProjectのignore設定を書かない規則はBL-019）。

### BL-011 Reusable migration procedure

- ID: BL-011
- Title: Reusable migration procedure
- Status: OPEN
- Kind: procedure
- Problem: 既存Projectを旧authority（Workline以前の手順・文書・自動化）からWorklineへ移す手順が、再利用可能な形で存在しない。最初の移行は、その場の指示とRoadmap設計で進められた。
- Why it matters: 移行のたびに手順を設計し直すと、ownership inventoryの漏れ、local safety ruleの喪失、旧authorityとの二重正本、standalone recoveryの未確認が起きやすい。
- Likely scope: 再利用可能なprocedure / checklist: ownership inventory（どの文書・Skill・自動化が何のauthorityか。project-local Skillの棚卸しと扱いを含む）、local safety preservation、current state migration、legacy authority retirement、standalone recovery（fresh sessionが正本だけから回復できるか）、final achievement。置き場（非正本のguideか、canonical Skill / ruleか）も決める。設計段階で保留したProject-specific Skillの標準配置の論点はここで扱う。
- Cross-project impact: あり（今後の移行すべて）
- Backfill likely: no
- Human confirmation likely: yes（canonical Skill / 共通ルールにする場合。非正本guideなら不要）
- Self-hosting prerequisite: no
- Evidence class: real-project migration, design deferral

### BL-012 Unsupported self-hosting guard

- ID: BL-012
- Title: Unsupported self-hosting guard
- Status: RESOLVED
- Kind: implementation, spec
- Problem: Project開始は、Project rootとWorkline rootが同一（Project root == Workline root）であることを拒否しない。self-hostingは正式にサポートされていないが、現状では機械的に初期化できてしまう。
- Why it matters: 意図せずWorkline root自身がProjectになると、変更対象と、それを統治・回復するrule / 実装が同じ作業ツリーに入る（循環）。さらに、Workline root側から行うpre-project操作と「成立済みProjectの内側」という文脈が衝突する（BL-013）。
- Likely scope: self-hostingを正式サポートするまでの扱いとして、STOP / explicit human confirmation / unsupported-mode validation のどれを採るべきかを調査する。Project開始と既存Project向けmaintenance経路での検出、BL-013の再評価条件との結び付け。
- Cross-project impact: no（通常Projectの開始挙動は変えない想定）
- Backfill likely: no
- Human confirmation likely: yes（Project開始の拒否条件の追加はcanonical Skillの変更）
- Self-hosting prerequisite: yes（正式サポートまでのguard）
- Evidence class: code inspection, self-hosting assessment
- Resolution: Project rootとそのWorkline rootが別の実体directoryだと証明できない配置を、現在のWorkline rulesではサポートしないself-hostingとして扱うguardを入れた。判定はfile identity（`os.path.samefile` 相当）で行い、同じdirectory、または両方存在するのに判定できない場合はfail-closedで `workline_self_hosting_unsupported` としてSTOPする。Project開始はpre-project contextの確認の後、git init・`.workline`・bootstrapより前に止まる。既にこの配置の成立済みProjectでも、state-changing operationはProject contextの後、Workline implementation照合とexecution lockの前に止まり、何も書かない。読み取りは引き続き可能で、validate-projectはこの配置をproblemとして報告しPASSにしない。overrideや自動修復は設けず、project.yaml・bootstrap・intent / event形式は変えず、既存Projectのbackfillは不要。self-hostingの正式サポートはBL-013で扱う。`rules/git` とproject-start Skill、READMEへ反映済み。

### BL-013 Self-hosting readiness

- ID: BL-013
- Title: Self-hosting readiness
- Status: DEFERRED
- Kind: design
- Problem: workline-coreをWorkline自身で管理する（self-hosting）前提が揃っていない。現時点ではself-hostしない判断をしており、次の条件が揃った時点で再評価する。
  - runtime / development separation（他Projectを統治する実装と、開発中の作業ツリーの分離）
  - release boundary（どの版がProjectを統治するか。設計段階で保留したbranch strategyを含む）
  - intent format compatibility policy（mutation recovery recordの形式互換）
  - break-glass / repair / reconcile path（実装が壊れた時の修理手順と、記録との突き合わせ）
  - session / Project context rule（Workline root側の操作とProject内の操作の区別。BL-001）
  - public/private disclosure policy（public repositoryへ計画記録を置く範囲）
  - portable root handling（Project設定に保存されるWorkline rootの絶対pathの可搬性）
- Why it matters: 前提なしにself-hostすると、Workが自身の完了条件を統治するruleや実装を変更でき、recovery recordの形式変更で自身のpending mutationを再開不能にし得る。実装が壊れた時は、manual fallbackを禁じる原則によりWorkline経由での修理ができない。また、全Projectが共有するWorkline rootの作業途中状態が各Projectへ露出する。real-project migration中にも、Workline root側の破壊的変更により、移行途中のProjectでmaintenance操作が必要になった。
- Likely scope: 上記条件ごとの設計、BL-012のguard、BL-001 / BL-008 / BL-019 との整合
- Cross-project impact: あり（Workline rootを共有する全Project）
- Backfill likely: 可能性あり（root handlingや実行環境を変える場合）
- Human confirmation likely: yes
- Self-hosting prerequisite: n/a（この項目自体が再評価条件）
- Evidence class: self-hosting assessment, real-project migration, design deferral

### BL-014 Deterministic tie-break among startable candidates

- ID: BL-014
- Title: Deterministic tie-break among startable candidates
- Status: RESOLVED
- Kind: spec, implementation
- Problem: startable Work（またはPhase）が複数ある場合、仕様は人間の明示意図・planned_next・dependency・priority・parallel safety等から選ぶと定めるが、最後のtie-breakが実装上の候補listの先頭になっている。START outer continuationの次Work選択、Roadmapのhandoff / Phase entryでのentry Work選択、startable Phaseの選択が該当する。
- Why it matters: 選択結果が実装の内部順序に依存し、仕様から予測・再現できない。候補の並び順を変える実装変更で、挙動が黙って変わる。
- Likely scope: `skills/roadmap` / `skills/start` の複数候補時の最終規則（現行順序の明文化か、新しい決定規則か）、START next Work選択、Roadmap handoff / Phase entry / Phase選択の実装とtest
- Cross-project impact: あり（選択結果が変わり得る）
- Backfill likely: no
- Human confirmation likely: yes（選択規則の意味を変える場合。現行順序の明文化だけなら不要）
- Self-hosting prerequisite: no
- Evidence class: smoke test, code inspection
- Resolution: 候補の先頭を取る4経路（startable Phase選択、Phase entryのentry=None、Roadmap handoffのentry_work_id=None、STARTのouter continuation）を、`ProjectView.planned_next_preference` / `choose_startable` という1つの規則へ統一した。規則は既存のstartable Phase選択が持っていた2段構成をそのまま一般化したもの: (1) 既にcompleted / completeなentityが `planned_next` で次に推奨している候補、(2) そのうち、まだ終わっていないentityから `planned_next` で前に置かれていない候補。各段は結果が残る場合だけ適用する。cancelled / plan_excludedのpredecessorは二度と完了しないので候補を後ろへ押さえない（`skills/roadmap` / `skills/start` のterminal/inactive planned_nextの扱いに合わせた）。1件に決まればそれを選び、2件以上残れば `ambiguous_startable_candidates` でSTOPし、診断に候補IDを含める。既存のstartable Phase選択は `planned_next` の走査を候補起点のedgeに限っていたため、計画が順序を与えている場合でも先頭選択へ落ちていた。その制限を外したことが一般化の実体であり、`planned_next` の意味は拡張していない。調査で再現した欠陥をすべて直した: Phase entry（entry=None）とhandoffは `planned_next` を全く見ていなかった（宣言順の先頭を選んでいた）。STARTは直前に完了したWork由来の `planned_next` しか見ていなかった。startable Phase選択は同格の `planned_next` が複数あるとrelation recordの記録順で勝者が変わった（同一graphの記録順を入れ替えるだけで結果が反転することを確認済み）。最終的な先頭選択はULID順（作成ミリ秒＋80bitの乱数）であり、ID予約間のdisk書込みが偶然ミリ秒境界を跨ぐことだけが宣言順を保っていた（同一ミリ秒に収まると一様乱数になることを実測）。実行順として、候補listの先頭・ID / ULID順・relation fileの記録順・宣言順・表示番号・dict / listの挿入順のいずれも使わない。regression testは、ULIDを逆順に発行する場合・同一ミリ秒に発行する場合・同一graphのrelation記録順を入れ替えた場合のそれぞれで、一意なら同じ候補、曖昧なら同じくSTOPになることを確認する。人間の明示意図は従来どおり優先し、`select_phase(explicit=...)`・`design.entry`・`handoff(entry_work_id=...)` はいずれも自動選択より先に効く。priority / external constraint / target / parallel safetyは、schemaにもrelationにもimplementationにも存在しないため今回は実装せず、`skills/roadmap` 上で「現在適用する正式条件」から外して「将来の設計候補（現在は未実装）」として明確に分離した。存在しない条件を適用したつもりで候補を絞らない。STOPはcanonical stateを変更する前に行う: Phase entryは新規展開のdesignがentry Workを一意に定めない場合、ID予約・mutation開始・entity書込み・commit / pushのいずれよりも前に、既存の明示entry検査と同じ位置でSTOPする（実測: HEAD不変・Work 0件・event 0件・pending mutation 0件）。handoffはSTARTを呼ぶ前にSTOPし、executorを動かさない。中断した展開のresumeはこの検査の対象外とし、BL-023のとおり同じmutationで最後まで進める。その場合にentry Workが一意に決まらなければ、finalize済みの展開を失敗させずに `entry_work_id` を `None` として返し、実行はhandoffが明示を求める。STARTのouter continuationは、直前に完了したWorkのfinalizationを通常どおり閉じたうえでstatus `stopped` として返し、stop理由に候補IDを含める。pending mutationは残さない（実測）。schema・event・relation type・project.yamlは変更しておらず、backfillは不要。既存Projectで影響するのは今後の選択だけで、記録済みの履歴は変わらない。`skills/roadmap` / `skills/start` へ反映済み。「表示番号を実行順として使わない」は維持した。

### BL-015 Silent no-op on Phase re-entry

- ID: BL-015
- Title: Silent no-op on Phase re-entry
- Status: RESOLVED
- Kind: interface, implementation
- Problem: 既にWorkが展開されたPhaseへPhase entryを再実行すると、重複展開はしない（仕様どおり）が、渡したWork設計が既存構造と食い違っていても検証も警告もされずに無視される。結果には「展開しなかった」ことを示すflagがあるだけで、呼び出し側が見落とすと、意図したWorkが登録されたと誤認し得る。
- Why it matters: 別Phaseの設計を取り違えて渡す等の誤りを検出できず、計画と正本がずれたまま実行へ進む可能性がある。
- Likely scope: 再entry時の結果（食い違いの検出・警告・STOPの要否）、`skills/roadmap` のPhase entry記述、test
- Cross-project impact: あり（Roadmap APIの挙動。影響は小さい）
- Backfill likely: no
- Human confirmation likely: no（診断を追加するだけの場合。STOPへ変える場合はyes）
- Self-hosting prerequisite: no
- Evidence class: smoke test, code inspection
- Resolution: 既にWorkが展開されたPhaseへ新しいentry designを渡すPhase entryの再実行を、state変更より前に `phase_already_expanded` でSTOPすることにした。designを黙って無視せず、既存entryへ差し替えず、通常成功として返さず、ID発行・mutation開始・canonical state変更・commitも行わない。診断にはPhase IDと「既に展開済みなので新しいentry designを受け付けない」ことを含める。Roadmapがactiveでない場合、Phaseがcomplete / held / cancelled / plan_excludedの場合、dependencyが未充足の場合は、従来からあるそれぞれの診断が優先する（そちらの方が具体的な理由を示すため、順序は変更していない）。いずれの経路でもcanonical stateは変更しない。「既存構造と同じdesignか」の機械照合は実装していない（graph同型判定・対応付け探索・design key復元を含む）。設計調査の結果、designの比較はlabel付きgraph同型判定に相当し、endpoint解決順序・特殊Work比較・曖昧性・探索の完全性という独立した正しさの要件を伴うため、欠陥の規模に対して複雑すぎると判断した。展開済みPhaseの現在構造はread-onlyで読み、計画の正式変更はRoadmap側の経路で行う。併せて、明示entryの妥当性をdomain write・mutation effect・Git commit / pushより前へ移した。従来は初回展開をfinalizeした後に「entry Workがstartableでない」と判明して例外を返しており、成功してcommit済みのoperationにAPIが失敗を返していた（再現済み: HEAD進行・Work commit済み・mutation completed・validate_project PASSと同時にSpecViolation）。新しい検査はdesignと既存Workの状態だけから判定する: entryがdesignのWork keyにない場合、この展開が作るWorkの完了を待つ場合はSTOPし、既存Workの完了を待つ場合はそれがcompletedなら妥当とする。rollbackでは直していない。空design・reserved keyの検査も展開済み判定の後へ揃え、初回とre-entryで意味が逆転しないようにした。BL-014の一般tie-break（entry=None時の複数startable候補選択）は変更していない。このPhase自身のPhase entryが中断してpending mutationが残っている状態は呼び出し側の再実行と区別し、`phase_already_expanded` として扱わず、pending recordをabandon・削除しない。その中断状態のresume自体はBL-023が扱う。schema・event・relation type・project.yamlは変更しておらず、backfillは不要。`skills/roadmap` へ反映済み。

### BL-016 Default result commit message type

- ID: BL-016
- Title: Default result commit message type
- Status: RESOLVED
- Kind: implementation
- Problem: STARTがWork成果をcommitする際、messageの指定がなければ既定で `feat(workline): ...` になり、Workの性質（docs / fix / chore等）に関わらず `feat` typeが付く。
- Why it matters: Conventional Commits等を運用するrepositoryでは履歴の分類が不正確になり、message指定が実質必須になる。
- Likely scope: 成果commitの既定messageの決め方、executorからtypeを渡す手段、`skills/start` の記述
- Cross-project impact: あり（全Projectの成果commit履歴）
- Backfill likely: no
- Human confirmation likely: no
- Self-hosting prerequisite: no
- Evidence class: smoke test, code inspection
- Resolution: 成果commitの既定messageから固定の `feat` を廃止し、neutralな `chore(workline): <display> <name>` にした。成果がfeat / fix / docs / refactorのどれに当たるかはproduct判断であり、Work名・成立状態・work_kind・成果fileの内容から推測しない。推測経路は追加していない（名前の語からのtype判定、work_kindからのtype導出、成果diffの解釈を含む）。再現のとおり、変更前はfeature・bugfix・document-only・deletion-onlyのいずれのWorkでも `feat(workline): ...` が付き、同じWorkのfinalize commitが `chore(workline): complete ...` であるため、1 Workが矛盾するtypeの2 commitを残していた。既定messageは他のWorkline生成commit（create / expand phase / complete / hold / cancel等、およびProject開始の固定message `chore(workline): initialize project`）と同じ側へ揃えた。executorが `Completed(message=...)` で明示したmessageの扱いは変更していない。そのままverbatimで使い、prefixを足さず、typeを変換せず、形式も検査しない（Conventional Commits validatorは追加していない）。messageは必須化していない。create / modify / deleteの成果は従来どおり同一owned setとして1 commitにまとめるため、deletionだけの成果も同じ既定messageになる。owned result fileが無いWorkが成果commitを作らない挙動も変えていない。commit typeを読むconsumerはrepository内に無く（CI・changelog・release自動化・commit message parser・hookのいずれも存在しない）、過去commitは書き換えていない。schema・event・relation type・project.yamlは変更しておらず、backfillは不要。canonical authorityはConventional Commitsを要求しておらず、allowed type一覧も持たないため、この既定はneutral defaultであってtype体系の採用ではない。`skills/start` へ反映済み。BL-014のstartable候補のtie-breakは変更していない。（review hardening）空白だけのmessageを「messageを渡さなかった」と同義とし、neutral defaultにする。従来は空白だけのmessageがgit_commitのvalidationでSTOPし、executor実行後のためpending mutationが残って後続operationを `reconcile_required` で止めていた。変えたのは空白だけの文字列の扱いだけで、他の入力の意味は従来の `message or default` と同一に保つ: None・空文字・その他のfalsyな値は従来どおりdefault、文字列でないtruthyな値は従来どおりそのまま渡りgit_commitのvalidationで拒否される（正常入力へ昇格させず、独自の拒否も追加しない）。判定にだけ空白を無視し、中身のあるmessageはstripせずそのままcommitする。

### BL-017 Declared write scope broader than actual writes

- ID: BL-017
- Title: Declared write scope broader than actual writes
- Status: RESOLVED
- Kind: implementation
- Problem: Roadmap operationは種類に関わらず、予定write scopeのfilesとして共通ledger（roadmap relations / related / events）の3ファイルを固定で宣言する（例: Phase entryはeventを書かず、Phase hold / resumeはrelationを書かない）。STARTも同じ3ファイルを固定で宣言する。
- Why it matters: pending mutationとのoverlap判定は宣言されたwrite scopeで行われるため、実際には独立なoperation同士も重なりありとして `reconcile required` になる。現時点で実害は確認されていないが、中断や並行作業の際に不要な停止の原因になり得る。
- Likely scope: operationごとの予定write scopeの精密化（意図的に直列化しているなら、その明文化で足りるかの判断を含む）、Mutation Controllerのoverlap判定との整合、既存pending recordとの互換、BL-021との関係
- Cross-project impact: あり（operation同士の衝突判定）
- Backfill likely: no
- Human confirmation likely: no
- Self-hosting prerequisite: no
- Evidence class: smoke test, code inspection
- Resolution: 各Roadmap operationが共通ledger 3ファイルを一律に宣言するのをやめ、そのoperationが最後まで進んだ場合に書き得るcanonical fileだけを予定write scopeとして宣言するようにした。実測での再現: pendingの `phase-hold`（実際にはevent logしか書かない）が `work-related-maintenance`（related.yamlだけ）と `add_phases`（roadmap.yamlだけ）をいずれも `reconcile_required` で停止させ、pendingの `work-related-maintenance` と pendingの `roadmap-create` も同様に、1ファイルも共有しない `phase-hold` を停止させた。計4組を再現し、recordのdeclared filesと実effectのfilesを並べて、原因がdeclared scopeの重なりだけであることを確認した。宣言内容はoperation種別から静的に決まる: Roadmap作成 / Phase追加 = `relations/roadmap.yaml`、Phase entry = `relations/roadmap.yaml` + `relations/related.yaml`（lifecycle eventではないのでevent logを書かない）、Roadmap / Phaseの hold / resume / cancel と achievement記録 = `events/events.jsonl`、plan exclusion（Phase / Work）= replanがWork登録とrelation変更を行い得るため3つとも、既存未開始WorkのRelated maintenance = `relations/related.yaml`。direct standalone CREATEも `relations/related.yaml` だけに狭めた（relation payloadを渡さずlifecycle eventも記録しないため、他の2つはこの経路では書き得ない）。STARTは変更していない: question wait / completion / hold / derive の各経路を実測し、宣言している3ファイルがそのまま書き得る集合であることを確認した（start-plan-excludeもreplan経由で3つとも書き得る）。`project-start` / `push-destination-pin` / `bootstrap-backfill` は元から自分のpath listを宣言しており実測でも一致していたため変更していない。「実際に書いたfile」ではなく「書き得るfile」を宣言する: scopeはどの経路を通るか決まる前に宣言するので、条件付きでしか書かないfile（Phase relationを伴うRoadmap作成、Relatedを伴うPhase entry、Workを登録するreplan）も含める。共有ledgerはfile単位で書き直すため、同じledgerを書き得る2 operationは独立ではなく、両方が宣言する。検証: 13のRoadmap operation・START・direct CREATEを一通り実行し、記録された全effectのledger集合が宣言集合の部分集合であること（under-declarationが無いこと）を確認した。scopeを決めていないoperation名は全ledgerを宣言するfail-closedとし、過大宣言の側へ倒す。既存pending recordのwrite scopeは書き換えない: 旧実装が記録した広いscopeはそのまま尊重され、resume時の比較は新旧scopeの和集合で行われる。backfillもrewriteもしない。entity scopeは元から精密だったため変更していない（`add_phases` がRoadmap entityを宣言する点だけは実書き込みより広いが、同一Roadmapの計画変更同士を競合させる意図として妥当なので残した）。BL-021のProject execution lockとは責務が別で、lockは同時実行の直列化、write scopeは中断して残ったpending mutationからの独立性判定であり、lock側は変更していない。write scopeはeffectに対する許可ではなく衝突宣言であり（`MutationController.open` 以外に読む場所がない）、狭めてもoperationが書けるものは変わらない。挙動上の帰結として、Phaseの展開が中断して残っているRoadmapでも hold / resume ができるようになった（展開recordは無変更のまま残り、Roadmapをresumeすれば同じmutationで完了することを実測）。`skills/roadmap` と `skills/create` へ反映済み。 再OPENと修正（第三者reviewで実再現されたMAJOR regression）: 上記のscope精密化の結果、同じfileは書かないが、他のmutationが決めたlifecycle factを実行可否の判断に読むoperationが独立扱いになっていた。実測: `roadmap-cancel` が `roadmap_cancelled` をintentへ記録済み・未適用の間に `enter_phase` が通ってWork登録とcommitまで到達し、後からcancelをresumeすると、cancelled RoadmapにWorkが残り `validate_project` もcleanだった。`work-related-maintenance` と `roadmap-hold` でも同じだった。原因は、write scopeが「同じfileを書くか」しか表さず、event logを書かないoperationがevent log由来のlifecycle factを読む依存を表せないこと。露出するのは「自分がdeclareしないentityのlifecycleを読み、かつevent logをdeclareしない」operationで、全Roadmap operationを調べてPhase entryとRelated maintenanceの2つだけと特定した（Phase追加はRoadmap entityを、lifecycle・achievement・plan exclusionはevent logをdeclareしており、既に守られている）。write scopeを広げ直すと重なりが対称になり、途中まで展開したPhaseがRoadmapのhold / resumeを止める等の、上で解消した不要停止が復活するため、そうしなかった。代わりに読む側だけの一方向の確認として、読むentity（Phase entry = 対象Roadmapと対象Phase、Related maintenance = 対象Roadmapと対象Work）について、未完了mutationが記録済みで未適用（event logに存在しない）のlifecycle eventを持つ場合に限り、mutationを開かず何も書かずに `reconcile_required` で停止するようにした。停止理由にはmutation・owner・entity・event種別を含める。適用済みのeventはcurrent stateに現れるので、通常のprecondition（cancelled / held）が報告し、この確認は停止理由を上書きしない。新たに停止する組合せは「対象Roadmapの未適用lifecycle決定 × そのRoadmap内のPhase entry / Related maintenance」だけで、Phaseの未適用lifecycle決定×そのPhaseのentry、Workの未適用lifecycle決定×そのWorkのmaintenanceは従来もentity scopeの重なりで停止しており、停止理由が明確になっただけ。停止しないことを実測した組合せ: 別Roadmapの未完了lifecycle決定、同じRoadmapの別Phaseの未完了決定、途中まで展開したPhaseがある間のRoadmap hold / resume、未完了のRelated修正がある間のPhase hold、自分自身のpending recordのresume。依存は読む側から決める側への一方向だけなので循環せず、未適用のlifecycle決定は常に先にresumeでき、その後で中断した展開も同じmutationで完了することを実測した（途中展開とhold決定が同時にpendingの状態から、最後はpending 0・validate clean）。Phase entryが依存関係として読む先行Phaseの未完了lifecycle決定は対象にしない: relationを再計画せずに先行Phaseをcancelするとcancel自身のpostcheckが停止させるので、Phase entryを止めなくても不整合は作られない（実測）。テストは、中断を `_finalize` 手前（適用済み・未commit）でしか作れなかったhelperへ記録済み・未適用の窓を加え、既存の独立性・競合テストを両方の窓で実行するようにした。lifecycle operationがevent適用後・commit前に中断すると自分自身のpreconditionでresumeできない件は別の既存問題であり、この修正には含めていない（この確認はlifecycle operationに掛けていないので、その件を悪化させない）。`skills/roadmap` へ反映済み。

### BL-018 Unused assignment in Phase expansion

- ID: BL-018
- Title: Unused assignment in Phase expansion
- Status: RESOLVED
- Kind: hygiene
- Problem: Phase展開の実装で、構造検査の戻り値を代入した変数が使われないまま、直後に状態の再読込結果で上書きされている。動作には影響しない。
- Why it matters: 構造検査の結果を後続で使っているように読め、保守時の誤読を招く。
- Likely scope: 該当実装の整理（構造検査は失敗時のSTOPのためだけに呼ぶ意図の明確化）
- Cross-project impact: no（挙動は変わらない）
- Backfill likely: no
- Human confirmation likely: no
- Self-hosting prerequisite: no
- Evidence class: smoke test, code inspection
- Resolution: Phase展開の構造検査について、未使用の代入 `after = ` だけを削除し、`_stop_on_structure(store, "phase structure check")` の呼出しは保持した。この呼出しはcommit / pushより前に置かれた唯一の構造ゲートであり、`_finalize` は `gitops.finalize`（git_commit / git_push effect）を適用してから自身の postcheck を走らせるため、呼出しごと消すと構造不正な展開がcommit・pushされた後にしかSTOPできなくなる。canonical Skill `skills/roadmap` の順序（Phase構造検査 → commit / push）もこの呼出しを要求している。意図が読み取れるよう、値のためではなく失敗時のSTOPのために呼んでいることをcommentで明記した。挙動・schema・canonical semanticsは不変で、canonical wordingの変更もbackfillも不要。代入された値が読まれないことはAST上で確認済み（`after` の出現はStore / Store / Loadの3つのみ、唯一のLoadは上書き後、間の文は `after` を読まず、分岐・try・closureなし）。この代入は初出commit `2ba599f` から存在し、linter未設定のため検出されていなかった。既存testは呼出しの有無を一切固定していなかった（呼出しごと削除したmutantでもfocused 67件が全passする実測）ため、`ExpansionStructureCheckTests` を1件追加した: 展開自身の構造検査を失敗させ、`structure_invalid` / `phase structure check` でSTOPし、かつHEADが動いていない（commit・pushに到達していない）ことを固定する。このtestは修正前・修正後いずれもPASSし、呼出しを削除したmutantではHEADが進んで失敗する。実測: focused 68 passed / 36 subtests、full suite 532 passed / 2 skipped / 397 subtests、validate-registry PASS。

### BL-019 Runtime recovery record retention and ignore policy

- ID: BL-019
- Title: Runtime recovery record retention and ignore policy
- Status: RESOLVED
- Kind: design, implementation, hygiene
- Problem: `.workline/runtime/` のmutation recovery recordは完了後も残り続け、各operationの開始時には完了済みを含む全recordが読み込まれ、形式versionが検査される。放置されたpending record（stale mutation）の扱いは設計段階で保留したままで、完了済みrecordの保持期間やcleanup経路もない。また、Project開始はruntime領域をGitのignore対象にしないため、Projectによってはruntime recordがuntrackedとして表示され続ける。
- Why it matters: recordの蓄積は起動時のコスト（BL-007）を増やし、形式を変更した時に過去record全体が互換性の問題になる（BL-013 の intent format compatibility policy）。untrackedのruntime recordはdirty noiseになり、Workline以外のtoolから誤ってcommitされる余地がある。一方で、cleanupを誤るとrecoveryに必要な情報を失う。
- Likely scope: 完了済み / abandoned / stale pendingの各recordの保持とcleanup規則（`rules/git` のcleanup定義との整合）、runtime領域のignore方針（Workline-owned範囲に置くignoreの是非を含む）、形式versionの互換方針
- Cross-project impact: あり
- Backfill likely: yes（既存Projectのruntime ignoreと既存record）
- Human confirmation likely: yes（cleanup規則は共通ルールの変更。Projectのignore設定を変える場合も）
- Self-hosting prerequisite: partial（BL-013 の intent format compatibility policy と関係する）
- Evidence class: design deferral, smoke test, real-project migration, code inspection
- Resolution: closeしたrecovery recordはresumeの対象ではないため、completedとしてcloseしたmutationが、自分が今回作成したrecord自身を削除することにした。削除は次をすべて機械的に示せる場合に限る: 今回の実行が作成し既存recordをresumeしていない / top-level fieldがcloseしたrecordのfield setと完全一致しversionがint型そのものである / 通常のregular fileであり間接参照でない / Gitのindexにも HEADにも存在しない（判定できないgit呼び出しは「存在する」として扱う）/ 内容がそのmutationが最後に書いたものと完全一致する。1つでも示せなければrecordを残す。resumeしたmutationのrecordを削除しないのは、operationが待っている間に第三者が加えた変更がloadで許容されsaveで書き戻されるため、最終的な内容が作成者の証明にならないからである。indexとHEADを別に問うのは、`git rm --cached` でindexから外れてもHEADが保持しているrecordを削除するとcommit済みの変更を消すことになるからである。削除はoperation自身の後始末でありresultではないため、全effect・Git stage・構造postcheckの後に行い、失敗してもoperationは成功のままとし、そのためにdomain effectを再実行しない。cleanupが中断して残ったcompleted recordは次回も通常どおり読める。pending record、abandoned record、resumeしたmutationのrecord、他のmutationが残したrecordは削除しない。abandoned recordを残すのは、Project開始がそのfolderをやり直してよいと判断できる唯一の証拠だからである（BL-022）。既存Projectに既に蓄積したrecordは今回の対象とせず、保持期間・件数上限・一括cleanup・maintenance commandは設けない。Git ignore設定（Project rootの `.gitignore`、`.git/info/exclude`、runtime配下のignore file等）はWorkline側で作成・変更せず、Projectの既存ignore設定はProject側の所有物として扱う。schema、intent version、project.yaml、event、relation typeは変更しておらず、backfillは不要。残る論点の分離: 狭いwrite scopeを持つ `bootstrap-backfill` / `push-destination-pin` のpending recordは通常作業を止めないため気付かれずに残り得る（その可視化はBL-006のpending mutation報告の範囲）、残存recordのintent format互換方針はBL-013、cold-start性能全体はBL-007、workline-core自身の生成物hygieneはBL-010。`rules/git` とproject-start Skillへ反映済み。

### BL-023 Phase expansion cannot resume after an interruption

- ID: BL-023
- Title: Phase expansion cannot resume after an interruption
- Status: RESOLVED
- Kind: implementation, design
- Problem: Phase entryの展開が、通常Work適用後・integration適用前などで中断すると、pending mutationが残ったままProjectが進めなくなる。再実行したPhase entryは、そのPhase自身のpending phase-entry mutationをrecovery stateとして識別するものの、既にWorkがある場合はresumeせずそのまま返すため、integrationは作られない。一方で他のoperationはそのpending mutationとwrite scopeが重なるため `reconcile_required` で停止する。実測では `add_phases` とWork STARTの両方が停止し、`validate_project` は問題を報告しなかった。
- Why it matters: Project全体が進行不能になり、Workline経由で回復する経路がない。中断したoperationは同じmutationをresumeするという `rules/git` の前提（`registry.md` Multi-write mutation）と `skills/roadmap` の記述が、この経路では成立していない。
- Likely scope: 中断したPhase expansionのrecovery semantics。実測した障害は少なくとも3点: `register_works()` のprojection check（integration invariant等）が `has_stage` guardより前に走るため、適用済みintegrationを持つPhaseで記録済みintegration stageを再投影すると `integration invariant: would have 2 unfinished integrations` で衝突する。`roadmap._open()` が戻る前に `mutation.apply()` を呼ぶため、resume時に記録済みeffectが照合より前に適用される。`abandon_on_stop()` が `mutation.resumed` を見ないため、resumeしたmutationをabandonし得る経路がある。registration coreは共有であり、Phase entry / add_phases / standalone CREATE / STARTのderiveへの横断影響を調べる必要がある。pending recordにdesignを束縛する（または記録済み決定を正本とする）方法、designを伴わない legacy pending recordの扱いも決める。rollbackでは直さない。
- Cross-project impact: あり（全Projectの回復可能性）
- Backfill likely: 可能性あり（既に詰まっているProjectの回復手順）
- Human confirmation likely: yes（recovery semanticsとMutation Controller契約に触れる）
- Self-hosting prerequisite: no
- Evidence class: code inspection, smoke test
- Resolution: Phase entryが、最初のID予約より前に、展開対象のPhaseEntryDesignをmutationのinvocationへcanonicalな形で記録するようにした（通常Workの宣言順・key・name・成立状態・Related、integration、human_confirmationの有無と内容、planned_next、requires_completion、明示entry、記録形式version）。digestではなく内容そのものを保持するため、人がrecordを読んで何を展開していたか分かる。通常Workの順序はdisplay番号を決めるため意味として扱い、並べ替えは別designとする。これにより、中断した展開が「どのdesignに束縛されているか」が最初のdurable recordの時点で確定する。再実行のdesignが一致する場合だけpending mutationをresumeし、既に記録済みのstageはcaller specから決め直さず記録済みeffectをclassify / applyし、IDと結果は記録済みreserved ID / effectから再構成する。未記録stageだけを同一性確認済みのcaller designから決めるため、前半が旧design・後半が新designという混在が構造的に作れない。一致しない再実行は `reconcile_required` とし、pending recordを一切変更しない。実測: 中断点 P0（intent作成直後）/ P1（ID予約後）/ P2（works記録・未適用）/ P4（works適用済み）/ P5（integration記録・未適用）/ P7（integration適用済み）/ P8（confirmation記録・適用）/ P9（finalize前）/ P10（commit後・complete前）のすべてで、同じmutation IDのまま前進して完了し、Work・integration・confirmation・relationの重複なし、validate_projectもclean。以前はP4以降が回復不能で、pending中は `add_phases`・Work START・`hold_phase` がいずれも `reconcile_required` で停止していた。併せて次の2点を直した: 展開済みPhaseの早期returnが `_open` より前にあり pending mutationへ到達できなかった点（このPhase自身のpending phase-entryがある場合はresume pathへ入れる。BL-015の `phase_already_expanded` は、pendingが無い展開済みPhaseに対して従来どおり）。`register_works()` が記録済みstageでもspec由来のreserve / projectionを先に行い、適用済みintegrationを二重計上して `integration invariant: would have 2 unfinished integrations` で衝突していた点（記録済みstageはregistration coreを呼ばずに再構成する。registration core自体は変更していない）。`abandon_on_stop()` はglobalには変更していない: BL-022がresumeしたeffect 0件のmutationをabandonする動作に依存しているため（`test_a_stop_while_resuming_is_retried_as_a_new_mutation`）、Phase entryの内側でのみ、resumeしたmutationをabandonしないようにした。designを記録していない旧実装のpending recordはどの位置でも自動resumeせず `reconcile_required` とし、record・effects・reserved IDs・statusをそのまま保持する（P8 / P10も例外にしない。effect completenessは証明できても、過去に別designが混ざっていないことを証明できないため）。`INTENT_VERSION` とgeneric mutation schemaは変更していない: 記録はphase-entry invocationのoperation-localな意味追加であり、既存のrecord形式でそのまま往復する。明示entryもdesign記録に含むため、resume後の `entry_work_id` は元のdesign.entryから決まる。BL-014の一般tie-breakは変更していない。standalone CREATE / add_phases の共有binding欠陥はBL-024。`skills/roadmap` へ反映済み。

### BL-024 Resume invocation does not bind decided content

- ID: BL-024
- Title: Resume invocation does not bind decided content
- Status: RESOLVED
- Kind: implementation, design
- Problem: 中断したoperationをresumeするかどうかは、mutationのinvocationが一致するかで決まる。しかしinvocationは「何を決めたか」を含んでいないため、同じinvocation identityで内容の違うrequestを再実行すると、記録済みの旧決定を保持したまま新requestへsuccessを返す。実測: standalone CREATEで同じname / keyのまま異なる成立状態を渡して再実行すると、successが返るがstoreされているWorkは旧内容のまま（`create.py` のinvocationは operation / name / key だけで、postcheckも存在確認しか行わない）。`add_phases` も同様に、同じPhase名で異なる成立状態を渡すとD1のPhase本文を保持したままsuccessを返す。
- Why it matters: 呼び出し側は自分が渡した内容が登録されたと解釈するが、正本は別の内容のままになる。中断・resumeが絡む経路でのみ起きるため気付きにくい。
- Likely scope: resume identityが「決定内容」を束縛するための共通契約。Phase entryではBL-023でoperation-localに解決済み（designをinvocationへ記録し、不一致は `reconcile_required`）。同じ考え方をcaller横断の共通contractにするか、各ownerでoperation-localに解くかを決める必要がある。対象は少なくとも standalone CREATE（`create.py` の direct owner invocation）と `add_phases`（`roadmap.py` の invocation key）。postcheckが存在確認しか行わず内容一致を見ていない点も併せて扱う。rollbackでは直さない。
- Cross-project impact: あり（resumeを伴う全operation）
- Backfill likely: 可能性あり（既存pending recordの扱い）
- Human confirmation likely: yes（共通resume contractに触れる）
- Self-hosting prerequisite: no
- Evidence class: code inspection, smoke test
- Resolution: callerが内容を決める4つのowner（direct standalone CREATE / Roadmap作成 / Phase追加 / 既存未開始WorkのRelated maintenance）が、最初のID予約より前に、決定内容の正規identityを自分のinvocationへ `request` として記録するようにした。`MutationController.begin` がそれをdurableに書いてからID予約が始まるため、中断したoperationがどのrequestに束縛されているかは最初のdurable recordの時点で確定する。共有helperは `mutation.py` の `same_request`（canonical JSON比較。Pythonが `True` と `1` を同一視する一方recordは区別するため、objectではなくencodingで比較する）、`pending_for_slot`（invocationのうち「どれを対象にしているか」を言うslot部分だけで未完了recordを探す。内容が違うrequestでも、本来resumeしていたはずのrecordを必ず見つけて拒否できるようにするため）、`require_same_request`（legacy record / 複数件 / 内容不一致のいずれも `reconcile_required`）。identityはdigestではなく内容そのもので、人がrecordを読んで何を決めていたか分かる。正規化はrenderingに合わせた: section本文になる値はstrip（`store.render_body` がstripする）、nameはverbatim（renderingがそのまま書く）、Roadmapの任意sectionは有無と内容を別に記録（renderingがtruthinessで含める）、`derivation_detail` は無しと空を区別。宣言順はdisplay番号とID予約の対応を決めるため保持し、並べ替えは別requestとする。検査位置は、mutationを開くより前・記録済みeffectをreplayするより前（`roadmap._open` は戻る前にapplyする）で、Related maintenanceではno-op fast pathより前（D1の適用済みedgeの部分集合をD2が要求すると、D1をpendingのまま「やることなし」と返してしまうため）。実測（baseline `5e6580b`）: standalone CREATEは同じname / keyで別の成立状態を渡すとsuccessを返し正本はD1のまま、roadmap-createはD1の本文 + D2のPhaseという混ざったRoadmapをsuccessで返し、add_phasesはD1の成立状態を保持したままsuccess、Related maintenanceは明示keyで別のaddを渡すとD1のedgeを保持したままsuccess。いずれも修正後は `reconcile_required` となり、正本もpending recordも変わらない。同一requestの再実行は、intentだけ / 記録済み未適用 / 適用済みのどの中断点でも同じmutation・同じreserved IDで前進し、重複entity・重複relation・取り残しreservationはない。`_related_request_key` はrequest文字列を区切り文字で連結しescapeしないため別内容が同じ文字列になり得ることを実測で確認し（`(must_read, "a: | +obey:b")` と `(must_read,"a") + (obey,"b")` が同一label）、identityはlabelではなく構造で持つようにした。requestが自分自身の中に持つ重複（同一edge・同一removal id）は `_resolve_related_changes` がもともと1件として扱うので、identityを作るより前・request解決より前に畳み、畳んだ位置からrelation IDを予約する（`related:add:<index>` はrequest内の位置がkeyのため、畳まないと `(E, E, F)` と `(E, F)` でFのreservationが取り残される）。畳むとdefault labelが変わるので、畳む前のlabelの下も探す（そうしないと、以前の実装が `(E, E)` で作ったlegacy pending recordを唯一見つけられるはずの再実行がno-opを返して取り残す）。`future_plan_change` はidentityへ含めない（held Roadmapを通すかどうかだけを決めeffectに到達しないため、含めるとactive Roadmapで同一内容の再実行を拒否してしまう）。代わりにrequest一致検査の後・mutationを開くより前に検査し、継続できないrecordの存在をlifecycle factで隠さず、拒否された再実行が既存のpending mutationをabandonしないようにした。requestを記録していない旧実装のpending recordはどの中断位置でも自動resumeせず `reconcile_required` とし、record・effects・reserved IDs・statusをそのまま保持する（rollback・abandon・削除・差し替え・新規mutationのいずれも行わない）。これにより、従来default keyで自動resumeできていたRelated maintenanceのpending recordは、以後は人の照合対象になる。変更しなかったowner: `start`（決定はattemptごとで、attemptごとに新しいstageを開く。2回目のattemptが別の結果を決めて同じmutationを完了できることを確認）、`start-plan-exclude` / `phase-plan-exclude` / `work-plan-exclude`（eventのpayloadは対象entityに対する `plan_excluded` で固定。replan effectを記録する `apply_replan` はterminal eventをapplyした後にしか走らないため、replan effectが記録されている＝eventが適用済み＝次の呼び出しはunstarted prechekで止まる。唯一通る窓〈eventが記録済み・未適用〉でも、正本には再実行自身の内容だけが残り最初のrequestの内容は残らないことを3 operation × 2窓で実測）、`roadmap-achievement`（eventは `roadmap_achieved` 固定、`detail` は返すだけ）、Roadmap / Phase lifecycle（event種別固定）、`project-start`（内容不一致はpostcheckが拒否）、`bootstrap-backfill`（payloadは定数）、`push-destination-pin`（決定内容は既にinvocationにある）。どのidentityもそのownerのinvocation内のoperation-local keyなので、`INTENT_VERSION` とgeneric record schemaは変更していない（共通contractへ昇格させていないため人間判断を要する一般規則の変更も発生しなかった）。BL-023がPhase entryにoperation-localで入れた比較器 `_same_design` は、同じ内容の共有 `same_request` へ置き換えた（Phase entryの挙動は変えない）。Phase relationのendpointをspec keyで書くかそのkeyが予約されたPhase IDで書くかは別requestとして扱う: identityは最初のID予約より前にdurableでなければならず、その時点でPhase IDは存在しないため一方を他方へ解決できない（理由は `_phase_relation_records` のdocstringへ記録）。`skills/create` と `skills/roadmap` へ反映済み。

### BL-025 Lifecycle mutation cannot resume after its own event is applied

- ID: BL-025
- Title: Lifecycle mutation cannot resume after its own event is applied
- Status: RESOLVED
- Kind: implementation, recovery
- Problem: Roadmap / Phaseのlifecycle operation（Roadmap / Phaseのhold / resume / cancel、Roadmap achievementの記録）は、mutationをopen / resumeするより前に、前提条件（holdなら対象がactive、resumeならheld 等）を現在stateで検査する。自分のpending mutationがlifecycle eventを適用した後、commit / push / 完了より前に中断すると、同じoperationの再実行はその自分のeventを含む現在stateで前提条件を検査して拒否され（例: `SpecViolation: Phase ... is held`）、resumeへ到達しない。実測（baseline `ad4cace`、remoteありProject、他operationを介在させない単独の再実行）: 7 operationすべてで、intent作成直後とevent記録済み・未適用の中断はresumeできたが、event適用後・commit記録後・commit後push前・push後complete前の4窓はすべて拒否され、pending mutationが残り、`validate_project` は問題を報告しなかった。commit後push前の窓ではremoteへ反映されないまま残る。
- Why it matters: commit / push / finalization前の中断から回復できず、write-ahead recovery contract（`rules/git`: 途中失敗は同じmutationをresumeし、commit / push失敗は同じpending mutationのGit段階からresumeする）を破る。残ったpending mutationはevent logをwrite scopeに持つため、Work STARTや他のlifecycle / plan exclusion operationも `reconcile_required` で止まり続ける（実測: push先に到達できずholdのpushが失敗した後、holdの再実行は拒否され、別PhaseのWork STARTも停止した）。
- Likely scope: lifecycle precheckとmutation resume判定の順序、自分のpending mutationが適用済みeffectを持つことの証明、別request / 複数pending / effect不一致の扱い、achievementのnot_ready判定との整合、BL-017の独立性判定・BL-019 / BL-023 / BL-024のresume contractとの整合
- Cross-project impact: あり（全Projectのlifecycle operationの回復可能性）
- Backfill likely: no
- Human confirmation likely: no（canonical contractの意味を変えず、実装をcontractへ合わせる場合）
- Self-hosting prerequisite: no
- Evidence class: code inspection
- Resolution: 原因は `roadmap._record_lifecycle` が前提条件（`precheck(view)`）を現在stateだけで判定し、拒否されると `_open`（`MutationController.open` によるresume判定）へ到達しなかったこと。自分のpending mutationがeventを適用した後の再実行では現在stateが自分のeventを含むため、前提条件が自分自身を拒否した。前提条件はまず従来どおり現在stateで判定し、拒否された時だけ、このoperation自身の未完了mutationが適用済みだと証明できるlifecycle event 1件を除いたstateで判定し直すようにした（`_decide_lifecycle` / `_own_applied_event` / `_without_event`。achievement記録の事前検査も同じ判定を使う）。除外するのは次をすべて示せる場合だけ: 同じoperation・同じ対象の未完了mutation（`MutationController.open` がresumeするinvocationと完全一致、owner roadmap）がちょうど1件ある / その `event` stageが、このoperationが記録するevent（種別・対象）の `append_event` ちょうど1件である / Mutation Controllerのclassifyが `applied_matching`（event logが同じIDで記録どおりの内容を保持）である。示せなければ、次の2つの例外を除き判定も結果も従来と同じで、元の拒否をそのまま返す: eventを記録していない、記録したeventがevent logに無い、記録したeventがこのoperationのeventでない未完了mutationは自分のeventを証明せず、読めないrecovery recordも何も証明しない（従来どおりmutationを開く時に報告される）。例外として、前提条件が拒否された時に、同じoperation・対象の未完了mutationが複数ある場合と、event logが同じIDで別内容を保持する（`applied_mismatch`）場合は `reconcile_required` とし、record・event log・HEAD・remoteを変更しない。前提条件が通る経路は一切変えていない。resume後のeffectは従来どおりMutation Controllerがclassify / applyするため、再実行が行うのは残りのeffectだけである。構造検査（`_stop_on_structure`）は従来どおり現在stateで行う。achievement記録は、自分の未完了mutationが既に `roadmap_achieved` を適用している場合はnot_readyとして報告せず、同じmutationで最後まで進めるか `achievement_not_ready` でSTOPする（適用前は従来どおりnot_readyを返す）。実測（baseline `ad4cace`、remoteありProject、他operationを介在させない単独の再実行）: 7 operation（Phaseのhold / resume / cancel、Roadmapのhold / resume / cancel、achievement記録）すべてで、intent作成直後とevent記録済み・未適用の中断はresumeできたが、event適用後 / commit記録後・未適用 / commit後・push前 / push後・complete前の4窓はすべて `SpecViolation`（`Phase ... is held` / `is not held` / `already terminal` / `Roadmap ... is achieved` 等）で拒否され、pending mutationが残り、commit後・push前ではremoteも未反映のまま、`validate_project` は問題を報告しなかった。push先に到達できずholdのpushが失敗した場合も同じで、その間は別PhaseのWork STARTも `reconcile_required` で止まった。修正後は同じ7 operation × 7窓を再計測し、完了後以外の6窓すべてで同じmutationのまま完了し（event 1件・commit 1件・pending 0件・HEADとremoteが一致・`validate_project` clean）、完了後は従来どおり `SpecViolation` だった。regression test（`tests/test_lifecycle_resume.py`）はPhaseの3 operationを5窓、Roadmapの3 operationとachievementを3窓で固定し、各窓で再実行が実行したeffectが残りだけであること（commit後・push前は `git_push` のみ、push後は無し）、eventの重複・commitの重複・pushの重複・recordの増殖・reserved IDの追加が無いこと、最終lifecycle、HEADがそのoperationのcommitでremoteと一致すること、`list_pending == []`、`validate_project` clean、worktree clean（runtime領域を除く）を確認する。resumeしたrecordは従来どおりcompletedとして保持される（BL-019）。修正前のコードではこのtestの35件（subtestを含む）が失敗し、修正後は22 test / 38 subtestがすべてpassする。full suiteは567 passed / 2 skipped / 397 subtestsから589 passed / 2 skipped / 435 subtestsになり、増えたのはこのtestだけである。新しい不要なSTOPや、拒否されていた入力の受理を作らないこと: 再実行が出会い得るrecovery state 22通り（未完了mutation無し / 何も記録していないrecord / 未適用event / 自分の適用済みevent 3窓 / event logの `at` 不一致 / event logのtype不一致 / 別種eventを記録したrecord 3種 / 同じrecordの複製 2種 / 読めないrecovery record 3種 / 自分のevent後に別eventが加わった場合 / 同じPhaseの別operationのpending）で、baselineと修正後の結果（例外の型・code・ID正規化後のmessage、record・event log・HEAD・remoteの変化、pending数、lifecycle、別PhaseのWork STARTが止まるか）を比べ、異なったのは5通りだけだった: 自分の適用済みevent 3窓（resume完了）と、明示的に求められた2通り（event logが記録と異なる場合と同じrecordの複製がある場合に `SpecViolation` → `reconcile_required`）。それ以外の17通りはmessageまで同一だった（別種eventを記録したrecordは、baselineで受理されていた場合も含めて従来どおり。読めないrecovery recordも従来どおりの型とmessage）。同じ中断状態（phase-holdを7窓で中断）でbaselineと修正後に同じ要求列を実行したA/B比較では90行中83行が一致し、差分はすべて同じoperation・同じ対象の再実行（とそれが完了した後にだけ実行できる後続START）だった（event適用後 / commit後・push前 / push後・complete前は完了。commit記録後・未適用は、この比較では間に独立operationのcommitが入るため、下記(3)により `SpecViolation` から `reconcile_required` へ変わった）: 同じPhaseへのcancel / resume、別Phaseのhold、Roadmapのhold / cancel、別PhaseのWork START、standalone plan exclusionは全窓で従来どおり停止し（event logを共有するためBL-017の独立性判定どおり `reconcile_required`、未適用の窓で前提条件を満たさないresumeは `SpecViolation`）、record・event log・HEAD・remoteは変わらず、Related maintenance・Phase追加・standalone CREATE・Roadmap作成は全窓で従来どおり進む。BL-017で広いwrite scopeを記録した旧recordも同じようにresumeする。lifecycle recordは元々 `request` を持たず、決定内容（event種別と対象）はinvocation（operationと対象）で決まるため、BL-024のrequest記録は追加していない（invocation・stage名・event keyの形は初回実装 `2ba599f` から不変）。push destinationが承認済みの別destinationへ変わっていればresume時に `reconcile_required`、未承認のdestinationならmutationを開く前に `push_destination_mismatch` で停止し、どちらもどこへもpushしない。変更していないもの: Mutation Controller、`INTENT_VERSION`・record schema、write scope宣言（BL-017）、Project execution lock（BL-021）、record retention（BL-019）、Phase entryのresume（BL-023）、decided-content binding（BL-024）、startable候補の選択（BL-014）、plan exclusion、START、構造検査。schema・event・relation type・project.yamlは変更しておらず、backfillは不要。残る論点（今回は扱っておらず挙動も変えていない。別項目として扱う候補）: (1) plan exclusion（`phase-plan-exclude` / `work-plan-exclude` / `start-plan-exclude`）も、`plan_excluded` eventを適用した後に中断すると同じoperationの再実行がunstarted前提条件（Work plan exclusionでreplan未適用の場合は中間状態の構造検査）で拒否されresumeできないことを実測した。ただしreplanは現在stateから解決され（適用済みの削除relationは解決できない）、BL-024はこの経路のreplan内容を束縛しない根拠に「event適用後の再実行は前提条件で止まる」ことを挙げているため、resumeを通すにはreplan内容の束縛と記録済みstageの再構成を伴う別の設計が要る。(2) `cancel_phase` は、他のPhaseの `requires_completion` predecessorであるPhaseに対し、投影検査なしに `phase_cancelled` を適用・commit・pushしてからpostcheckの `dependency_unreplanned` で失敗し、pending mutationを残す（再実行は構造検査で停止する）。原因はterminal eventの適用前に構造を投影検査しないことで、前提条件の順序とは別である。(3) commitを記録した後・作る前に中断したmutationは、その間に独立なoperationがcommitするとHEADが記録時の `base_head` から進み、再実行でgit_commit effectが `applied_mismatch` に分類されて `reconcile_required` になる。lifecycle固有ではないMutation Controllerのcommit分類による既存の挙動で、baselineでもRelated maintenanceとdirect CREATEが同じ結果になることを実測した（間にcommitが無ければいずれもresumeする。lifecycle operationも修正後は同じ挙動）。(4) 同じinvocationのrecordが別種のevent（例: phase-holdのrecordが `phase_resumed`）を記録するよう書き換えられていると、前提条件が通る場合はbaselineと同じくそのeventを適用して完了する。Workline自身はこの形のrecordを書かないため今回は変えていない。`skills/roadmap` へ反映済み。

### BL-026 Phase cancel publishes an unreplanned structure before its postcheck

- ID: BL-026
- Title: Phase cancel publishes an unreplanned structure before its postcheck
- Status: RESOLVED
- Kind: implementation, safety, recovery
- Problem: Phaseのcancel（`cancel_phase`）は、そのPhaseが他のPhaseの `requires_completion` predecessorであっても、cancel後の構造を検査せずに `phase_cancelled` を記録・適用し、commit・pushしてから、postcheckの構造検査で `dependency_unreplanned` を検出して失敗する。実測（baseline `8af8da3`、remoteありProject、Phase A --requires_completion--> Phase B、両方unstarted）: `cancel_phase(A)` の実行順は、構造precheck（問題なし）→ mutation開始 → `event` stage記録 → `append_event` 適用 → `finalize` stage記録 → `git_commit` 適用（HEADが `chore(workline): phase_cancelled <A>` へ進む）→ `git_push` 適用（remoteのmainも同じcommitになり、remoteのevent logに `phase_cancelled` が載る）→ postcheck（`dependency_unreplanned`）→ `ValidationError`（code `structure_invalid`、`postcheck: dependency_unreplanned: <relation>: requires_completion predecessor <A> is cancelled`）。append_event / git_commit / git_pushが適用済みのpending mutationが残り、`validate_project` は `dependency_unreplanned` を報告し、Bはunsatisfied dependency（A cancelled）のままstartable Phaseは0件になった。同じcancelの再実行も別Phaseのholdも構造precheckの `structure_invalid` で停止した。`return_to` 先のPhaseをcancelした場合も同じ順序で `return_to_unreplanned` になる。BL-025の調査中に見つかった（BL-025 Resolutionの残る論点(2)）。
- Why it matters: 失敗したoperationがremoteへ不正なcanonical stateを公開し、pending mutationも残す。以後そのProjectの全operationが構造precheckで停止し、rollbackもrepairも禁止されているため正式経路では回復できない。`rules/git` の順序（effects → validation → commit → push）と、cancel / plan exclusionを決めたowner自身がreplanして構造validationを通すという `rules/ai-decision` の責任にも反する。
- Likely scope: Phase cancelの構造検査の位置（commit / pushより前、できればmutationを開くより前）、plan exclusion / Work cancelが既に持つ投影検査の再利用、BL-025のlifecycle resume（自分の適用済みeventを除いた前提条件判定）との整合、BL-017の読取依存の記述（先行Phaseのcancelはcancel自身が止める）との整合
- Cross-project impact: あり（全ProjectのPhase cancel）
- Backfill likely: no（既に公開された不正なcancelは自動repairしない）
- Human confirmation likely: no（canonical contractの順序へ実装を合わせる）
- Self-hosting prerequisite: no
- Evidence class: code inspection
- Resolution: 原因は、lifecycle operationの共通経路 `roadmap._record_lifecycle` がeventを記録・適用して `_finalize`（commit → push → 構造postcheck）へ進むだけで、terminal eventであるPhase cancelについて、eventを加えた後の構造を事前に検査していなかったこと。plan exclusion（`_plan_exclude_locked`）とSTARTのWork cancel（`_cancel`）はeventを記録する前に `projected_view` / `validate_projection` で投影検査しており、Phase cancelだけがその検査を持たなかった。`cancel_phase` の前提条件に、現在stateへ `phase_cancelled` を1件加えた投影を同じ `validate_projection`（中身はcanonicalな `validate_structure`）で検査する処理を加えた。独自の依存判定・問題codeの選別は加えていない。検査はPhaseの解決とalready terminalの判定の後、mutationを開く前に行うので、拒否はeventの記録・適用、recovery record（abandonedを含む）の作成、commit、pushのいずれよりも前になる。拒否は既存の投影検査と同じ `SpecViolation`（code `spec_violation`、`phase cancel: structure would be invalid: <問題のmessage>`）で、待っているrelationと理由をすべて列挙する。正常なcancelを止めない根拠: 投影するのは構造precheckを通過した現在stateにこの1 eventを加えた状態であり、cancelが最後まで進んだ場合に `_finalize` のpostcheckが検査する状態と一致する（Project execution lockにより間に他の書込みは入らない）。したがって投影が拒否するのは、旧実装であればcommit・pushの後にpostcheckが失敗していた（またはそれより前にmutationを開く段階で止まっていた）cancelであり、最後まで成功していたcancelは拒否しない。唯一の例外は、Workline自身が書かない形に手で書き換えられたrecordが要求とは別のeventを適用して成功を返していた場合で、下記(4)のとおり。Phase cancelはchild Workをcancelせず、already terminalのPhaseは前提条件が先に拒否するので、この1 eventが作り得る構造問題は、そのPhaseを `requires_completion` predecessorとする、またはそのPhaseを `return_to` 先とする、cancelled / plan_excludedでないPhaseがある場合の `dependency_unreplanned` / `return_to_unreplanned` だけである。`return_to` の場合も同じ欠陥（push後にpostcheckで失敗）だったため、同じ検査で止まる。同じProjectで「修正後のcancel（拒否）」と「投影を無効にした旧経路のcancel（push後にpostcheckで失敗）」を続けて実行し、拒否messageの問題列が公開後の `validate_structure` の問題列と完全に一致することをtestで固定した。BL-025との両立: 投影は前提条件の一部なので、自分の未完了mutationが既に `phase_cancelled` を適用した後の再実行では、`_decide_lifecycle` が現在state（already terminalで拒否）ではなくそのeventを除いたstateで判定し直し、そこへeventを1回だけ加えて投影する。自分のeventを二重に数えて自分のresumeを拒否することはない。記録済み・未適用のeventをresumeする場合も、記録済みeffectを適用する `_open` より前に検査する。別operation・別対象のrequestが未完了mutationを継続しないこと、write scope（BL-017）、recovery record（BL-019）、execution lock（BL-021）、Phase entryのresume（BL-023）、決定内容の束縛（BL-024）は変更していない。実測（A/B、baseline `8af8da3` と修正後で同じ61 scenario・235 stepを実行し、例外の型・code・ID正規化後のmessage、実行したeffect、HEAD / remoteの移動、event、pending、record status、`validate_project`、Phase / Roadmapのstateを比較）: 差分は28 stepで、すべて投影が不正になるPhase cancelとその帰結だった。(1) 不正になるcancelそのもの12 step（両方unstarted / successor held / predecessor held / predecessor complete + successor in_progress / 両方complete / `return_to` 先 / successor 2件 / 別Roadmapのsuccessor / successorの一部だけcancel済み / 中断の間にPhase追加でrelationが増えた再実行2窓 / 後続のcancel）: baselineはappend_event / git_commit / git_pushを適用してremoteへ公開した後に `structure_invalid` で失敗しpendingを残したが、修正後は実行effect 0件・HEADとremote不変・pending増加なし（再実行の場合は既存recordを1 byteも変えない）で `spec_violation`。(2) その後の操作10 step: うち9 stepは、baselineでは公開された不正構造のためhold / resume / cancel / STARTが `structure_invalid` で止まっていたが、修正後は通常どおり進む。残る1 step（中断の間にrelationが増えた窓でのhold）は、baselineの `structure_invalid` から、適用されずに残ったpending cancelとの重なりによる `reconcile_required` になり、どちらも停止する。(3) 拒否理由だけが変わった5 step: 不正になるcancelを、event logを共有する別の未完了mutation（同じPhaseのhold、別Phaseのcancel、successorの記録済み・未適用のcancel）がある間に要求した3 stepでは、baselineの `reconcile_required`（mutationを開く時の重なり判定）が修正後は `spec_violation`（前提条件）になる。未完了mutationはその後の同じ再実行で完了する。mutationを開く段階で止まる状態と重なった2 stepでは、承認されていないpush destinationの `push_destination_mismatch` と、detached HEADの `detached_head` が `spec_violation` になる（detached HEADのbaselineはmutationを開いた後に停止するためintentだけのpending recordを残したが、修正後はrecordを作らない。投影が不正にならないcancelは両者とも従来どおり `detached_head` でそのrecordを残す）。どれも他には何も書かず、lifecycle operationでは前提条件がmutationを開く段階の判定より先に行われるという既存の順序（例: held Phaseへのholdは重なりがあっても `SpecViolation`）に従った結果である。(4) 手で書き換えたrecovery recordの1 step: phase-cancelのpending recordが記録したeventを `phase_held` へ書き換え（Workline自身はこの形のrecordを書かない）、その後そのPhaseを前提とするPhaseが追加された状態で再実行すると、baselineは記録どおりholdを適用してcommit・pushし `phase_cancelled` の成功を返したが（BL-025の残る論点(4)）、修正後は要求されたcancelの投影が不正なので適用より前に拒否し、recordを変更しない。投影が不正にならない同じ書き換えは両者とも従来どおりholdを適用する。書き換えられたrecordの扱い自体は変更していない。差分の無かった207 stepには、誰にも待たれていないPhase・successor側のPhase・`planned_next` だけ・`return_to` の戻る側・successorがcancelled / plan_excluded済み・plan exclusionのreplanで先にdependencyを外した場合・held / completeのPhase・別Roadmapの無関係なdependency・展開済みでin_progressのPhase・Phaseを跨ぐWork間dependency・already terminal / plan_excluded / 解決不能のPhaseのcancel、Phase / Roadmapのhold / resume / cancel、achievement（not_readyを含む）、Phase entry（依存未充足の `phase_blocked` を含む）、Related maintenance、START、startable Phase選択、Phase / Workのplan exclusion（replanの有無）、STARTのWork cancel、cancel済みPhaseを前提とするPhase追加、standalone CREATE、正常なcancelを6窓（intent作成後 / event記録後 / event適用後 / commit記録後 / commit後push前 / push後complete前）で中断してからの再実行（relation無しと、cancel済み側を含むrelation有りの2通り、いずれも同じmutationで完了）、旧実装が公開した不正なcancelの再実行（従来どおり `structure_invalid`、自動repairなし）、BL-017の読取guard（未適用のcancel決定がそのPhaseのentryを止める）、承認されていないpush destinationやdetached HEADでの投影が不正にならないcancelが含まれる。regression test（`tests/test_phase_cancel_projection.py`）は17 test / 26 subtestで、不正になるcancelが何も開かず何も書かないこと（mutationの開始0件・実行effect 0件・event log / recovery record / HEAD / remoteのbyte一致・pending 0・`validate_project` clean）、拒否後に他のoperationが進むこと、正常なcancelがevent 1件・commit 1件・push済み・record cleanupで完了すること、replan後にcancelできること、中断した正常なcancelが6窓すべてで残りのeffectだけを実行して同じmutationで完了すること、中断の間に不正になった再実行が記録済みeventを適用しないこと、別の未完了mutationが拒否の後も完了できることを固定する。修正前のコードではこのうち16件（subtestを含む）が失敗する。規則の取り違えも固定されることを4つのmutantで確認した: successorのstateを見ずに依存があれば拒否する規則（17件失敗）、BL-025の判定し直しを経ずに現在stateへ投影する規則（適用済み4窓などで5件失敗）、already terminalより先に投影する規則（2件失敗）、mutationを開いてから投影してabandonする規則（16件失敗）。full suiteは628 passed / 2 skipped / 464 subtests（baseline `8af8da3`）から645 passed / 2 skipped / 490 subtestsになり、増えたのはこのtestだけである。schema・event・relation type・project.yaml・Mutation Controller・`INTENT_VERSION`・validatorは変更しておらず、backfillは不要。`skills/roadmap` のLifecycle（Phase cancel）へ規則を追記し、BL-017由来の「先行Phaseのcancelはcancel自身のpostcheckが停止させる」という記述を、eventを適用する前の構造投影で停止する、へ改めた。残る論点（今回は扱っておらず挙動も変えていない）: (1) cancelがintentだけ、またはeventを記録して未適用のまま中断している間に、event logを書かない独立なoperation（Phase追加）がそのPhaseを前提とするrelationを加えると、再実行は適用より前に拒否されるがpending recordは残り、event logを共有するreplan経路もそのrecordと重なるため、人による照合が要る（baselineではこの再実行が不正な構造を公開して失敗していた）。(2) Phase追加は、cancelled / plan_excludedのPhaseを前提とするrelationを、Phase fileとrelationを適用した後のregistration postcheck（`postcheck_failed`）で拒否するため、commit / pushより前ではあるが、適用済みのcanonical file変更とpending mutationを残す（baselineと修正後で同一の挙動であることを確認）。

### BL-027 Registration applies its effects before refusing them

- ID: BL-027
- Title: Registration applies its effects before refusing them
- Status: RESOLVED
- Kind: implementation, recovery
- Problem: Phase CREATE（`phase_create.register_phases`）とCREATEのregistration core（`create.register_works`）は、登録するentity file・relationをmutationへ記録して適用した後に、登録postcheckとして構造validation（`validate_structure`）を行う。適用後の構造が不正になるrequestは、commit / pushより前ではあるが、適用済みのcanonical fileとpending mutationを残して `postcheck_failed` で失敗する。実測（baseline `da97915`、remoteありProject、Phase a / b）: `cancel_phase(a)` の後の `add_phases(<Roadmap>, {c}, requires_completion a → c)` は、`phases` stageを記録して `write_file` / `add_relation` を適用した後に `ValidationError`（code `postcheck_failed`、`postcheck: <relation>: requires_completion predecessor <a> is cancelled`）で失敗した。HEADとremoteは不変だが、2 effectとも `applied_matching` のpending mutationが残り、`.workline/phases/<c>.md`（untracked）と `.workline/relations/roadmap.yaml`（modified）がdirtyになり、`validate_project` は `dependency_unreplanned` を報告した。以後、同じrequestの再実行・relationを外したrequest・Phaseのhold / resume・Phase entryは構造precheckの `structure_invalid` で停止し、構造precheckを持たないdirect standalone CREATEは自分のWork fileを適用してから同じ理由で失敗して2件目のpendingを残した。同じ順序で同じ帰結になる経路を実測した: Phase追加（plan_excludedのpredecessor、cancelled Phaseを `return_to` 先にする、既存relationとの `requires_completion` cycle、新Phaseを通るcycle、既存edgeの重複、request内の重複edge）、Roadmap作成（別Roadmapのcancelled / plan_excluded Phaseを前提とする、cancelled Phaseへの `return_to`、新Phase同士のcycle（最初のRoadmapでも同じ）、request内の重複edge）、Phase entry（plan_excluded / cancelled Workを `requires_completion` predecessorにするdesign）、STARTのDerive（executorが決めたrelationがplan_excluded / cancelled Workを前提にする。STARTのlifecycle event 2件に加えてderived Workとrelationが適用済みで残る）、構造が既に不正なProjectでのdirect standalone CREATE（Work fileを適用済みのpendingを残し、別のdirect CREATEはそのpendingとのscopeの重なりで `reconcile_required` になる）。callerが複数のstageで登録する経路では、構造が不正にならない入力不備でも先行stageが適用済みで残った: Roadmap作成はRoadmap fileを `roadmap` stageとして適用してからPhase CREATEを呼ぶため、Phase CREATEの入力検査（空のPhase名・成立状態、未知のrelation type、self relation、解決できない / mixed endpoint）で拒否されるrequestがRoadmap fileを適用済みのpendingを残し、同じ名前で修正したrequestはBL-024の束縛で、Phase追加・Phase entryはwrite scopeの重なりで `reconcile_required` になった。Phase entryは通常Workのstage（とintegrationのstage）を適用してから後続stageのpayloadを検査するため、integrationの名前が空、human_confirmationのRelated typeが未知等のdesignも先行stageを適用済みのpendingを残し、修正したdesignは `reconcile_required` になった。resumeでは、`roadmap._open` が記録済みeffectをreplayしてから検査するため、Phase追加・Roadmap作成が中断している間にwrite scopeの重ならない `cancel_phase` がrelationの前提を変えると、同じrequestの再実行は記録済み（または未記録）のeffectを適用してから同じ `postcheck_failed` で失敗した。BL-026の調査中に見つかった（BL-026 Resolutionの残る論点(2)）。
- Why it matters: 拒否されるべきrequestが、拒否される前にcanonical fileを変更しpending mutationを残す。rollback・自動repairを行わない規則のため、Workline経由で回復する経路がない。構造が不正になる場合はreplanの正式経路自体が構造precheckで止まり、Project全体が進行不能になる。構造が不正にならない入力不備でも、残ったpending recordがwrite scopeの重なるoperationと、同じ名前で修正したrequestを止め続ける。拒否の理由（cancelled / plan_excludedの前提は `requires_completion` を満たさない、cycle禁止等）は正しく、欠陥は拒否の時点にある。
- Likely scope: 登録するeffectを記録する前の構造投影検査（Phase CREATE / CREATEのregistration core）、Roadmap作成の `roadmap` stageより前のPhase検査、Phase entryの後続stageのpayload検査、resume時に記録済み・未適用のeffectをreplayする前の検査、replan経由の登録との切り分け、拒否の例外の型・code、BL-023 / BL-024 / BL-025 / BL-026との整合
- Cross-project impact: あり（全ProjectのRoadmap作成・Phase追加・Phase entry・START derive・direct CREATE）
- Backfill likely: no（既に残ったpending recordや不正な構造は自動repairしない）
- Human confirmation likely: no（拒否の理由を変えず、拒否の時点を最初のeffectより前へ移す）
- Self-hosting prerequisite: no
- Evidence class: code inspection
- Resolution: 原因は、登録coreが「effect記録 → 適用 → postcheck（`validate_structure`）」の順で、記録前の検査が入力payload・endpointの解決・`_check_projection`（integration invariant、human_confirmationの対象と再帰、CREATEの `requires_completion` cycle）だけだったこと。`dependency_unreplanned` / `return_to_unreplanned` / 重複edge / Phase CREATEのcycleは適用後にしか検出されなかった。callerにも同じ順序があった: Roadmap作成はRoadmap fileを適用してからPhase CREATEを呼び、Phase entryは先行stageを適用してから後続stageのpayloadを検査し、`roadmap._open` は記録済みeffectをreplayしてから検査していた。修正: (1) `ProjectView.with_effects` を加え、決定済みのeffect（`write_file` / `add_relation` / `remove_relation` / `append_event`）を、storeが適用後に読み戻すのと同じ規則で現在のviewへ重ねる。entity fileは `ProjectStore._parse_entity` の検査を `store.entity_fields` へ切り出して共有し（disk読取の挙動とmessageは不変）、改行は `read_text` と同じく正規化し、entityはstoreと同じくID順に並べる。relationは適用と同じくfile末尾への追加とIDによる削除として重ねる。(2) `mutation.unapplied_effects` は、`Mutation.apply()` がまだ書くfile effectを同じclassifyで記録順に返す（mismatchで止まり、git effectは分類しないのでnetworkに触れない。BL-022が固定する `Mutation` / `MutationController` のpublic methodは増やしていない）。(3) `create.refuse_invalid_work_writes` / `phase_create.refuse_invalid_phase_writes` は、未適用の記録済みeffectと今回のstageを投影し、postcheckと同じ `validate_structure` を、postcheckと同じ報告（`ValidationError`、code `postcheck_failed`、同じmessage形式）で適用する。registration coreはstageを記録する前（resumeで記録済みなら適用する前）にこれを呼ぶので、拒否されたrequestはeffectを記録・適用せず、今回開いたmutationは既存の `abandon_on_stop` でabandonされる。書くものが残っていなければ何もせずpostcheckに任せ、postcheck自体は変更していない。(4) Phase CREATEの決定（precheck・ID予約・relation payload検査・effect構築）を `decide_phases` へ分離し、Roadmap作成は `roadmap` stageを記録する前に、Roadmap fileを加えた投影に対してPhasesを決定して同じ検査を行う。Phase / relation IDはこの時点で予約する。(5) Phase entryは最初のstageを記録する前に、未記録の全stageのWork payload（name・成立状態・Related）をCREATEと同じ規則（`validate_work_specs`）で宣言順に検査する。後続stageのrelationとconfirmation対象はPhase entryが登録済みWorkから組み立て、未開始Workの追加はその規則が読むPhase stateを変えないので、payloadを通った後続stageが先行stageの適用後に拒否されることはない。(6) `roadmap._open` は、Roadmap作成・Phase追加ではPhase CREATEの、Phase entryではCREATEの検査を、resumeしたmutationの記録済みeffectをreplayする前に行い、拒否時はrecordを変更しない。(7) replan（`ops.apply_replan`）経由の登録は `register_works(..., refuse_before_apply=False)` として従来の順序（適用 → postcheck）のまま残した。所有者がreplan全体を記録前に投影検査しており、その適用途中の状態を登録coreの投影で判定しないため。例外は `spec_violation`（`validate_projection` 形式）へ揃えず、baselineが適用後に返していたのと同じ型・code・messageのまま前倒しした。投影検査はpostcheckの規則そのものであり、名前を揃えるためだけに全経路の拒否を変えないため。正常なrequestを止めない根拠: 投影はProject execution lockの下で、storeと同じ読取規則・適用と同じ順序でpostcheckが見るstateを作るので、投影が拒否するのは旧実装でも適用後にpostcheckが拒否していたrequestだけである。Roadmap作成の前倒しは旧実装でRoadmap file適用後にPhase CREATEが拒否していたrequestだけを、Phase entryの前倒しは旧実装で後続stageが拒否していたrequestだけを、resume時の検査はreplayすれば同じpostcheckが拒否する記録済みeffectだけを拒否する。実測（A/B、baseline `da97915` と修正後で同じ177 scenario・836 stepを実行し、例外の型・code・ID正規化後のmessage、開始したmutation、記録したstage、実行したeffect、HEAD / remoteの移動、新しいcommit、pending recordのeffect classify・予約key・scope、record status、worktreeのdirty、`validate_project`、entity stateとrelationを比較）: 133 scenarioは完全に一致し、差分の168 step（44 scenario）はすべて次のいずれかだった。(a) 対象requestそのもの26 step（Phase追加7、Roadmap作成12、Phase entry 4、START Derive 2、既に不正なProjectのdirect CREATE 1）: 型・code・messageは同一のまま、登録のeffectの記録・適用が0件になり、pending・dirty・新たな `validate_project` のproblemが残らず、abandoned recordだけになった。START Deriveは拒否までにSTARTが記録したlifecycle event 2件がSTART mutationに残り、そのWorkのSTARTの再実行で完了する（executor実行中の他のSTOPと同じ）。既に不正なProjectでは既存のproblemだけが残る。(b) 2つの問題を同時に持つPhase entry designの2 step: 修正後は後続stageのpayloadの問題を先に報告する（baselineは通常Workの `dependency_unreplanned` を適用後に、または解決できないendpointを適用前に報告）。修正後はどちらも何も書かない。(c) 拒否されたrequestの再実行23 step: baselineは自分の残した不正構造で止まるか、残したpendingをresumeして同じ拒否を繰り返していたが、修正後は何も書かずに元の理由で拒否される（1 stepは(b)と同じ報告順の違い）。(d) その後のoperation 86 step: baselineで残った不正構造・pending recordにより `structure_invalid` / `reconcile_required` で止まっていた修正request・Phase追加・Roadmap作成・hold / resume・Phase entry・START・direct CREATEが進む（うち2 stepはbaselineでも成功しており、残ったpendingとdirtyの有無だけが違う）。(e) 中断の間に `cancel_phase` がrelationの前提を変えたresume 12 step（Phase追加5窓・Roadmap作成7窓）: baselineは記録済み / 未記録のeffectを適用してから `postcheck_failed` になったが、修正後は同じmessageで、未記録の窓はstageの記録前に（effectの無いmutationはabandonされ、Roadmap fileを適用済みのmutationはそのまま残る）、記録済みの窓はreplay前に（recordを変更せずに）止まる。Roadmap fileを記録した後・適用前の窓だけは、Phaseがまだ決定されていないため、Roadmap fileをreplayしてからPhaseを記録する前に止まる。(f) その後のPhase追加6 step: baselineの `structure_invalid` が、適用されずに残ったpending recordとのscopeの重なりによる `reconcile_required` になる（どちらも停止し、holdは進む）。(g) 中断recordの内容10 step: `roadmap` stageの記録前後で中断したRoadmap作成のrecordが、Phase / relationの予約keyとPhase IDのscope entityを先に持つ（その後のresume・別requestの拒否・独立なoperationの結果は同一）。(h) 既に不正なProjectでの後続3 step: どちらも停止し、baselineの残したpending・dirtyの有無と、そのpendingとの重なりによる `reconcile_required` が修正後は元の `postcheck_failed` になる違いだけ。差分の無かった668 stepには、正常なRoadmap作成（最初のRoadmap、別Roadmapのactive Phaseへの依存、対象範囲 / 対象外付き）、正常なPhase追加（active / held / completeのPhaseへの依存、cancelled Phaseからの `planned_next` / `return_to`、held Roadmapへの `future_plan_change`、既存Phase同士のrelation、既存のpayload拒否）、正常なPhase entry（human_confirmation、Related、条件付きRelated、completed Workへの依存）とその後のSTART、STARTのDerive / HumanNG / `reintegration_required`、direct CREATE（Related・derivation detail）とSTART・standalone plan exclusion、Related maintenance、Phase cancel（BL-026の拒否を含む）、Phase / Roadmapのhold / resume / cancel、plan exclusion、新Workを登録するreplan（relation削除が妥当性の条件になる妥当なreplanが途中の状態で `postcheck_failed` になる既存の挙動を含めて同一）、achievementまでの全体flow、Phase追加（9窓）・Roadmap作成（11窓）・Phase entry（12窓、integration / confirmationの各stageを含む）・direct CREATE（9窓）をintent作成前 / intent作成後 / ID予約後 / stage記録後 / 部分適用後 / 適用後 / commit記録後 / commit後push前 / push後complete前で中断してからの同じrequest（同じmutationで完了）と別request（`reconcile_required`、recordは不変）の再実行、中断中のPhase entryとdirect CREATEに対する独立・非独立なoperationが含まれる。regression test（`tests/test_registration_projection.py`）は23 test / 36 subtestで、Phase追加・Roadmap作成・Phase entry・START Derive・既に不正なProjectのdirect CREATEの拒否が何も記録・適用しないこと（記録stage 0件・実行effect 0件、canonical file / HEAD / remote / worktreeの一致、pending 0、`validate_project` clean、abandoned recordだけ）、拒否のmessageが検査を外した旧順序の実行が適用後に返すものと予約IDの対応で一致すること、正常な登録が従来どおり記録・適用されること（除外されたPhaseが待たれない関係の境界を含む）、後続stageのpayloadの拒否が先行stageを書かず修正したdesignが展開・実行できること、payloadの問題が宣言順に報告されること、中断の間にstateが変わったPhase追加（記録済み・部分適用）・Roadmap作成（Roadmap適用済み・Phase記録済み）・Phase entry・direct CREATEの再実行が適用前に止まりrecordを変えないこと、変わっていなければ同じmutationで完了すること、Roadmap作成の中断recordがPhaseを決めたIDでresumeすること、別requestがrecordを乗っ取らないこと、replanの登録にこの検査を掛けないことを固定する。修正前のコードではこのうち37件（subtestを含む）が失敗する。規則の取り違えも固定されることを6つのmutantで確認した: 除外されたentityから出るrelationを種類とsuccessorに関係なく拒否する規則（16件失敗）、replay前の検査を外す（4件）、Roadmap作成の前倒しを外す（12件）、Phase entryのpayloadの前倒しを外す（5件）、replanの登録にも投影検査を掛ける（1件）、記録済み・未適用のeffectを投影しない（5件）。full suiteは645 passed / 2 skipped / 490 subtests（baseline `da97915`）から668 passed / 2 skipped / 526 subtestsになり、増えたのはこのtestだけである。`skills/phase-create` / `skills/create` / `skills/roadmap` / `skills/start` へ規則を追記した。schema・event・relation type・project.yaml・`INTENT_VERSION`・validatorの規則は変更しておらず、backfillは不要。残る論点（今回は扱っておらず挙動も変えていない）: (1) replan経由の登録は対象外で、relation削除が妥当性の条件になるreplan（未開始Workの置き換え、integrationのcancelと再統合）が、新Workの登録後・relation削除前の状態で `postcheck_failed` になりpendingを残す既存の欠陥はそのまま残る（別の根本原因）。replanの新Workのpayload不正（空の名前等）も、terminal eventを適用した後に登録coreが拒否する（A/Bで同一）。(2) 中断の間にstateが変わって記録済みeffectを適用できなくなったresumeは、適用前に止まるがpending recordは残り、write scopeが重なるoperation（Roadmap relationを書くoperation）を止め続けるので、人による照合が要る（自動abandon・rollbackはしない）。(3) 既に不正な構造やpending recordを残したProjectは自動repairしない。

### BL-028 Replan registers new Works before its removals and fails on the intermediate structure

- ID: BL-028
- Title: Replan registers new Works before its removals and fails on the intermediate structure
- Status: RESOLVED
- Kind: implementation, ordering, recovery
- Problem: plan exclusion（`roadmap._plan_exclude_locked` のPhase / Work、`start._plan_exclude_standalone_locked` のstandalone）とSTARTのWork cancel（`start._Session._cancel`）のreplanは、所有者がterminal event・relation削除・relation追加・新Workをすべて加えた最終状態を `projected_view` / `validate_projection` で検査してからeventを記録・適用し、`ops.apply_replan` で (1) 新Workの登録（`register_works`: Work fileと追加relationを記録・適用）→ (2) 登録postcheck（Project全体の `validate_structure`）→ (3) relation削除の記録・適用 → commit / push、の順に進む。relation削除が最終状態を妥当にするreplanでは、event適用後・削除適用前の中間状態に、除外したWork / Phaseを前提とする削除予定のrelationが残る。(2) はその中間状態の `dependency_unreplanned` / `return_to_unreplanned` / 重複edgeを検出して `postcheck_failed` で失敗し、登録前の `_check_projection` のcycle検査も削除予定のrelationを含めて判定するため、削除前にだけcycleに見える並べ替えを `validation_failed`（`requires_completion would form a cycle`）で拒否する。所有者が妥当と判定したreplanが途中で失敗し、eventと（(2)の場合は新Work・追加relationも）適用済みのpending mutationとdirty fileが残り、`validate_project` はproblemを報告し、同じrequestの再実行・START・Phase holdは構造precheckの `structure_invalid` で止まる。BL-027は登録前の構造検査をreplan経由の登録に掛けず（`refuse_before_apply=False`）、この欠陥を残る論点(1)として残していた。`apply_replan` の呼出し元はこの3箇所だけで、STARTのDerive / HumanNG・Phase entry・direct CREATEは `apply_replan` を通らない。実測（baseline `d5851c5`、remoteありProject）: (A) `plan_exclude_work` で未開始W2を除外しW3で置き換える（W2 → integrationを削除、W3 → integrationを追加）と、event・W3のfile・W3 → integrationを適用した後に `postcheck_failed`（`requires_completion predecessor W2 is plan_excluded`）。(B) STARTでintegration I1をcancelし新integration I2へhuman_confirmationの前提を付け替える（W1 → I1・I1 → confirmationを削除、W1 → I2・I2 → confirmationを追加）と、STARTのlifecycle event 4件とI2・relation 2件を適用した後に `postcheck_failed`（`predecessor Integration is cancelled`）。(C) standalone plan exclusionの置き換え、(D) Phase plan exclusion（後続Phaseの `requires_completion` を外して新standalone Workを登録）、(E) START cancelでfix Workの `return_to` 先を外して置き換え（`dependency_unreplanned` と `return_to_unreplanned`）、(G) 同じedgeを削除して新しいIDで付け直す（重複edge）も同じ順序で `postcheck_failed`。(F) 削除予定のrelationを通るときだけcycleになる並べ替えは、eventだけを適用した後に `validation_failed`。どの場合もpending 1件・dirty・`validate_project` のproblemが残り、後続operationは `structure_invalid` で停止した。新Workを伴わないrelation追加（`<prefix>:relations` stage）は途中で構造を検査しないので起きない。
- Why it matters: 所有者が妥当と判定した正式なreplan（未開始Workの置き換え、integrationのcancelと再統合、`return_to` 先の整理）が途中で失敗する。canonical stateが途中まで適用され、pending mutationが残り、replan・resumeを含む後続operationが構造precheckで止まる。rollback・自動repairを行わない規則のため、正式経路では回復できない。
- Likely scope: `apply_replan` の適用順序と、登録coreの構造検査（cycle検査・BL-027の登録前の構造検査・postcheck）が判定する状態。relation削除を実fileへ先に書く順序変更は、登録が別の理由で拒否されたときに削除だけが適用済みで残るため採らない。BL-027との整合、resume（stage名・予約key・記録済みrecord）、BL-025の残る論点(1)とSTART cancelのresumeは別論点として混ぜない。
- Cross-project impact: あり（全Projectのplan exclusion / STARTのWork cancelのreplan）
- Backfill likely: no（既に残ったpending recordや不正な構造は自動repairしない）
- Human confirmation likely: no（replanの意味・所有者の投影検査・適用順序を変えず、登録の構造検査の対象を所有者の決定に合わせる）
- Self-hosting prerequisite: no
- Evidence class: code inspection
- Resolution: 原因は、replanの登録が行う構造検査（登録前の `_check_projection` のcycle検査と、適用後の登録postcheckの `validate_structure`）が、同じoperationが登録の直後に削除すると決定済みのrelationを含む状態を判定していたこと。所有者の投影（最終状態）は通っており、失敗したのは適用手順の途中の状態に対する検査だった。修正: `create.register_works` にkeyword `removed_after`（Roadmap relation IDの列）を加え、その登録が行うすべての構造検査 — `requires_completion` のcycle検査、BL-027の登録前の構造検査（`refuse_invalid_work_writes`、掛ける呼び出しの場合）、適用後のpostcheckの `validate_structure` — を、名指しされたrelationを除いたviewで判定する。payload・endpoint・integration invariant・human_confirmationの規則と「追加relationが登録されたか」の検査は現在のviewのまま（integrationとconfirmationの規則はWorkとeventだけを読むので、除いたviewでも結果は同じ）。`ops.apply_replan` は `plan_replan` が解決済みの削除relationのIDを渡すだけで、`refuse_before_apply=False`（BL-027）、stage名（`<prefix>:works` / `<prefix>:remove` / `<prefix>:relations`）、記録と適用の順序（event → 新Work・追加relation → 削除 → commit → push）、予約key、所有者の投影検査、write scope、Mutation Controllerは変えていない。削除は従来どおり登録の後に記録・適用するので、登録が別の理由で拒否されたreplanは削除を適用しないまま、従来と同じ位置・同じmessageで止まる。`removed_after` を渡さない呼び出し（Phase entry、STARTのDerive / HumanNG、direct CREATE）は何も変わらない。採らなかった方式: relation削除を登録より先に実fileへ書く順序変更は、登録coreが別の理由（新Workのpayload不正、新Workと一緒に渡したPhase relation等）で拒否したときに削除だけが適用済みで残り、`validate_project` がcleanになって未完了のreplanを隠す。replan全体を記録前にもう一度検査する方式は既存の所有者の投影と重複し、それだけでは登録途中の検査を変えない。BL-027との関係（`d5851c5` で実測）: 登録前の構造検査はreplan経由の登録に掛かっていない（登録coreの検査関数の呼び出し0回、BL-027のtestが固定）。testだけの差し替えで試しにreplan経由にも掛けると、baselineは(F)以外を新Workの記録前・terminal eventの適用後により早く拒否してstuckのままであり、修正に対して現在のviewで掛けると(A)〜(G)が再び拒否されたが、`removed_after` を除いたviewで掛けると修正と65 stepすべて一致した。登録前の構造検査を将来replanに掛ける場合も同じviewで判定する必要があり、`register_works` はそう実装してある。BL-027の意味（不正な登録を書く前に拒否する）は変えておらず、BL-027のregression test（23 test / 36 subtest）はすべて通る。実測（A/B、baseline `d5851c5` と修正後で同じscenarioを実行し、例外の型・code・ID正規化後のmessage、実行したeffect、HEAD / remoteの移動、pending record、record status、worktree、`validate_project`、state、roadmap relation、重複（Work名・edge・terminal event・commit subject）を比較）: (1) 経路31 scenario・65 step: 差分19 stepはすべて(A)〜(G)の有効なreplanとその帰結で、修正後はevent・新Work・追加relation・削除・commit 1回・push 1回で完了し、pending 0・`validate_project` clean・HEADとremoteが一致・重複なしで、後続のSTART / Phase holdも進む（完了後の同じrequestは通常どおり `plan_excluded is only for unstarted Works` 等で拒否）。一致した46 stepには、削除だけ / 新Workだけ / 新Workを伴わない追加だけのreplan、削除が途中の状態の妥当性に関係しないreplan（integrationのcancelと再統合、`planned_next` の削除）、replanなしのPhase plan exclusion、replanなしのcancel（所有者の拒否）、STARTのDerive / HumanNG / direct CREATE、BL-027が拒否するDerive、不正なreplanのうち所有者の投影がevent前に拒否するもの（除外を修復しない削除、削除後も残るcycle、既存edgeの重複、未知のrelation type、解決できないendpoint、2件目の未完了integration、derived relationの削除、解決できない削除ID）と、登録coreがevent後に拒否するもの（通常Workへの `confirmation_target`、未知のRelated type、空の名前、新Workと一緒に渡したPhase relation、event適用後・登録前にだけ完了に見えるPhaseへの追加）が含まれ、型・code・message・適用済みeffect・pendingまで同一だった（無効なrequestは成功へ昇格していない）。(2) 回帰flow 6 scenario・33 step（Roadmap作成、Phase追加、Phase entry（human_confirmation・Related・relation付き）、START（Derive・HumanNG）、無関係なRoadmapのentryとSTART、direct CREATE、Related maintenance、Phase / Roadmapのhold / resume / cancel、BL-026のcancel拒否、achievement、BL-025のevent適用後resume、新Workなしのreplan、BL-027の登録前拒否3種）: 33 stepすべて一致。(3) resume 10 scenario × 10窓（ID予約後 / event記録後・未適用 / event適用後 / Works記録後・未適用 / Works適用後 / 削除記録後・未適用 / 削除適用後 / commit記録後・未適用 / commit後push前 / push後complete前）で、同じrequestを再実行し、pendingが残る場合はprecheckを飛ばして同じmutationの記録済みの決定から残りを進めるtest専用のdriverも実行した: 修正後はevent前の窓で同じrequestが同じmutation・同じ予約IDで完了し（baselineは `postcheck_failed`）、event後の窓の再実行はbaselineと同じく拒否され（plan exclusionは構造precheckまたはunstartedの前提条件、STARTはterminal Workの拒否、recordは不変）、baselineが欠陥のため到達しなかった削除記録後以降の窓も同じ拒否になる。driverでは修正後はplan exclusion 3種の全窓とSTART cancelのcancel event適用までの窓で、残りのeffectだけを実行して完了した（baselineが到達した同じ窓では `postcheck_failed`）。各窓で実行されたeffectを合算して決定済みの回数を超えたeffectは両者とも0件（event・Work・relation・削除・commit・pushの重複なし）、重複行も0件で、baselineより悪化した行は0件。削除が途中の状態に関係しないreplanと追加だけのreplanの6 scenarioは全行一致した。regression test（`tests/test_replan_registration.py`）は18 test / 14 subtestで、(A)〜(G)が最後まで進むこと（stageと実行effectの順序、commit・pushが1回ずつ、重複なし、後続のPhase完了）、登録coreが別の理由で拒否するreplanは削除を適用せずeventだけの従来の位置で止まること、所有者が拒否するreplanは何も書かないこと、`removed_after` は名指ししたrelationだけを登録前の構造検査・postcheck・cycle検査から除き、名指ししていないrelationの問題は従来どおり登録前（適用後の検査を求める呼び出しではpostcheck）で拒否されること、event前の窓からの同じrequestが完了すること、event後の再実行が従来どおりrecordを変えずに拒否されることを固定する。修正前のコードではこのうち18件（subtestを含む）が失敗する。規則の取り違えも固定されることを9つのmutantで確認した: cycle検査だけ除いたviewにする（15件失敗）、postcheckだけ除いたviewにする（3件）、登録前の構造検査だけ現在のviewにする（2件）、削除が名指しされていればpostcheckを省く（1件）、削除が名指しされていれば登録前の構造検査を省く（1件）、名指しすると全Roadmap relationを除く（2件）、`apply_replan` が削除を渡さない（12件）、削除を登録より先に記録・適用する（8件）、replan経由の登録にも登録前の構造検査を掛ける（このtestは通り、BL-027のtestが1件失敗して固定する）。full suiteは668 passed / 2 skipped / 526 subtests（baseline `d5851c5`）から686 passed / 2 skipped / 540 subtestsになり、増えたのはこのtestだけである。`skills/create` の登録前の構造検査の節に規則を追記した。schema・event・relation type・project.yaml・`INTENT_VERSION`・validator・Mutation Controllerは変更しておらず、backfillは不要。残る論点（今回は扱っておらず挙動も変えていない）: (1) plan exclusionはterminal eventの適用後に再実行すると前提条件（または削除前の状態に対する構造precheck）で拒否され、resumeできない（BL-025の残る論点(1)）。STARTもcancel eventを記録した後の再実行でterminal Workを拒否し、resumeできない。(2) それらをresumeする場合の要件（baselineと同一の挙動を実測）: 適用済みの削除relationを決め直すと解決できない、STARTの `_cancel` を再入するとprefixが記録済みstageの数だけ進む、記録済みのintegration登録stageで `register_works` を再実行するとintegrationを二重に数える。plan exclusionのreplan内容はinvocationに束縛されていない（BL-024）。(3) 登録coreが別の理由（新Workのpayload不正、新Workと一緒に渡したPhase relation、event適用後・登録前にだけ完了に見えるPhaseへの追加）で拒否するreplanは、terminal eventを適用した後に止まりpendingを残す（BL-027の残る論点(1)後半と同じ）。(4) 既に不正な構造やpending recordを残したProjectは自動repairしない。

### BL-029 Plan exclusion resume after its own terminal event

- ID: BL-029
- Title: Plan exclusion resume after its own terminal event
- Status: RESOLVED
- Kind: implementation, recovery
- Problem: plan exclusion（Roadmapの `plan_exclude_phase` / `plan_exclude_work`、STARTの `plan_exclude_standalone_work`）は、`plan_excluded` eventを記録・適用してから、replan（新Workの登録と追加relation、relationの削除）を記録・適用し、commit / pushする。自分のeventを適用した後に中断すると、同じrequestの再実行は、そのeventとreplanの適用済み部分を含む現在stateで構造precheck・前提条件・replanを判定して自分自身を拒否し、mutationを完了する経路が無かった。実測（investigation baseline `a9937fa`、remoteありProject、Phase（replanなし / dependencyを外して新standalone Workを登録）・Phase WorkのW2を新Workで置換・standalone WorkのS2を新Workで置換・未開始integrationのI1を新integrationで置換の5 scenario × 12中断窓（intent作成後 / ID予約後 / event記録後 / event適用後 / 新Work記録後 / 適用後 / 削除記録後 / 適用後 / commit記録後 / commit後push前 / push後complete前 / complete後））: event適用後からpush後・complete前までの到達した窓はすべて、削除を適用する前は構造precheckの `structure_invalid`（除外した対象を前提とする、削除予定のrelationがまだ残っている）、適用した後は前提条件の `spec_violation`（`plan_excluded is only for unstarted ...` 等）で拒否され、pending recordが残った。削除を適用する前の窓では `validate_project` が `dependency_unreplanned` を報告し、commit後・push前の窓ではremoteへ反映されないまま残り、未commitのevent logはそれを書く他のoperation（START、Roadmapのlifecycle、別のplan exclusion）を `reconcile_required` で止め続けた。前提条件を通しても、記録済みの削除は現在stateでは解決できず（削除済み）、記録済みの新Work登録を登録coreで再実行すると記録済みのintegrationを二重に数える。また、plan exclusionのinvocationはoperationと対象だけでreplan内容を持たず（BL-024の対象外）、event適用前の窓では、別のreplanを渡した再実行がそのrecordを継続し、再実行側の内容で成功を返していた。BL-025の残る論点(1)、BL-028の残る論点(1)(2)。BL-036の残る論点(4)（plan exclusionの再実行は前提条件で止まるので、決定の所在とcommitへの引継ぎを計測できなかった）もこれに当たる。
- Why it matters: 途中失敗・commit / push失敗から同じmutationで回復できず、write-ahead recovery contract（`rules/git`: 途中失敗は同じmutationをresumeし、commit / push失敗はGit段階からresumeする）を破る。`plan_excluded` eventとreplanの一部が未commitのまま残り、削除前の窓では構造も不正になり、pending recordとdirty fileが以後のoperationを止める。rollback・自動repairを行わない規則のため、正式経路では回復できない。別のreplanによる継続は、呼び出し側が渡した内容と正本が食い違ったまま成功を返す。
- Likely scope: plan exclusionの決定内容（replan）をinvocationへ記録するrequest identity、未完了mutationが今回のrequest自身のものであることの証明、自分が適用したeffectを除いたstateでの構造precheck・前提条件・replanの判定、記録済みの削除・新Work登録のrecordからの読み戻し、記録済み・未適用のeffectをreplayする前の検査、requestを記録していない旧record、BL-023 / BL-024 / BL-025 / BL-027 / BL-028 / BL-031 / BL-032 / BL-033 / BL-035 / BL-036との整合。STARTのWork cancelのresume（BL-030）は含めない。
- Cross-project impact: あり（全Projectのplan exclusionの回復可能性）
- Backfill likely: no（requestを記録していない既存pending recordは推測でresumeせず、人による照合とする）
- Human confirmation likely: no（canonical contractの意味を変えず、実装をcontractへ合わせる。旧recordと別requestの扱いは採用時に決定済み）
- Self-hosting prerequisite: no
- Evidence class: code inspection
- Resolution: 原因は、plan exclusionのowner（`roadmap._plan_exclude_locked` / `start._plan_exclude_standalone_locked`）が、構造precheck・前提条件・replan（削除の解決、新Workの登録、投影）を現在stateだけで判定し、自分の未完了mutationが既に適用したevent・新Work・追加relation・削除をrecovery stateとして扱わなかったこと。replan内容がinvocationに無いため未完了mutationが今回のrequestのものか示せず、記録済みstage（削除・新Work登録）をrecordから読み戻す経路も無かった。BL-025のlifecycle resumeはevent 1件だけを除くので、新Work・追加relationの二重計上、削除済みrelationの解決不能、integration置換でのintegration invariantが残り、plan exclusionにはそのまま使えない（調査で実測）。修正は `src/workline/ops.py` のprivate関数とowner 2箇所からの呼び出しで、`src/workline/create.py` は登録coreの検査とeffect構築を同じ順序・同じ報告のままprivate関数へ切り出しただけ（`register_works` の挙動は不変）。(1) request identity: plan exclusionは決定内容をinvocationの `request` として記録し、`MutationController.begin` が最初のID予約より前にdurableに書く。内容はversion・対象、replanの新Work（key・name・成立状態・phase_id・roadmap_id・work_kind・confirmation_target・Related・derivation detail）を宣言順で、追加relation（type / from / to）を宣言順で、削除relation IDを指定順で。正規化は既存のrequest identityと同じで、成立状態とderivation detailはrenderingがstripするのでstrip後、derivation detailの有無は区別し、name・ID・kind・confirmation target・Related・endpointは書かれたとおり（新Workのkeyと、そのkeyに予約されたIDは別request）。どのbranchで確定するかはrequestに含めない（Mutation Controllerが決定したeffectと一緒に記録する）。(2) 未完了mutationの判定: 同じowner・operation・対象（slot）の未完了mutationを、mutationを開く前・何もreplayする前に探し、BL-024と同じ規則で、requestを記録していないrecord、同じslotの複数record、別requestのrecordはどの中断位置でも `reconcile_required` とし、recordを変更しない。stageを記録していないrecordは何も適用していないので、予約IDがこのrequestの予約するkeyと種類だけであることを示せば、従来どおり現在stateで判定する。(3) recordの証明: stageを記録したrecordは、次をすべて示せる時だけ継続する。予約IDがこのrequestの予約するkeyだけに、そのkeyの種類の互いに異なるIDとしてある。stageが `event` → `replan:works`（新Workが無ければ `replan:relations`）→ `replan:remove` → `finalize` の先頭部分で、eventは予約IDでこの対象の `plan_excluded` ちょうど1件、登録stageはrequestと予約IDから登録coreが作るeffect列（Work file・derivation detail・追加relation・Related。display番号は現在のWork数から自分の新Workを除いた数から）と完全一致、relation追加stageはrequestの追加relationと一致、削除stageはRoadmap relationの削除だけでIDがrequestと同じ順、commit stageは `git_commit`（と `git_push`）だけでpathsが記録済みfile effectのpaths、messageがこのoperationのmessage。file effectのclassifyに不一致が無く、適用済みが記録順の先頭部分で、未適用は最後に記録したstageにだけあり、commitを記録した後には無い。effectに記録された決定の所在（`decided_on`）と、commitのbase・branch・push先はこの証明に含めない（Mutation Controllerの分類と検査に任せる）。(4) 自分のeffectを除いたstate: 証明したrecordの適用済みfile effectを現在stateから取り除いたview（eventと追加relationをIDで除き、書いたentity fileを除き、削除したrelationを記録したsnapshotから1回だけ戻す。何も書かない）で、ownerと同じ順に構造precheck（`structure_invalid`）・前提条件・commit messageの照合・削除の解決（記録済みの削除stageがあればそのsnapshot）・replan全体の投影（`validate_projection`）を行う。(5) replay前の検査: 記録済み・未適用のeffectと未記録のstageを、replayする前に、登録coreが記録前に行う検査（payload・endpoint・integration invariant・human_confirmation・`requires_completion` cycle。登録が会う状態と `removed_after` の構造で）、effectの記録時の検査（`validate_effect`）、残りをすべて適用した後の構造検査、commitが未記録なら開始前からdirtyなpathとの分離で検査する。拒否は記録・適用した場合と同じ型・code・message（`spec_violation` / `validation_failed` / `dirty_overlap` 等）で、eventを含め何もreplay・記録・commit・pushせず、recordを変更しない。(6) 継続: 証明・判定・検査はすべて読取りだけで、その後にmutationを開き（`Mutation.apply` が記録済みeffectをreplayする）、未記録のstageだけを従来の順序で記録・適用する。`ops.apply_replan` は記録済みの登録stageを登録coreで再実行せず、予約IDとstageのeffectから読み戻す（STARTのcancelはprefixが常に未記録のstageを指すので通らない）。削除は記録済みstageのsnapshotを使う。変更していないもの: stage名・記録と適用の順序・予約key・`removed_after`（BL-028）、replan登録の `refuse_before_apply=False`（BL-027）、新規のplan exclusionの動作（invocationに `request` が加わるだけで、stage・effect・commitは同一）、Mutation Controller（BL-032 / BL-033 / BL-035の記録済みcommitの分類、BL-036の決定の所在）、`INTENT_VERSION`・closeしたrecordのtop-level field・effect種別・payload、operationの公開API、`Mutation` / `MutationController` の公開method、lifecycle operationのresume（BL-025）、Phase entry（BL-023）、STARTのterminal finalization（BL-031）とcancel（BL-030）。Git recovery contractとの関係: resumeはすべて `Mutation.apply` / `add_effects` / `reserve_id` / `set_note` / `extend_scope` を通るので、BL-036の決定の所在の検査を迂回しない。eventと登録・追加・削除のstageは記録時に所在を持ち、決定を記録した後・commitを記録する前に別branchへ移った再実行は、読取りだけの証明・判定・検査の後、最初のreplayより前に `reconcile_required` で止まり（移った先のstateでそれらが拒否すれば、その拒否で止まる）、何もreplay・記録（新Work・relation・削除・Git stage）・commit・pushしない。決定したbranchへ戻れば同じ再実行が完了し、commitは所在のbranchを `branch` として引き継ぐ。commitを記録した後は、BL-035（HEADがbaseのままでも別branchなら停止）、BL-032（独立なcommitで伸びただけのbranchでは記録どおりのcommitをその上に作る）、BL-033（同じmessageのcommitを作成済みとみなさない）がそのまま扱う。旧recordは2種類を区別する: requestを記録していないrecordは(2)で `reconcile_required`（所在の有無より先に判定）、requestを記録していても決定の所在やcommitの `branch` を持たないrecordは `rules/git` のCommit / pushに従って推測せず `reconcile_required`。実測（設計時、同じ設計のthrowaway prototypeで、baseline `a9937fa` と `13804d6` のそれぞれと比較）: 上記5 scenario × 12窓 × 11種の再実行（同じrequest / 新Work・成立状態・追加relation・削除・順序を変えたrequest / requestを消した旧record / 複製した複数record / 書き換えたstage / requestに無い予約key / 別operation・別対象）の1128行で、意図外の差分は0。同じrequestは到達した全窓で同じmutationのまま完了し、中断と再実行を通して実行したevent・Work・relation追加・削除・commit・pushは決定した数ちょうど1回、pending 0、HEADとremoteが一致、`validate_project` clean、重複0。別内容・request無し・複数・書換え・予約不一致はpendingのある全窓で `reconcile_required`（実行effect 0、recordのbytes不変、HEAD・remote不変）。成功していた再実行が拒否になったのはintent作成後 / ID予約後 / event記録後の窓の80行だけで、すべて採用時に決めた安全性の強化（別内容のrequestによる継続45、requestを持たない旧record 15、requestに無い予約key 15、書き換えたeventを適用して成功を返していた偽成功5）。拒否のまま理由が `reconcile_required` へ変わった行が282行、両方 `reconcile_required` でmessageだけ変わった行が15行。replay前の検査は、baselineがeventをreplayしてから失敗してpendingと不正な構造を残した3形（新WorkのPhaseが中断の間にcancel、新Workと一緒に渡したPhase relation、中断の間に加わったrelationで投影が不正）を、同じ型・code・messageのまま、replay前・record不変に変えた。新規requestの入力境界14行は12行が完全一致し、残る2行はAPIの誤用（Replanに想定外のobject）で、baselineより早く失敗し、baselineが残した空のpending recordを残さない。STARTのcancelの同じ形のmatrix（401行）とBL-031のmatrix（714行、`13804d6`）はbaselineと全行一致した。本実装（`9178e97` 上）: regression test（`tests/test_plan_exclusion_resume.py`）は26 test / 50 subtestで、Work置換（10窓）・standalone置換・Phase（replanなし / あり）・integration置換が同じmutationで残りのeffectだけを実行して完了すること（stageの順序、integrationを二重に数えない、commit・push 1回、pending 0）、requestがID予約より前にdiskにあり正規化どおりであること、別内容のrequest・requestの無いrecord・複数record・書き換えたstage・予約不一致・commit記録後に戻されたeffectが `reconcile_required` でrecordを変えないこと、replay前の検査がeventを適用せずに止まること、commit記録後・commitがhookに拒否された後・独立なcommitの後・同じmessageの別commitの後に記録どおりのcommitを1回作ること、別branchでは決定を記録した後の窓もcommit記録後の窓も何も変えずに止まり、戻れば完了して所在がcommitの `branch` へ引き継がれること、commitのbranchまたは決定の所在を消したrecordはrequestがあっても推測で進まず、requestを消したrecordとは別の理由で止まること、STARTのterminal finalizationがplan exclusionのrecordを読まず、plan exclusionがSTARTの未完了completionを乗っ取らないこと、中断の無いplan exclusion・STARTのcancelの従来の拒否・別operationによる乗っ取りなしが変わらないことを固定する。修正前のコード（`9178e97`）ではこのうち21 test（subtestを含む47件）が失敗し、失敗しない5 testは変えてはいけない挙動だけである。規則の取り違えも4つのmutantで検出されることを確認した: 別requestのrecordをそのrequestのものとして継続する（4件失敗）、現在stateで判定する（30件）、replay前の検査を外す（2件）、記録済みの登録を登録し直す（integration置換で2件）。既存testは2件を更新した: `tests/test_decided_content_binding.py` の、別replanの再実行がplan exclusionのrecordを継続することを固定していたtestを、`reconcile_required`・record不変・再実行の内容を書かないことへ、`tests/test_replan_registration.py` の `KnownResidualTests` のうちplan exclusionのevent適用後の再実行が拒否されることを固定していたtestを、同じmutationで完了することへ（STARTのcancel側は不変）。直接関係するBL-031 / BL-032 / BL-033 / BL-035 / BL-036のtestは変更なしですべて通る。full suiteは768 passed / 2 skipped / 712 subtests（baseline `9178e97`）から794 passed / 2 skipped / 762 subtestsになり、増えたのはこのtestだけである（通過したtestは、名前を変えた上記2件を除いて同一）。計測の後にBL-034（`260cd07`）がlandingしたので、BL-029の変更file（BL-034の変更とは重ならず、`9178e97` と同一の内容）を同じ内容で最新mainへ再統合した。`skills/roadmap` のMutation / Git（request identityの記録内容、plan exclusionのresume）、`skills/start` のWork cancel（standalone plan exclusionのresumeとSTARTのresumeの分離）、`skills/create` の登録前の構造検査（resumeでの登録の読み戻しと事前検査）へ反映した。schema・event・relation type・project.yaml・`INTENT_VERSION`・validatorは変更しておらず、recordのinvocationに `request` が加わるだけで、backfillは不要。残る論点（今回は扱っておらず挙動も変えていない）: (1) STARTのWork cancelはcancel eventを記録した後の再実行を従来どおり拒否する（BL-030）。(2) requestを記録していない既存のpending plan exclusionはどの窓でも `reconcile_required` になり、人による照合が要る。(3) 適用済みの削除のsnapshotの内容（IDと順序以外）を書き換えたrecordは検出しない（削除は適用済みなので何も書かない）。(4) replay前の検査や証明で止まったrecordはpendingのまま残り、write scopeの重なるoperationを止めるので、人による照合が要る（自動abandonしない）。

### BL-030 START Work cancel resume requires a durable cancel decision

- ID: BL-030
- Title: START Work cancel resume requires a durable cancel decision
- Status: RESOLVED
- Kind: implementation, recovery
- Problem: STARTのWork cancel（`start._Session._cancel`）は、executorが返したcancelの決定（replanの新Work・追加relation・削除relation、理由）をrecovery recordに記録しなかった。recordに残るのはcancelのlifecycle event（`work_target_removed` / `work_cancelled`）とreplanの予約keyだけで、新Workの内容と追加relationのendpointは登録stageを記録した後、削除relationの内容は削除stageを記録した後にしか現れず、理由は最後まで記録されない。cancel eventを記録した後に中断すると、同じWork・同じmodeのSTART再実行は決定をrecordから再構成できず、cancel済みWorkのexecutorを再実行することもできなかった。実測（`4d24730`、remoteありProject、W1の完了後にintegration I1をcancelしてconfirmationを新integration I2へ付け替えるreplan、10中断窓）: single-workの再実行は、cancel eventの記録後（未適用）の窓ではeventをreplayしてからSTART precheckの `structure_invalid`（`dependency_unreplanned`）、event適用後から削除stageの記録までの窓ではmutationを開く前のSTART precheckの `structure_invalid`、削除の適用後・structural validationの後・commitの記録後・commit後push前・push後complete前の窓では `spec_violation`（`terminal Works are not started`。commitが記録済みならcommit / pushをreplayした後）で止まり、どの窓でもmutationがpendingのまま残った。outerの再実行は、cancel eventの記録後・適用後の窓ではBL-031によりreplayの前に `reconcile_required` で止まり（依存を外すPhaseのreplanでは適用後の窓でSTART precheckの `structure_invalid`）、commitの記録後の窓ではcommit / pushをreplayした後に次のWorkを選んで実行し、`cancelled` ではなく `stopped` を返した。`_cancel` へ入り直すと、prefixが記録済みstageの数から `:cancel:1` / `:cancel:2` へずれ、削除の適用後は削除relationを現在stateで解決できず、integrationの登録stageの後は登録coreが記録済みのintegrationを二重に数える。
- Why it matters: cancel eventを記録した後の中断から同じmutationで回復できず、write-ahead recovery contract（`rules/git`: 途中失敗は同じmutationをresumeする）を破る。pending recordはevent logとrelationをwrite scopeに持つので、それらを書く後続operationを止め続ける。outerではcommit記録後の再実行が、cancelではなく次のWorkの結果を返していた。
- Likely scope: cancelの決定を、notesではない機械検証可能な形でcancel eventと同じsaveに記録すること。再実行はexecutorを再実行せず、記録済みの決定と記録済みのeffectから残りだけを進め、prefix・予約ID・削除relation・登録stageをrecordから取ること。決定を持たない旧recordは推測せず `reconcile_required`。outerは未完了のcancelを終えるまで次のWorkを選ばない。recordが保てない決定の値の扱い。BL-026 / BL-027 / BL-028 / BL-029 / BL-031 / BL-032 / BL-033 / BL-035 / BL-036との整合。`INTENT_VERSION` / closed record fields / effect kinds / Mutation public methodsは変えない。
- Cross-project impact: あり（全ProjectのSTART Work cancelの回復可能性）
- Backfill likely: no（決定を記録していない既存のpending recordは推測でresumeせず、人による照合とする）
- Human confirmation likely: no（決定の記録位置、single-work / outerの再実行の結果、旧recordと保てない値の扱いは採用時に決定済み）
- Self-hosting prerequisite: no
- Evidence class: code inspection
- Resolution: 原因は、cancelの決定がexecutorの戻り値のままrecovery recordに入らず、START再実行がrecordから決定を再構成できなかったことと、再実行が現在stateからprefix・削除・登録を決め直す経路しか持たなかったこと。修正は `src/workline/start.py`（cancelの記録と、再実行でのrecordの証明・継続）と、`src/workline/ops.py` にあるBL-029のprivate helperの共有（`_prove_plan_exclusion` から記録済みstageの読み取り `_recorded_stages` と適用状況の分類 `_recorded_progress` を切り出し、replay前検査 `_refuse_before_replay` にstageのprefix・構造拒否の文脈・commitが運ぶeffectを渡せるようにした。plan exclusionでの既定値は従来どおりで、挙動は変えていない）。(1) 記録: `_cancel` はreplanの解決と予約、owner投影（`cancel replan`）を従来どおり行った後、決定を `work_cancelled` の `append_event` effectのpayloadのkey `cancel` に載せ、cancelのlifecycle stage（`<Work>:lifecycle:<n>`）を記録する同じ `add_effects` のsaveでdurableにする。決定は `version`（1）・`work_id`・`prefix`（replan stageのprefix）・`reason`・`new_works`（key・name・desired_state・phase_id・roadmap_id・work_kind・confirmation_target・Related（type / to / condition）・derivation_detailを宣言順に、executorが渡した値のまま）・`add_relations`（type / from / toを宣言順に、endpointは書かれたとおり）・`remove_relations`（決定した時にProjectが持っていたrelation recordを指定順に）で、予約IDは `reserved_ids` のまま決定に書かない。同じsaveでMutation Controllerが両eventに決定の所在（`decided_on`、BL-036）を記録し、cancelのcommitはそのbranchを持つ。recordのtop-level field・`INTENT_VERSION`・effect kind・stage名・予約key・effectの順序・commit messageは変えていない。(2) recordが保てない決定: 決定はrecordと同じ形式（yamlish、UTF-8）で書いて読み戻し、keyを含むすべての部分が同じ種類の同じ値であり、かつ再実行が決定として読む形（新Workのname・desired_state・Relatedのtype / toが文字列、phase_id・roadmap_id・work_kind・derivation_detailが文字列かnull、conditionがmappingかnull、新Workのkey・endpoint・confirmation targetがIDかkeyとして使える値）である場合だけ記録する。保てる値は文字列（前後の空白・引用符・予約語や数字に見えるもの・制御文字・非ASCIIを含む）、整数、true / false、null、それらのlistと、空でない文字列をkeyとするmapping。保てない値 — 小数、tuple / set / bytes / その他のobject、listの中のlist、文字列以外や空のkey（listの中のmappingの整数keyは読み戻すと文字列になる）、recordの行が分断されるU+0085 / U+2028 / U+2029やsurrogateを含む文字列、書き出せない大きさの整数 — と、その形にならないreplan（`RelatedSpec` の形でないRelated、文字列でない新Workのname等）は、owner投影の後、cancelのlifecycle stageを記録する前に `validation_failed`（`cancel decision: ...`）でSTOPし、cancelのeventも決定も記録しない。mutationには投影で拒否された場合と同じくWork開始のlifecycleと予約だけが残り、再実行はexecutorに決め直させる。入力境界のA/B（33 case × 最初のSTARTと同じSTARTの再実行、`4d24730` と修正後）: 14 caseは両stepとも完全一致（有効なreplan、前後空白の値、理由の空文字列・null・整数・true・mapping・引用符や改行を含む文字列、条件の追加key、duck typingのRelated、整数keyの新Work、replanの無いcancelとIDの解決できない削除の拒否）。11 caseは中断の無いcancelが成功から記録前の `validation_failed` に変わる（理由が小数・object・listの中のlist・tuple・整数keyのmappingを含むlist・行区切りを含む文字列、新Workのname・成立状態・derivation detailが行区切りを含む、新Workのkeyがtuple・小数。いずれも以前は中断すれば再開できないrecordを作っていた。意図した変更）。5 caseは以前もcancel eventを適用した後に登録coreの拒否やrecord保存時の例外（`YamlishError` / `AttributeError`）で止まってcancel eventを残していたもので、cancel eventを記録する前の `validation_failed` に変わる（tupleのconfirmation target、小数を含むcondition 2種、`RelatedSpec` の形でないRelated、文字列でない新Workのname）。3 caseは最初のSTARTの拒否（登録coreの `validation_failed`）が同じで、再実行がSTART precheckの `structure_invalid` から同じ `validation_failed`（replay前検査）に変わる（通常Workのconfirmation target、空白だけの成立状態、未知のRelated type）。どのcaseでも、成功への昇格は無い。(3) 再実行: `_start_locked` はSTART precheck・mutationのopen・replayより前に `_cancel_to_finish` でrecordだけを読む。同じWork・同じmodeのpending STARTにcancelが無ければ従来どおり（読めないrecovery areaは従来どおりopen時に報告）。複数あれば `reconcile_required`。決定を持たないcancel（旧implementationのrecord）は、commitの記録の有無にかかわらず `reconcile_required` で、event・commit・現在state・予約から決定を推測しない。決定があれば次をすべて示せる時だけ継続し、示せなければ `reconcile_required` でrecordを1 byteも変えない: cancel stageが対象Workの `work_target_removed`（targetがあれば）/ `work_cancelled` だけを予約済みIDで持つそのWorkの次のlifecycle stageで、決定が `work_cancelled` にだけ載っている。決定が(2)の形で、対象がeventのWorkと一致し、single-workでは指定Work、outerでは同じPhase範囲のWorkである。prefixがそのcancelの記録するprefixである。cancel stageの後のstageが、決定の作る `<prefix>:works`（新Workが無ければ `<prefix>:relations`）→ `<prefix>:remove` → `commit:<n>` の先頭部分で、それより前の終端stageは自分のfinalizeが済んだcompletionだけである。決定が使う予約ID（新Work・追加relation、登録stageが記録済みならRelated・derivation）が正しい種類で互いに異なる。記録済みの登録stageが決定と予約から登録coreの作るeffect列（BL-029の `_registration_effects`）と、`:relations` / `:remove` が決定の追加 / 削除から作るeffect列と、kindとpayloadで一致する。記録済みのcommitがcommit（とpush）だけで、messageが `chore(workline): cancel <display>`、pathsがこのmutationの書くpathに含まれる。適用済みのfile effectが記録順の先頭部分で、未適用のeffectは最後のstageにだけあり、commitの記録後には無い。commitが未記録なら、このmutationが記録していないcommitによってHEADのevent logが既にcancel eventを持っていない。そのうえで、cancel自身の適用済みeffectを除いたProject（BL-029の `_own_effects_free_view`。同じmutationで先に確定したWorkの結果は残す）で、WorkがcancelのeventのとおりにIn progressであること、START precheck（`structure_invalid`）、削除するrelationを決定の内容のまま持つこと（持たなければ `reconcile_required`）、cancelのreplanの投影（`spec_violation`）を判定し、BL-029の `_refuse_before_replay` で、残りの登録を登録coreが記録前に行う検査、まだ記録していないeffectのvalidation、残りをすべて適用したProjectの構造、commitが未記録ならpre-existing dirtyとの分離（`dirty_overlap`）を、何もreplayする前にそれぞれ本来のcodeで拒否する。(4) 継続: 証明したmutationを開き（別のmutationを開いたら `reconcile_required`）、BL-031の `_completion_to_finish` は使わずにreplayする。決定の所在と違うbranchでは、Mutation Controllerがreplayの前に `reconcile_required` で止め、決定したbranchへ戻れば同じ再実行が続きから進む（BL-036）。`_Session.finish_cancel` はexecutorを呼ばず、commitが記録済みならreplayで済んだcancelを、そうでなければ `ops.apply_replan` をrecordのprefix・決定から組み直したreplan・予約ID・決定の削除relationで呼び（記録済みの登録stageは、BL-029と同じく登録coreを再実行せずrecordから読み戻す）、cancel structural validation・commit（決定のbranch）/ pushを中断が無い場合と同じstage名で行って、決定の理由とともに `cancelled` を返す。outerでも次のWorkを選ばず、Phase完了やintegrationへ進まない。commit記録後に同じbranchで独立なcommitがHEADを進めた場合はBL-032のとおりその上にcommitし、commit記録後に別branchへ移った場合はBL-035のとおり止まる。BL-031の `_completion_to_finish` でsingle-workのcancelを素通ししていた分岐は、cancelを持つrecordを先に `_cancel_to_finish` が扱うので到達しなくなり、防御として両modeとも `reconcile_required` にした（completionの扱いは不変）。変えた既存test: `test_terminal_finalization_resume.py::UnfinishedCancelTests`（outer / single-work / commit記録済みの再実行が `cancelled` で終わり、次のWorkを実行しない）、`test_replan_registration.py::KnownResidualTests::test_start_cancel_after_its_event_now_finishes`、`test_plan_exclusion_resume.py::NeighbourTests::test_a_start_cancel_after_its_event_finishes_from_its_own_decision`、`test_decision_branch_binding.py::ContinuationTests::test_a_cancel_is_carried_on_only_on_the_branch_it_was_decided_on`、`test_recorded_commit_resume.py::StartTests::test_an_outer_cancel_is_committed_after_an_independent_commit`（`stopped` → `cancelled`）。実測: 新規 `tests/test_cancel_decision_resume.py` は28 test / 66 subtest（決定の記録と同じsave・所在・commitへの引継ぎ、保てる値の継続と保てない値の記録前の拒否、10窓の継続（残りのeffectだけを実行、stage名とprefixが中断の無い場合と同じ、予約の追加なし、integrationの二重登録なし、executorを呼ばない）、Related・derivation detailの復元、executor返却前・決定の記録前の窓（executorに決め直させる）、別の決定を返すexecutor・completion・失敗するexecutorでも記録済みの決定で完了、別mode・別Workによる乗っ取りなし、outerの5窓とPhaseのreplan、決定を持たない旧recordの6窓（commit記録済み・commit後を含む）、recordの改ざん13種・Projectのrelationの書き換え・同じSTARTの複数record、別branch・commit記録後の別branch・同じbranchの独立なcommit、BL-026 / BL-027の拒否、replay前検査、他人がcommitしたcancel event、BL-028の中間構造、BL-031のcompletion）。修正前の実装（`4d24730`）に同じfileを適用すると77件（15 test / 62 subtest）が失敗し、executor返却前の窓の2 testと、replanの無いcancelが記録前に拒否されるtestだけが通る。規則を1つずつ壊したmutant 4種はすべて検出した（決定を持たない旧recordのcommit記録済みの窓をreplayで進める: 3 subtest、保てない決定を決定なしで記録する: 8 subtest、記録済みの登録stageを登録coreで登録し直す: 5窓で `integration invariant`、cancelのlifecycle stageに決定の所在を記録しない: 別branchの窓1 subtestと記録の形のtest）。残る論点: 同じSTART mutationの先行stage（Derive等）が追加したrelationをcancelが削除すると、削除適用後の再実行はその追加を未適用と分類して `reconcile_required` になる（Mutation Controllerの分類の問題）。記録済みの決定・予約を、どのeffectとも矛盾しないように人が書き換えた場合は検出しない。記録済みの登録stageの表示番号は「Work数 − 自分の新Workで存在する数」で示すので、中断中に人がWork fileを足すと `reconcile_required`。再実行が返す `completed_work_ids` は再実行したsessionの分だけ（BL-031と同じ）。開始前からevent logに変更があると、再実行はreplayの前に `dirty_overlap` で止まり続ける（BL-031の残る論点と同型）。yamlishがU+0085 / U+2028 / U+2029を含む文字列を読み戻せないこと自体は、他のrecordやcanonical fileにも及ぶが、このBLでは直していない。

### BL-031 START closes an unfinished terminal mutation without committing it

- ID: BL-031
- Title: START closes an unfinished terminal mutation without committing it
- Status: RESOLVED
- Kind: implementation, recovery
- Problem: STARTの再実行が、自分のmutationが記録・適用したterminal lifecycle event（`work_target_removed` / `work_completed`、cancelでは `work_cancelled`）を、それを含むcommit / pushを行わないまま `completed` / `stopped` / `phase_complete` を返してmutationをcompletedにする、または次のWorkのcommitへ混ぜる。実測（baseline `a9937fa`、remoteありProject、17 scenario × 14中断窓）: (1) single-work STARTを `work_completed` の記録後（未適用・適用済み）、finalize commitの記録前に中断し、同じWork・同じmodeのSTARTを再実行すると、`run_work` が現在stateのcompletedを見て `completed` を返し、`mutation.complete()` まで進む。HEADもremoteも動かず、`events.jsonl` は未commitのままdirtyで残り、recordはcompletedになり、`validate_project` は問題を報告しない。次にevent logをcommitするSTART（同じPhaseのintegration、無関係なstandalone Work）は、そのdirtyを開始前からの変更と見なして `dirty_overlap`（`.workline/events/events.jsonl`）でSTOPし、自分のlifecycle eventを適用済みのpendingをさらに残す。(2) outer STARTの同じ窓の再実行は、次のWorkが無ければ `stopped`（standalone scope）、同等に計画された候補が複数なら `stopped`、最後のWork（integration）なら `phase_complete` を返してmutationを閉じ、eventは未commitのまま残る。次のWorkがあれば再実行はそのWorkを実行し、中断したWorkの `work_completed` は次のWorkの最終commitに入ってcommit・pushされ、中断したWork自身の `chore(workline): complete <display>` commitは作られない。(3) outer STARTでS1の完了後にS2がcancelされ、S2のcancel eventの記録後（未適用・適用済み）に中断してentryのS1から同じSTARTを再実行すると、S2の後に何も無ければ `stopped`（`no startable Work in standalone scope`）を返してmutationをcompletedにし、S2の `work_target_removed` / `work_cancelled` はcommitもpushもされずdirtyのまま残る。S2の次のWorkがあればそれを実行してcancel eventをそのWorkのcommitへ入れる。Phaseのcancelでreplanが `requires_completion` を削除する場合、削除の適用後の窓ではintegrationまで実行して `phase_complete` になり（cancel eventはintegrationのcommitへ入る）、cancel eventを記録済み・未適用の窓ではeventをreplayしてからpostcheckの `structure_invalid` で止まり、`validate_project` が `dependency_unreplanned` を報告する状態を残す。(4) 中断が無くても起きる: event logに開始前からの変更（内容を変えない空行など）があると、single-work STARTはcompletion eventを適用した後、finalize commitを記録する前に `dirty_overlap` でSTOPし、その再実行は `completed` を返してmutationを閉じる。どれも、STARTのresume分岐が現在stateから実行するWorkを選び直し、記録済みのterminal finalization（terminal event → finalize commit → push）が未完了かどうかを見ずに `mutation.complete()` へ進むことによる。finalize commitが記録済みの窓（commit記録後・commit後push前・push後complete前）は、resume時のreplayがcommit / pushを行うので起きない。single-workのcancel再実行は `run_work` がterminal Workを拒否してpendingのまま残るので、この形にはならない（BL-030）。BL-029 / BL-030の調査中に見つかった。
- Why it matters: START自身がterminal eventを適用したのにcommit / pushせず成功を返すため、呼び出し側はWorkが外部的にも完了 / cancelされたと扱う。`skills/start` のTerminal finalization（completed event記録後のcommit / push失敗は、同じpending finalization mutationをresumeしてGit段階だけ継続する）と、`rules/git` の成功条件（remoteありProjectでは必要成果がremoteへ反映されるまで成功扱いにしない）に反する。mutationはcompletedで閉じられるので正式なresume経路も残らず、未commitの `events.jsonl` がevent logをcommitする後続operationを `dirty_overlap` で止め続ける。outerでは中断したWorkのfinalization commitが存在しないまま、そのeventが別Workのcommitに紛れる。`validate_project` は構造だけを見るので検出しない。
- Likely scope: STARTのresume分岐（`_start_locked` のresume時のWork選択、`run_work` のcompleted早期return、outer continuationの次Work選択）と、`mutation.complete()` へ進む前提。自分のmutationが適用したterminal effectが未commit / 未pushのまま `completed` / `stopped` を返さないこと。記録済みfinalizationをGit段階から継続できる場合（completion）と、継続に必要な決定が記録に無い場合（replanを伴うcancel。BL-030）にpendingのままSTOPする場合の切り分け。中断窓（results commitの記録前後、terminal lifecycle stageの記録・適用、finalize commitの記録・commit後・push後）、single-work / outer、completion / cancel。BL-030（cancel決定の記録とresume）は混ぜない。
- Cross-project impact: あり（全ProjectのSTART finalizationの回復可能性）
- Backfill likely: no（既に未commitのまま閉じたmutationは自動repairしない）
- Human confirmation likely: no（`skills/start` のTerminal finalizationと `rules/git` の規定へ実装を合わせる）
- Self-hosting prerequisite: no
- Evidence class: code inspection
- Resolution: 原因は `start._start_locked` のresume分岐が、resumeしたmutationのrecordを見ずに現在stateから実行するWorkを選び直していたこと。single-workでは `_Session.run_work` がcompletedのWorkをそのまま `completed` として返し、outerでは `_next_or_ambiguous` が次のWorkを選び（無ければ `stopped` / `phase_complete`）、どちらも、自分のmutationが記録したterminal lifecycle stage（`<Work>:lifecycle:<n>`）の後にそれを確定するGit stage（completionは `<Work>:finalize:<n>`、cancelは `commit:<n>`）が記録されていないことを判定しないまま `mutation.complete()` へ進んだ。修正: `_start_locked` は、resumeしたmutationについて、記録済みeffectのreplayとpre-existing dirtyの記録より前に、`_completion_to_finish` でrecordだけを読んで次を決める。(1) Git stageの無いterminal stageが無ければ従来どおり進む（Git stageが記録済みなら従来どおりreplayがcommit / pushを行う）。(2) 未完了のterminal stageがcompletionで、recordがそれをこのSTART自身のcompletionと示す場合 — そのstageが対象Workの `work_target_removed` / `work_completed` の `append_event` 2件だけを予約済みID（`<stage>:event:0` / `:1`）で持ち、recordの最後のstageであり、single-workでは指定Workのものであり、HEADのevent logがそのeventをまだ持たない（`git grep` で判定し、Gitが答えられなければ持つとみなす） — は、replayの後に `_Session.finish_completion` がそのWorkの最終commit（`<Work>:finalize:0`、`chore(workline): complete <display>`）・push・postcheckだけを行い、completedの結果（完了したWorkとHEADを含む）を返す。executorを呼ばず、eventを追加せず、他のWorkを選ばない。最終commitとpostcheckは通常の完了と同じ処理（`_complete` から切り出した `_finalize_completion`）で、stage名・commit message・予約keyは中断が無い場合と同一。single-workはそのまま閉じ、outerはその後に従来のcontinuation（Phase complete判定、次Workの選択、同等候補のstop）へ進むので、中断したWorkのeventは自分のfinalization commitでcommit・pushされてから次のWorkが走る。(3) 未完了のterminal stageがcancelなら、cancelで決めたreplan（削除relation・追加relation・新Work）はrecordに無く、記録済みstageから残りを決められないので継続しない。outerでは `reconcile_required` でSTOPし、replay・executor・次Workの選択・commit・pushのいずれも行わず、recordを1 byteも変えない。single-workで指定Work自身のcancelなら判定を変えず、従来どおり拒否されて（`run_work` のterminal Workの拒否、replanの削除前はSTART precheckの `structure_invalid`）pendingのまま残る（BL-028のKnownResidualTestsと同じ結果）。(4) それ以外 — 未完了のterminal stageが複数、completion stageの形・位置・予約IDが違う、single-workのrecordに別Workのcompletion、HEADのevent logが既にそのeventを持つ（このmutationが記録していないcommitがある） — は、このSTART自身のfinalizationと示せないので、成功扱いにせず `reconcile_required` でrecordを変えずにSTOPする。Workline自身はこれらのrecordやGit状態を作らない。cancelのGit段階からの継続について調べたこと: cancelの `commit:<n>` が記録済みなら従来どおりreplayでcommit / pushが済むので、未commit / 未pushのterminal effectを残して成功を返すことはない（その後outerが次のWorkへ進み、結果が `cancelled` でない点はBL-030の論点）。`commit:<n>` の記録前にreplanの記録・適用がすべて済んだことをrecordから示せるのは、replanの最後のstageである `:remove` が記録済みの場合だけで、削除を含まないreplanやreplanの無いcancelでは示せない。また継続には結果のcancel理由など記録に無い情報が要り、single-workの現在の拒否を成功へ変えることになるため、BL-030へ残した。実測（A/B、baseline `a9937fa` と修正後で同じscenarioを実行し、例外の型・code・ID正規化後のmessage、実行したeffect、executorの呼出し、HEAD / remoteの移動、record（effectのclassify・予約key）、pending、worktreeのdirty、`validate_project`、state、relation、event、重複、中断したWorkのterminal eventを持ち込んだcommit、追加されたcommit subjectを比較）: (a) 17 scenario × 14窓（results commitの記録前 / 記録後・未実行 / commit後push前 / push後、terminal stageの記録後・未適用 / 適用後、cancel replanのWorks・relationsの記録 / 適用とremovalの記録 / 適用、最終commitの記録後・未実行 / commit後push前 / push後complete前、complete後）× 3 step（中断、同じSTARTの再実行、event logをcommitする無関係なSTART）の714行: 中断のstepは全行一致し、差分は68行（34窓の再実行とその後のSTART）ですべて次のいずれかだった。completion 8 scenario（single-workの成果あり / なし、outerの次Workあり / 2番目のWork / Phase complete / standaloneで次Workなし / 同等候補 / standaloneで次Workあり）のterminal stage記録後・適用後の16窓: baselineは未commitのまま成功を返す（10窓）か次のWorkのcommitへ混ぜる（6窓）が、修正後は中断したWorkの最終commitとpushを行ってから結果を返し、clean・pending 0・HEADとremoteが一致・executorの再実行なし・eventとcommitの重複なしで、その後のSTARTも完了する。outer cancel 4 scenario（次Workなし / 次Workあり / entry自身のcancel / Phaseのreplan）の8窓: baselineは未commitのまま `stopped` を返す（4窓）か次のWorkを実行してcancel eventをそのcommitへ混ぜる（3窓、Phaseのremoval適用後はintegrationまで実行して `phase_complete`）が、修正後は何も実行せず `reconcile_required` で止まり、recordとevent logとHEAD / remoteは不変。残る1窓（Phaseのcancel eventを記録済み・未適用）はbaselineも停止していたが、baselineはcancel eventをreplayしてからpostcheckの `structure_invalid` で止まって `validate_project` が `dependency_unreplanned` を報告する状態を残し、修正後はreplayせず `reconcile_required` で止まる（どちらも停止し、拒否理由とreplayの有無だけが変わる）。event logに開始前からの変更がある1 scenarioの10窓（terminal stage記録後・適用後と、中断が無くfinalize commitの記録前に `dirty_overlap` で止まった後の再実行）: baselineは未commitのまま `completed` を返したが、修正後は同じ `dirty_overlap` で止まりpendingを残す。一致した646行には、results commitの記録前後・push前後の窓（従来どおりexecutorを再実行して完了）、最終commitの記録後・commit後push前・push後complete前の窓（replayで完了）、complete後の再実行（terminal Workとして拒否）、single-workのcancel 2 scenarioの全窓（`spec_violation` / `structure_invalid` でpendingのまま）、Phaseのcancelでreplanの削除より前の窓（cancel event適用後・removal記録後。従来どおりSTART precheckの `structure_invalid` で止まる）、outer cancelのcommit記録後の窓、hold 2 scenarioの全窓が含まれる。(b) 成功を返した行の不変条件（pendingなし、runtime以外のdirtyなし、中断したWorkのterminal eventがcommit済み、HEADとremoteが一致、そのeventを持ち込んだcommitがそのWork自身のfinalization commit）: baselineは325行中dirty 24行・terminal event未commit 24行・別Workのcommitへの混入18行、修正後は315行中0行（holdの4行は下記の残る論点(2)でbaselineと同一）。(c) BL-029 / BL-030の調査harness（plan exclusion 5 scenario・START single-workのcancel 5 scenario × 同じ / 別内容 / 別operationのrequest × 12窓と、precheckを外したprobe）797行は、baselineと修正後で全行一致した。(d) Workline自身が作らないrecord / Git状態のprobe 7件: 未完了completionが2件、single-workのrecordを別Workへ付け替え、completion stageの後に別stage、予約IDの不一致、event logを手でcommit（pushなし / pushあり）の6件は、baselineではどれも成功を返し（dirtyのまま、別Workのcommitへの混入、または自分のfinalization commitなし）、修正後は何も実行せず `reconcile_required` でrecordを残す。Workの開始eventだけを手でcommitしcompletionが未適用の1件は、baselineは未commitのまま `completed`、修正後は最終commitとpushで完了する。regression test（`tests/test_terminal_finalization_resume.py`）は21 test / 13 subtestで、single-workのcompletionが最終commitの記録前後の5窓で残りのeffectだけを実行して同じmutationで完了すること（executorを再実行しない、eventとcommitが1回ずつ、そのeventを持ち込むのが自分の `complete` commit、HEADとremoteが一致、clean、pending 0、予約IDの追加なし）、再実行が完了したWorkとHEADを報告すること、その後のSTARTがevent logで止まらないこと、中断が無いfinalize前のSTOPの再実行を成功扱いにしないこと、outerで中断したWorkのfinalizationが次のWorkより先に行われること（次Workあり、2番目のWork、Phase complete、standaloneで次Workなし / あり、同等候補）、outerが未完了のcancel（次Workなし / あり × 記録後・適用後、entry自身、replanのremoval適用後）で何も実行・変更せずに止まること、single-workのcancelが従来どおり拒否されること、cancelのcommit記録後はreplayで進むこと、示せないrecord 5種（手でcommitしたevent log、別Workへ付け替えたsingle-workのrecord、後続stage、予約IDの不一致、未完了2件）で止まること、completion記録前の中断は従来どおりexecutorを再実行して完了することを固定する。修正前のコードではこのうち23件（subtest 8件を含む）が失敗する。規則の取り違えも固定されることを12のmutantで確認した: terminal event適用後もGit段階から継続せずそのまま `completed` を返す（12件失敗）、outerを従来どおり次Workの選択や `stopped` にする（14件）、commitせずに完了を返してmutationを閉じる（12件）、outerで最終commitをせずに次のWorkへ進む（7件）、cancelもcompletionと同じくGit段階から継続する（8件）、recordの判定をreplayの後に行う（4件）、HEADのevent logの検査を外す（1件）、最後のstageの条件を外す（1件）、single-workで別Workのcompletionも継続する（1件）、未完了が複数なら最後のものを継続する（1件）、予約IDの照合を外す（1件）、single-workのcancelも停止させる（3件、BL-028のKnownResidualTestsを含む）。full suiteは686 passed / 2 skipped / 540 subtests（baseline `a9937fa`）から707 passed / 2 skipped / 553 subtestsになり、増えたのはこのtestだけである（file別の結果もこのtest以外は同一）。`skills/start` のTerminal finalizationとouter continuationへ規則を追記した（`rules/git` のfinalization resumeと成功条件はregistryに既にあり、変更していない）。schema・event・relation type・project.yaml・`INTENT_VERSION`・recordの形式・Mutation Controller（公開methodを含む）・stage名・予約key・validatorは変更しておらず、backfillは不要。残る論点（今回は扱っておらず挙動も変えていない）: (1) cancelのresume（BL-030）: single-workのcancel再実行はcancel event記録後は従来どおり拒否されてpendingが残り、outerの再実行は修正後 `reconcile_required` で止まる。cancelのcommitが記録済みの窓でouterの再実行が次のWorkへ進み、結果が `cancelled` にならない点も同じ論点。(2) hold / branch移動（非terminal）の中断: outerでholdのevent適用後・commit記録前に中断すると、再実行は次のWorkを実行してholdのeventをそのcommitへ入れ（cleanで成功を返す）、single-workではheldのWorkを再開してexecutorを再実行する。terminal finalizationではないため対象外で、baselineと同一の挙動を実測した。(3) 最終commitが記録済みの窓でsingle-workの再実行が返す結果は、従来どおり完了したWorkの一覧が空でHEADを持たない。(4) event logに開始前からの変更があってfinalize commitが `dirty_overlap` で止まったmutationは、開始前の変更がrecordのnoteに固定されるため再実行も同じ理由で止まり続け、人による照合が要る（baselineでは再実行が未commitのまま成功を返していた）。(5) 既に未commitのまま閉じたmutationや、別Workのcommitに混入したeventは自動repairしない。

### BL-032 Recorded commit cannot resume after an independent commit advances HEAD

- ID: BL-032
- Title: Recorded commit cannot resume after an independent commit advances HEAD
- Status: RESOLVED
- Kind: implementation, recovery
- Problem: operationはcommitを作る前に、git_commit effectとしてcommit message・commitするpaths・記録時のHEAD（`base_head`）を記録する。resume時の `MutationController._classify_commit` は、base以降に同じmessageのcommitがある場合と、pathsにcommitする変更が残っていない場合を適用済み・期待値一致、HEADが `base_head` のままの場合を未適用とし、それ以外をすべて適用済み・期待値不一致とする。そのため、commit stageを記録した後・commitを作る前に中断した（pre-commit hookの拒否などcommit自体の失敗を含む）operationの間に、BL-017で独立と判定されるoperationや人・bootstrap backfillのcommitがHEADを進めると、commitは作られておらずpathsに誰も触れていないのに、同じrequestの再実行は毎回 `reconcile_required`（`effect N (git_commit) applied with unexpected result`）で止まる。実測（baseline `13804d6`、remoteありProject）: Phase Bのholdのcommitを実際のpre-commit hookが拒否し（`git_error`）、write scopeが独立なW1のRelated maintenanceがcommit・pushした後、holdの再実行は `append_event` 一致・`git_commit` 不一致で `reconcile_required` になり、2回目も同じで、recordはpendingのまま、`validate_project` は何も報告せず、event logを書くPhase Cのholdは `pending mutation ... overlaps the planned write scope: reconcile required` で止まる。9つのcommit stage（Phase hold、Related maintenance、Roadmap作成、Phase entry、Phase追加、direct CREATE、plan exclusion、STARTの成果commit・finalization commit）×中断窓（commit stage記録前 / 記録後・未作成 / 記録後に実commitが拒否 / commit後push前 / push後complete前）×割り込み（なし、BL-017で独立なWorkline operation、人のcommit（push有無）、同じmessageのcommit、recordのpathsのsweep、amend、remoteだけの前進、同じledgerの対照）の212 scenarioでは、止まるのはcommit stageを記録済み・未作成の2窓だけで、独立な割り込みの50行（STARTでは人のcommit）すべてで再現し、同じ割り込みでもcommit stage記録前とcommit後の窓は収束した。plan exclusionはその前にBL-029の前提条件で止まるので表に出ない。STARTの成果commit・finalization commitは、別に測ったbootstrap backfillの割り込みでも同じく止まる。`gitops.finalize` でcommitするすべてのowner（lifecycle、Roadmap作成・Phase追加・Phase entry、Related maintenance、direct CREATE、STARTのcommit、bootstrap backfill、push destination pin、Project開始）に共通する。
- Why it matters: `rules/git` の「commit失敗 / push失敗時はdomain writeを再実行せず、同じpending mutationのGit段階からresumeする」と、BL-017で独立なoperationが進んだ後に中断したoperationをresumeできることが、この窓だけ成立しない。残ったpending recordはwrite scopeが重なるoperation（全ledgerを宣言するSTARTを含む）を止め続け、`validate_project` に現れず、messageは作られていないcommitを「適用済み」と事実と逆に述べる。手動回復は、Worklineが拒んだcommitを人が検査なしに行うことになる。
- Likely scope: HEADが動いた時のgit_commitの判定（`base_head` の祖先性、記録したpathsへの変更の有無、branchの一致）、commit effectへのbranch identityの記録、branchを持たない既存recordの扱い、`rules/git` のcommit分類とSkillのresume記述。message一致だけで適用済みとする判定（BL-033候補）と、bootstrap backfill自身のresume（BL-034候補）とは混ぜない。
- Cross-project impact: あり（全ProjectのGit段階からのresume）
- Backfill likely: no（branchを記録していない既存recordは推測でresumeせず、従来どおり停止する）
- Human confirmation likely: yes（`rules/git` のcommit分類の境界を変える。条件と停止境界は採用時に決定済み）
- Self-hosting prerequisite: no
- Evidence class: code inspection
- Resolution: 原因は `_classify_commit` が、HEADが `base_head` から動いた理由を区別しなかったこと。effectはmessage・paths・`base_head` しか持たず、commitが作られていないままbranchが独立なcommitだけで伸びた状態と、履歴の書換え・branchの切替・recordのpathsへの他者の変更を見分けられなかった。修正: (1) `gitops.finalize_effects` はgit_commit effectに、記録時にHEADが指すbranchの完全なref名を `branch`（`refs/heads/<name>`、`gitcmd.current_branch_ref`）として加える。detached HEADでは入れない。`Effect.git_commit` の省略可能な引数で、`validate_effect` は `branch` がある場合に完全なbranch名であることを検査する。短縮名はtag等との曖昧さで変わり得るので使わない。(2) `_classify_commit` は既存の3つの判定（base以降の同じmessageのcommit → 一致、commitする変更が残っていない → 一致、HEAD == `base_head` → 未適用）をそのまま先に行い、従来なら不一致に落ちる直前だけ、module関数 `_head_advanced_independently` で次をすべて示せる場合に未適用とする: recordの `base_head` が完全なcommit ID（40 / 64桁の小文字16進。`HEAD~1` 等の式・短縮ID・大文字は示さない）で、`git merge-base --is-ancestor` がHEADの祖先と答える / `git rev-list --full-history <base_head>..HEAD -- <paths>` が空（base以降のどのcommitも、mergeの全parentを辿り、rename検出なしで、recordのpathsを変更していない）/ 現在HEADが指すbranchがrecordの `branch` と一致する。Gitが答えられない呼び出し（想定外の終了コード）は示せないものとして扱う。未適用とされたcommitは従来のapplyどおり `git add` と `git commit --only` で現在のHEADの上に作られ、その後のpushも従来どおり。(3) 条件2の読み方: 採用時の候補は「`base_head` とHEADの最終tree差分が空」（net）だった。netと採用した「途中のどのcommitもpathsを変更していない」は、pathsを一度変えて元に戻した履歴だけで違う。focused comparison（同じscenarioをbaseline・修正後・netの3通りで実行）: pathsへ別の内容をcommitしてから元に戻す（commit記録後・実commit拒否後）、recordの内容そのものを人がcommitしてからrevertする、side branchで変更して戻してからmergeする、の4 probeでnetは再開してcommit・pushし（revertの行ではrevertが作業treeから取り除いたeventを再適用し、人が履歴に残した取り消しの上にcommitする）、修正後とbaselineは `reconcile_required` で止まる。それ以外（残る8 probeと、212 scenarioのうちcommit記録後・未作成の窓の86 scenario）は修正後とnetで全行一致した。revertは人がそのpathsについて下した決定で、最終treeが同じでもその上に作るのは誤ったresumeになるため、途中のcommitを見る読み方を採用した。(4) branchを持たないrecord（修正前のimplementationが書いたrecordを含む）は、同じstageのgit_pushのbranch等から推測せず、HEADが進んでいれば従来どおり不一致。修正前のimplementationで中断したProjectのWorkline rootを修正後のimplementationへ入れ替えてから独立なcommitの後に再実行する実測（commit記録後・実commit拒否後の2窓）でも、`reconcile_required`、実行effectなし、record・HEAD・remote不変。逆（修正後が書いたrecordを修正前が読む）も従来どおり停止する。実測（A/B: baseline `13804d6` と修正後で同じscenarioを実行し、例外の型・code・ID正規化後のmessage、実行したeffect、記録したstage、HEAD / remoteの移動、履歴の形、pending recordとeffectの分類、dirty、`validate_project`、stateを比較）: 324 scenario（上の212、probe 31（branchの切替、ff pull、両方pending、構造の破壊、message衝突、各commitの `validate_project`、commit -a、indexごとのcommit、手動reconcile、部分sweep、他者の内容、touch-then-revert、sweep-then-revert、mergeしたside branch、baseでのbranch切替、両方未作成、同じledgerの対照、独立commitのamend、baseより前へのreset、detached HEAD）、bootstrap backfill 15、STARTとplan exclusion 66（single-work / outerの成果commit記録後、completion eventの記録後・適用後（BL-031の経路）、finalization commit記録後・実commit拒否後・commit後・push後、outerのcancel（event記録後、commit記録後・実commit拒否後・commit後）、Roadmap / STARTのplan exclusion × 割り込みなし・人のcommit・push済みの人のcommit））のうち、226 scenarioは全行一致、13 scenarioは結果・実行effect・HEAD・remote・dirty・`validate_project` が同じで、まだpendingのcommitの観測上の分類が不一致から未適用に変わっただけ（plan exclusionの11行はBL-029の前提条件、構造を壊した1行は `structure_invalid`、bootstrap backfillの1行はBL-034（indexに入ったbootstrapを見て `already_present` を返す）で、どれもcommitの分類より前に止まる）。残る85 scenarioはすべて、baselineが最初に異なるstepでgit_commitの `reconcile_required` で止まり、修正後はそのstepで記録済みcommitを1回だけ作った（それより前に再実行されたのは前のstageのpushだけ）もの。83行は同じmutationで収束した（pending 0、HEADとremoteが一致、clean、`validate_project` clean、記録したcommitは割り込んだcommitの直後に積まれ、後続の重なるoperationがある行ではそれも進む。両方pendingの行は両順序で収束し、途中の各commitも `validate_project` clean）。うち手動reconcileの2 probeでは、再実行で収束したので後続の手動手順（recordのpathsを人がcommit）が不要になり、その後の再実行は既にheldのPhaseとして従来どおり拒否される。bootstrap backfill自身がcommit記録後に中断し、その間に人のcommitまたは独立なholdが入った2行は、修正後はbackfillのcommitとpushを作った後、BL-034の `postcheck: backfill commit missing` で止まり、次の再実行は `already_present` を返してrecordがpendingに残る。これは割り込みの無い同じ窓のbaselineと同じ結果である。regression test（`tests/test_recorded_commit_resume.py`）は20 test / 50 subtestで、Phaseのholdのcommitが独立な各種commit（別scopeのRelated maintenance、同じRoadmapへのPhase追加、別Roadmapの作成、人のcommit（push有無）、bootstrap backfill）の後に作られること、各owner（Related maintenance、Phase追加、Roadmap作成、Phase entry、STARTの成果commit・finalization commit）のcommitがevent-onlyのhold、Related maintenance、人のcommit、bootstrap backfillの後に作られること（commit記録後。一部は実commit拒否後も。どれも中断時の内容のblobどおり、親は割り込んだcommit、変更はrecordのpathsだけ、同じmutation、前のstageのpush以外は再実行しない）、両方pendingが両順序で収束すること、remoteの前進をff pullした後に収束すること、STARTのcompletionがcompletion event適用後・finalization commit記録後・実commit拒否後のいずれでも割り込みの後にfinalizeされること、outer STARTのcancel commitが同じく作られること、recordのpathsへの別内容のcommit・一部だけのcommit（2窓）・他の変更と一緒のcommit・変更して戻した履歴・recordの内容のcommitとrevert・別branch・amendとreset・完全なcommit IDでないbase（7種）・branchを示さないrecord（修正前の形、null、空、短縮名、別branch、文字列でない）・Gitが答えない3つの問い（どのbranchか、祖先か、どのcommitがpathsを変えたか）がすべて `reconcile_required` で何も実行・変更しないこと、remoteだけの前進は従来どおりcommitの後pushで止まること、branchが完全なref名で記録されdetached HEADでは記録されないこと、完全なbranch名以外の記録を拒否すること、同じmessageのcommitは従来どおり一致と扱われること（BL-033、変更なし）を固定する。修正前のコードではこのうち8 test（subtest 29件を含む32件）が失敗し、25件はgit_commitの `reconcile_required`、6件はbranchの検査が無いこと、1件はbranchが記録されないことによる。規則の取り違えも8つのmutantで検出されることを確認した: HEADが動いたら再開（25件失敗）、祖先判定なし（4件）、paths判定なし（7件）、branch判定なし（10件）、branchの無いrecordを再開（6件）、まだcommitする変更が残るpathsだけを検査（部分sweepを再開、3件）、pathsがHEADと違うことだけを検査（他者の変更を再開、4件）、最終tree差分（net）で判定（3件）。full suiteは707 passed / 2 skipped / 553 subtests（baseline `13804d6`）から727 passed / 2 skipped / 603 subtestsになり、増えたのはこのtestだけである。`rules/git` のCommit / pushにcommit effectの記録内容と、HEADが進んだ時にresumeする条件・停止する場合を追記し、`skills/roadmap`・`skills/start`・`skills/create`・`skills/project-start` のresume記述から参照した。`INTENT_VERSION`、closeしたrecordのtop-level field、effect種別、stage名、予約key、`Mutation` / `MutationController` の公開method、event・relation・project.yamlは変更しておらず、backfillは不要。残る論点（今回は扱っておらず挙動も変えていない）: (1) base以降に同じmessageのcommitがあるだけで一致と扱う判定（BL-033候補）。(2) bootstrap backfillが自分の中断を収束できないこと（BL-034候補）。上の2行のとおり、commitの窓では割り込みの有無によらず同じ結果になった。(3) commit記録後に、HEADをbaseのまま別branchへ切り替えただけの場合は、既存の「HEAD == `base_head` → 未適用」でその別branchにcommitし、recordのpush（記録時のbranch同士のrefspec）がdry-runで `=` を返すので、元のbranchとremoteにcommitが無いまま成功を返す（baselineと修正後で同一の実測）。今回記録したbranchで判定できるが、既存の先行判定の意味を変えるので別に扱う。(4) resumeは前のstageで記録済みのpushも再生し、その時点のbranchをpushするので、人がまだpushしていないcommitも公開される（従来どおり。STARTのfinalization commitの前に成果commitのpushが再生される場合を含む）。(5) plan exclusionの再実行はcommitの分類より前にBL-029の前提条件で止まるため、この修正の効果はBL-029の着地後に再計測する。(6) 修正前に同じ理由で止まったままのrecord（branchを持たない）は自動でresumeしないので、人による照合が要る。

### BL-033 Recorded commit is taken as made when another commit has the same message

- ID: BL-033
- Title: Recorded commit is taken as made when another commit has the same message
- Status: RESOLVED
- Kind: implementation, recovery
- Severity: major
- Problem: operationはcommitを作る前に、git_commit effectとしてcommit message・commitするpaths・記録時のHEAD（`base_head`）・branchを記録する。resume時の `MutationController._classify_commit` は最初に `base_head..HEAD`（`base_head` が無ければHEADの全履歴）を読み、記録と同じmessage（前後の空白を除いた全文）のcommitが1件でもあれば、そのcommitの内容・変更したpaths・親・branchを見ずに適用済み・期待値一致とした。mergeで入ったside branchのcommitや、切り替えた別branch上のcommitも対象になる。そのため、commit stageを記録した後・commitを作る前に中断した（pre-commit hookの拒否などcommit自体の失敗を含む）operationの間に、人などが同じmessageで別の変更（無関係なpathだけ、記録したpathsの一部、別の内容）をcommitすると、同じrequestの再実行は自分のcommitを作らずにpushへ進み、成功を返してmutationをcompletedにする。実測（baseline `989a781`、remoteありProject、monkeypatchなし）: Phase Bのholdのcommitを実際のpre-commit hookが拒否し（`git_error`）、人が同じmessageで `notes.md` だけをcommitした後の再実行は、git_commitを `applied_matching` と分類して `phase_held` を返した。holdのeventはHEADにもremoteにも無くworking treeにだけ残り（staged）、pushは人のcommitだけを公開し、recordはcompleted、pending 0、`validate_project` は問題なし、remoteのfresh cloneではPhase Bはactive。同じholdの再実行は `spec_violation`（Phaseがheld）、event logを書くPhase Cのholdは `dirty_overlap` で止まって自分のeventを適用したpendingを残し、その後のPhase Bのresumeはそのpendingと重なって `reconcile_required` になる。single-work STARTでfinalization commitを同じように拒否すると、再実行は `completed` を返し、W1のlifecycle event 4件（started / target_added / target_removed / completed）がすべて未commitのまま残り、fresh cloneではW1はunstarted。outer STARTではW1のcompletion eventが次のWorkの `complete W-02` のcommitに混ざり、W1自身のfinalization commitは作られない。下の333 scenarioのmatrixでは偽成功は49行で、すべてcommit記録後・未作成と実commit拒否の2窓の、同じmessageのcommit（無関係なpathだけ、記録pathsの一部、全pathsを別内容、2件、branchの切替、side branchのmerge）の行だった。push-pin maintenanceとProject開始（まだcommitの無いrepositoryでは全履歴を検索する）はpostcheckが毎回止め、STARTのcancelは `run_work` の拒否で止まるがrecordにはcancel commitが作られたと記録される。BL-032の調査中に見つかった。
- Why it matters: 成功を返したoperationの成果がcommitにもremoteにも無い。`rules/git` の成功条件（remoteありProjectでは必要成果がremoteへ反映されるまで成功扱いにしない）と、`skills/start` のTerminal finalization（未commit / 未pushのterminal eventを残したまま閉じない、前のWorkのterminal eventを次のWorkのcommitへ混ぜない）に反する。recordはcompletedで閉じるので正式な回復経路も残らず、Worklineは検証していない人のcommitをoperationのpushとして公開する。未commitのcanonical fileはevent logを書く後続operationを `dirty_overlap` で止め、止まったoperationが自分のpendingを残して連鎖する。`validate_project` はworking treeの構造だけを見るので検出しない。BL-032のD2とBL-035のbranch判定はmessageの判定より後に行われるので、この誤りを変えない。
- Likely scope: `_classify_commit` のmessage一致の判定を、recordが適用済みとして持つcommitの分類し直しに限ること（採用時に決定した方向）。messageの判定そのものは消さない（同じmutationの後続stage、question waitや中断の後の分類し直しで、pathsが再びdirtyになった自分のcommitを認識する唯一の根拠）。BL-032のD2・BL-035のbranch判定との順序、commit直後・`applied` 保存前の中断で認識できなくなる場合とSTARTの成果commitの一部の場合の停止（採用時に拒否拡大として受け入れた）、`rules/git` のCommit / pushとSkillのresume記述、BL-032・BL-035のtestがこの残件として固定していた挙動。
- Cross-project impact: あり（全ProjectのGit段階からのresume）
- Backfill likely: no（既に偽成功で閉じたmutation、同じmessageのcommitで適用済みと記録されたcommitは自動repairしない）
- Human confirmation likely: yes（`rules/git` のcommit分類の境界を変える。修正方向と2つの拒否拡大は採用時に決定済み）
- Self-hosting prerequisite: no
- Evidence class: code inspection
- Resolution: 原因は `_classify_commit` の最初の判定（base以降に記録と同じmessageのcommitがあれば一致）が、effectの `applied` を見ずにすべてのgit_commitへ使われていたこと。`Mutation.apply` はresumeのたびに（STARTではstageを記録するたびにも）記録済みのeffectをすべて分類し直すので、この判定は、まだ作っていないcommit（記録後・未作成、commit自体の失敗、commit直後・`applied` 保存前の中断）と、同じmutationが既に作ったcommitの分類し直し（後続stage・question wait・中断でそのpathsが再びdirtyになったもの）の両方に使われていた。後者ではmessageが一致の唯一の根拠だが、前者では同じmessageのcommitはそのcommitであることを何も示さない。`classify` はその区別に要る `applied` をrecordから受け取っていながら `_classify_commit` へ渡していなかった。修正: `MutationController.classify` はgit_commitのrecordの `applied`（`True` そのものの時だけ真。Worklineが書かない値を適用済みへ昇格させない）を `_classify_commit` へ渡し、message判定はそれが真の時だけ行う。判定の順序（message一致 → commitする変更が残っていない → HEAD == baseかつ記録したbranch（BL-035）→ BL-032のD2 → 不一致）とmessage判定の範囲・比較は変えない。`applied` はcommitが成功した直後か、前回の分類でpathsがcommit済みと確かめられた後に保存されるので、真なら同じmutationがそのcommitを作ったか内容の成立を確かめている。適用済みと記録していないcommitは、同じmessageのcommitが履歴にあっても、記録したpathsにcommitする変更が残っていない時だけ一致になり、残っていればBL-035の条件（HEAD == baseで記録したbranchの上）かBL-032のD2を示せる時だけ作られ、示せなければ不一致で止まる。message判定そのものは消していない（消したmutantは、`applied` 保存後にpathsが変わった再実行、outer STARTのquestion waitからの再開、finalizationの再実行後にouter STARTが次のWorkへ進む時点で止まる）。実測（A/B: baseline `989a781` と修正後で、BL-033の調査のscenario matrixをmonkeypatchなしで同じように実行。6つのcommit stage（Phase hold、Roadmap作成、Phase entry、direct CREATE、STARTの成果commit・finalization commit）× 中断窓（commit stage記録前 / 記録後・未作成 / 実commitの拒否 / commit後・`applied` 保存前 / 保存後・push前 / push後・complete前）× 割り込み（なし、別messageの無関係commit、同じmessageの無関係commit・記録pathsの一部・全pathsを別内容（worktreeはAの内容 / 別内容）・Aの内容そのもの・無関係commit 2件・無関係→Aの内容、別messageでAの内容、base以前だけ・mergeしていない別branchだけに同じmessage、別branchへ切り替えて同じmessage、同じmessageのside branchのmerge、commit後に他人がpathsを編集・同じmessageでcommit・reword amend）の333 scenario × 5 step（中断、割り込み、再実行、2回目の再実行、scopeの重なる後続operation）。成否は、中断時にAのpathsが持っていた内容がどれかのcommitにあるか、未commitで残るかで判定）: baselineの偽成功49行（成功を返し、commit 0回、recordはcompleted、pending 0、`validate_project` 問題なし、Aの内容はどのcommitにも無い。pushは人のcommitだけを公開し、branch切替では何もpushしない）は修正後0行になり、差分は64行ですべて次のどれかだった。(1) 同じmessageの無関係commit（1件・2件）とmergeの25行（STARTの成果commit以外）: BL-032のD2で人のcommitの上にAのcommitを作り、pushして収束する（同じ窓の別messageの無関係commitの行と全項目一致）。(2) pathsの一部・別内容・branch切替の24行: 何も実行せず `reconcile_required`、recordはpending。(3) STARTの成果commitで同じmessageの無関係commit・2件・mergeの5行: 収束のまま、偽の一致の後にexecutorを再実行して `results:1` を作る経路から、D2で `results:0` を作る経路へ変わる。(4) STARTの成果commitでpathsの一部・別内容・branch切替の6行: 収束から `reconcile_required`（採用時に受け入れた拒否拡大。偽の一致の後のexecutorの再実行がたまたま残りをcommitしていたもので、別messageで同じ操作は修正前から停止する）。(5) commit後・`applied` 保存前に中断し、再実行までに他人がpathsを編集した4行（Phase hold、Phase entry、STARTの成果commit・finalization commit）: 収束から `reconcile_required`（採用時に受け入れた拒否拡大。commitは存在するが、recordにはそのmutationのものと示すものが無い）。commit stage記録前・`applied` 保存後の窓、別messageの割り込み、Aの内容そのもののcommit、base以前・mergeしていない別branchだけのmessage、reword amendは全行一致した。matrixの外: 再分類のprobe（中断もquestionも無いouter START、deriveの後に完了するsingle-work、question waitからの再開、成果fileを次のWorkが変えた後のquestion、derive → holdの中断、次のWorkのlifecycle後の中断、Gitがmessageを正規化する成果message）は全行一致。outer STARTのfinalization commitを拒否して同じmessageのcommitを挟むと、baselineはW1のcompletion eventを `complete W-02` のcommitに混ぜて `phase_complete`、修正後はW1自身の `complete W-01` がcommitしてから進む。push-pin maintenanceは同じmessageのcommitの後もpostcheckで止まり続けていたのがD2で収束し、Project開始（まだcommitの無いrepository）はpostcheckの停止から分類での `reconcile_required` になり、STARTのcancelは停止のままcancel commitが実際に作られpushされるようになった。STARTのhold、plan exclusion 2種は同じ。内容を書き換えるpre-commit hookは、commit後・`applied` 保存前の中断だけが収束から停止に変わり（(5)と同じ理由）、中断なし・中断中の人の編集では同じ。regression test（`tests/test_recorded_commit_message.py`）は12 test / 28 subtestで、同じmessageの無関係commitの後の再実行がPhase hold（2窓）・Related maintenance・Roadmap作成・direct CREATE・STARTのfinalization commitで自分のcommitをその上に1回だけ作って収束すること（変更は記録pathsだけ、内容は中断時のもの、HEADとremoteが一致、pending 0、clean、`validate_project` 問題なし）、同じmessageのcommit 2件・同じmessageのside branchのmergeでも同じこと、mergeしていない別branchの同じmessageでは記録したbaseの上に作ること、同じmessageでpathsの一部（Phase entry、2窓）・別の内容（Phase hold、2窓）をcommitした後は何も実行・変更せずに止まること、Aの内容そのもののcommitは同じmessageでも別messageでもpushだけで収束すること、commit後・`applied` 保存前の中断はそのままでも同じmessageのcommitの後でも収束し、他人がpathsを変えた後は止まること、`applied` 保存後（push前に他人がpathsを変えた場合、push後・complete前に同じmessageのcommitがある場合、outer STARTのquestion waitからの再開）はmessageで自分のcommitと認識して収束すること、Phase holdで同じmessageのcommitを6窓それぞれに挟んだ結果、STARTのfinalization（single-work / outer）がW1自身のfinalization commitでcompletion eventをcommitすること、STARTの成果commitを同じmessageで一部だけcommitした後は止まることを固定する。BL-032の `tests/test_recorded_commit_resume.py` とBL-035の `tests/test_recorded_commit_branch.py` がこの残件として固定していた2 testは、同じmessageの無関係commitの後にcommitを作って収束すること、別branchの同じmessageのcommitでは止まり記録したbranchへ戻れば進むことへ改めた。修正前のコードではこのうち21件（subtest 17件を含む）が失敗する。規則の取り違えも8つのmutantで検出されることを確認した: `applied` を見ずにmessageで一致（修正前の規則、21件失敗）、`applied` が偽でもmessageが完全に同じなら一致（21件）、同じmessageのcommitが記録pathsのどれかを変えていれば一致（7件）、記録したbranchの上にいれば一致（20件）、同じmessageのcommitの親が記録したbaseなら一致（commit直後・保存前の形、21件）、BL-032のD2の直前で `applied` を見ずにmessage判定（21件）、記録したbranchの上にいなければmessageで一致（BL-035の停止をmessageで迂回、1件）、message判定の削除（3件）。full suiteは739 passed / 2 skipped / 632 subtests（baseline `989a781`、BL-035の計測）から751 passed / 2 skipped / 660 subtestsになり、増えたのはこのtestだけである（BL-032・BL-035のtest fileで改めた2 testは同数）。 `rules/git` のCommit / pushに、commit messageの一致だけでは記録したcommitの適用を示さないこと、同じmessageのcommitを一致の根拠にするのはrecordが適用済みとして持つcommitだけであること、適用済みと記録していないcommitの判断、commit直後・`applied` 保存前の中断でpathsがdirtyになった場合の停止を追記し、BL-032の段落の「base以降にそのcommitが無く」を「上記の一致に当たらず」に改め、`skills/roadmap`・`skills/start`・`skills/create`・`skills/project-start` のresume記述から参照した。`INTENT_VERSION`、closeしたrecordのtop-level field、git_commitのpayload、effect種別、stage名、予約key、`Mutation` / `MutationController` の公開method、BL-032のD2、BL-035のbranch判定、message判定の範囲と比較は変更しておらず、backfillは不要。残る論点（今回は扱っておらず挙動も変えていない）: (1) Gitがcommit時にmessageを正規化する（行末空白・連続空行・CRLF）と、同じmutationが作ったcommitも後の分類し直しでmessageが見つからず、pathsが再びdirtyなら `reconcile_required` になる（修正前と同じ。commitのidentityを変える別の論点）。(2) 修正前に同じmessageの偽の一致で `applied: true` と記録されたrecord（completedで閉じたもの、STARTのcancelのようにpendingで残るもの）は区別できず、自動repairしない。(3) plan exclusion（Roadmap / START standalone）はBL-029の前提条件で分類の前に止まるので未計測。BL-029着地後に再計測する。(4) 内容を書き換えるpre-commit hookはcommit後もpathsをdirtyに残す（修正前と同じ）ので、commit直後・`applied` 保存前の中断と重なると停止する。

### BL-034 Bootstrap backfill cannot converge its own interrupted pending mutation

- ID: BL-034
- Title: Bootstrap backfill cannot converge its own interrupted pending mutation
- Status: RESOLVED
- Kind: implementation, recovery
- Severity: major
- Problem: bootstrap backfillは、bootstrapが一致しtrackedであること（worktreeの内容と `git ls-files`）だけを見て `already_present` を返す判定を、自分の未完了mutationを探す `MutationController.open` より前に行っていた。bootstrapは固定textなので、中断したbackfill自身が置いたbootstrapと、既にあったbootstrapを区別しない。backfillのgit_commit effectは `git add` の後に `git commit --only` を行うので、commitを記録した後の中断はすべてこの判定に当たる。実測（investigation `72514f1`、`9178e97` でも同じ。remoteありProject、Worklineは無patch、実際のGit hookによる拒否・processのkill）: pre-commit hookがbackfill commitを拒否した後（`git_error`）の再実行は毎回 `already_present` を返し、bootstrapはindexにだけ残ってHEADにもremoteにもfresh cloneにも無く、recordはpendingのまま残った。commit後・push前（remoteの拒否、pushの中断）は `already_present` でremoteに無いまま、push後・complete前は `already_present` でrecordだけが残った。commitを記録した後・`git add` 前の中断では、再実行はresumeしてcommitとpushを作った後、replayの後に読んだ `head_before` と比べて `postcheck: backfill commit missing` で止まり（自分のcommitを「無い」と判定）、次の再実行から `already_present` になった。どの場合もrecordは永久にpendingで、push-destination pin maintenanceは `pending_operation` で止まり続け、`validate_project` は問題を報告しない。決定前・bootstrap stageの記録・適用の中断（commitの記録前）は収束していた。導入時（`9089838`）からの順序で、同じ形でsuccessを返すownerは他に無い（pin maintenanceとProject開始は自分のpendingを先に見ており、同じ中断で収束することを実測）。
- Why it matters: 成功を返したbackfillの成果がcommitにもremoteにも無い（`skills/project-start` の「remoteがあればcommit後pushまで」「再実行: idempotent」、`rules/git` のentryでのpending照合とGit段階からのresumeに反する）。recordはbackfill自身が二度と読まないので正式な回復経路が無く、pin maintenance（remoteを移した時に承認先を変える唯一の経路）が止まり続ける。一方で、commitを作ったと記録していないのに一致するbootstrapがHEADにある状態は、自分のcommitか他者の同一内容のcommitかをbootstrapの内容からは区別できないので、単純に「resumeして完了」にすると他者のcommitを自分のcommitとしてpushする。
- Likely scope: `bootstrap._backfill_locked` のfast pathと未完了recordの確認の順序、resume時のbackfill commitの確認（postcheck）、recordがbackfillのものと示せない場合（複数、別の起動、別の形、別のbootstrap text、branchを持たない未作成commit）の扱い、他者が作った同一内容のbootstrapの扱い、`skills/project-start` のbackfill記述。Mutation Controllerのcommit分類（BL-032 / BL-033 / BL-035）とBL-036のbranch bindingは変えない。
- Cross-project impact: あり（bootstrap導入前に初期化した全Projectのbackfill）
- Backfill likely: no（既に永久pendingで残っているbackfill recordは、修正後の再実行が同じmutationとして継続するか、自分のものと示せなければrecordを変えずに停止する）
- Human confirmation likely: yes（他者の同一内容のcommitを自分のcommitとして扱わない方針、effectの無いrecordを閉じる条件、content identity、owner単位の探索は採用時に決定済み）
- Evidence class: code inspection
- Resolution: 原因は2つで、どちらもbootstrap backfill ownerの中にあった（Mutation Controllerのcommit分類とBL-036のbranch bindingは原因ではない）。(R1) `_backfill_locked` が `already_present` の判定を、自分の未完了mutationを探す `MutationController.open` より前に、bootstrapの現在状態だけで行っていた。(R2) resume時、記録済みのcommit / pushを `mutation.apply()` がreplayした後に `head_before` を読み、postcheckが「このrunでHEADが動いたか」でbackfill commitの有無を判定していたため、replayが作った自分のcommitも既にあった自分のcommitも「無い」と判定した。R1だけを直すと、commit記録後の偽成功がすべてこの `postcheck: backfill commit missing` の停止に変わる（調査時の候補Aの実測）。修正（`bootstrap.py` のみ。`MutationController` / `Mutation` の公開method、`EFFECT_KINDS`、`INTENT_VERSION`、recordのfield、stage名、commit message、invocationの形、BL-032 / BL-033 / BL-035の分類、BL-036のbindingは変えていない）: (1) prechecks（not_a_project / registry）の直後、bootstrapの状態を見る前に、このProjectのbackfillのpending recordをownerで探す（`_unfinished_backfill`）。exact invocationではなくownerで探すので、Projectの移動等で別の起動として記録されたrecordも見逃さない。無ければ従来どおり（`already_present` のfast pathを含め不変）。2件以上、invocationが今回と違う、backfillが書く形でない（field・write scope・予約ID・note・stageの並び・applied順・payload）、決定したbootstrapがこのimplementationのtextと違う（content identity: 記録済みwrite_fileのcontentと `render_bootstrap()` を比較し、古いpendingを新しいtemplateで完了しない）、branchを持たない未作成のcommitを記録している、のいずれかなら、何も書かずに `reconcile_required`。(2) 同じ起動のrecordが1件なら、fast pathを使わずresumeする。ただしreplayの前に、HEADが持つbootstrapがこのbackfillのcommitだと示せるかを確認する（`_refuse_unowned_bootstrap`）: recordがcommitを未記録・未作成なのにHEADが既にbootstrapを持つなら、自分のcommit（commit直後・applied保存前の中断）か他者の同一内容のcommitかをbootstrapの固定textからは区別できないので、自分のcommitとして継続・pushせず `reconcile_required`。recordがcommitを作ったと記録している場合は、記録したbase以降にbootstrapを変更したcommitがちょうど1件で、記録したmessageを持ちbootstrapだけを変更していることを要求し、違えば `reconcile_required`。(3) 何も決定していない（effectを記録していない）recordで、bootstrapがHEADと一致してcommit済み（index・worktreeに変更なし）なら、そのrecordを `Mutation.abandon()` で閉じて `already_present` を返す。commit / pushは行わず、他者のcommitは他者のまま残す。push destinationやdetached HEADの検査より前に行う。stageだけのbootstrapならresumeして自分のcommitとしてcommit・pushする。(4) postcheckのbackfill commitの確認を、記録したgit_commitの `base_head` 以降の履歴（`commits_touching`）でちょうど1件・bootstrapだけを変更、に変えた。新規runでcommitが作られなければ従来どおり `postcheck: backfill commit missing`。(5) resumeしたcommit / pushは従来どおりMutation Controllerが分類・適用するので、BL-032（独立なcommitの上に作る）、BL-033（未作成commitに同じmessageのcommitを使わない）、BL-035（記録したbranch）、BL-036（commit記録前の決定のbranch）の検査がこの経路で効く。実測（Worklineは無patch、実際のpre-commit hookの拒否、remoteのpre-receiveによるpush拒否、post-commit / pre-push / remote post-receive / `core.fsmonitor` hookによるprocessのkill）: 同じ47 scenarioをbaseline `9178e97` と修正後で実行し、変化した38行はすべて次のどれかだった。収束20行（偽成功15、停止5）: commitを記録した後・`git add` 前、pre-commit hookの拒否（remoteあり / なし）とhook内のkill、pushの拒否とpush前のkill、push後・applied保存前、postcheck前、complete前の各中断、commitを作ったと記録したbranch無しの旧形record（push後）、別branchのpushが残るだけの行、独立なcommitや同じmessageの無関係なcommitの後、人がbootstrapを消した・unstageした後、何も決定していないrecordの後に人がcommit + pushした・stageした・別cloneのbackfillをpullした行。いずれも同じmutationで完了し（commitを記録した後の窓は再実行1回、recordはcompleted、pending 0）、backfill commitは1件、HEADとremoteが一致、pin maintenanceは `already_pinned`。reconcile_required 15行（record・HEAD・remoteは不変）: commit後・applied保存前の中断（remoteあり / なし、別branch、移動したProjectのrecord）、決定後に人が同じbootstrapをcommit、同じmessageでcommit、別cloneのbackfillをpull、pre-commit hookの拒否の後に別branchへ切替（BL-035）、recordの複製・branch無しの旧形・content改変・別の起動、canonical textが変わった後（2行）。何も決定していないrecordを閉じた2行: 人がcommitしてpushしていない（pushしない）、detached HEAD（停止せず閉じる）。停止が増えた1行: runtimeのmutation領域に読めないfileがあると、自分の未完了recordを探す読み込みが `reconcile_required` になる（従来は読まずに `already_present`。pending recordを読むpin maintenance / Project開始と同じ扱い）。拒否されていた入力の成功化は0行。変化しなかった9行: 未完了recordの無いProject（commit + push済み、commit済み・未push、stageだけ、bootstrap無し）、決定前・bootstrap stageの記録・適用の中断、決定後に別branchへ切替（BL-036の停止）、完了後の再実行。regression test（`tests/test_bootstrap_backfill_resume.py`、9 test / 27 subtest）: commitを記録した後の各窓（commit記録後、hookの拒否、pushの拒否、push後、complete前。remoteなしを含む）で同じmutationのまま完了し、残りのeffectだけを実行し（commit / pushの重複なし）、backfill commit 1件、remote一致、clean、`validate_project` clean、pin maintenanceが通り、次の再実行は `already_present` になること。決定前・bootstrap stageの記録・適用の中断は従来どおり収束すること。未完了recordの無いProjectのfast pathは不変（mutationを開かない、pushしない）。何も決定していないrecordは、人がcommitしたbootstrapの上ではpushせずに閉じること。commit後・applied保存前の中断、stageされたbootstrapを人がcommit、同じmessageでcommit、別cloneのbackfillをpullした後は、何も実行・変更せず `reconcile_required`。recordの複製・別の起動・branch無しの旧形・bootstrap改変・余分なeffect・別のcanonical textは何も実行・変更せず `reconcile_required`。独立なcommit（BL-032）と同じmessageの無関係なcommit（BL-033）の後はその上にbackfill commitを作ること。hookの拒否（BL-035）とbootstrap適用後（BL-036）に別branchへ切り替えると止まり、元のbranchに戻ると同じmutationで完了すること。修正前のコードではこのうち22件（subtestを含む）が失敗する。規則の取り違えも4つのmutantで検出されることを確認した: fast pathを未完了recordの確認より前に戻す（16件失敗）、backfill commitをreplay後のHEADの移動で判定する（9件）、recordが未作成のcommitでもHEADのbootstrapを自分のものとして継続する（4件）、未完了recordをexact invocationだけで探す（1件）。直接関係するtest（`tests/test_bootstrap.py` 33、`tests/test_push_destination.py` 45、`tests/test_decision_branch_binding.py` の全ownerのbranch test 13 subtest、`tests/test_recorded_commit_resume.py` 20 / 50 subtest）はすべてpassした。`skills/project-start` のbackfillに、再実行が未完了recordを先に確認すること、継続・閉じる・停止の条件を追記した。backfillは不要（既に永久pendingで残っているbackfill recordは修正後の再実行で継続するか、recordを変えずに停止する）。残る論点（今回は扱っておらず挙動も変えていない）: (1) commit直後・applied保存前の中断は、commitが自分のものかをrecordが示せないので `reconcile_required` になる（commitのidentityをrecordへ残す別の設計が要る）。(2) 未完了recordの無いProjectで、人がstageしただけのbootstrapやcommitしてpushしていないbootstrapは、従来どおり `already_present` でcommit / pushしない。(3) Git自体が答えられない場合（`git ls-tree` / `rev-list` の失敗）はbootstrapを自分のものと示せないものとして停止する。

### BL-035 Recorded commit can resume on the wrong branch when HEAD still equals base

- ID: BL-035
- Title: Recorded commit can resume on the wrong branch when HEAD still equals base
- Status: RESOLVED
- Kind: implementation, recovery
- Problem: BL-032以降、git_commit effectは記録時にHEADが指していたbranchの完全なref名（`branch: refs/heads/<name>`）を持つが、`MutationController._classify_commit` の先行判定「HEADが `base_head` のまま → 未適用」はそのbranchを見ない。commit stageを記録した後・commitを作る前に中断した（pre-commit hookの拒否などcommit自体の失敗を含む）operationの間に、人が同じcommitを指す別branchをcheckoutする、またはbranch名を変えると、HEADはbaseのままなので、同じrequestの再実行はその別branchにcommitを作る。recordのpushは記録時のbranch同士のrefspec（例 `main:main`）で、そのbranchとremoteは元から一致しているのでdry-runが `=` を返して適用済みとなり、operationは成功を返してmutationをcompletedにする。実測（baseline `f3a3331`、remoteありProject、monkeypatchなし）: main上でPhase Bのholdのcommitを実際のpre-commit hookが拒否し（`git_error`）、人が `git checkout -b side` した後の再実行は `phase_held` を返した。holdのcommitはsideにだけあり、local mainとremote mainはbaseのまま、`git push --dry-run origin main:main` は `=`、recordはcompleted、pending 0、clean、`validate_project` は問題なし。後続のPhase Cのholdはside上でcommitしてremoteに `side` を作り、mainへ戻るとPhase Bはunstartedで、同じholdの再実行は新しいmutationとして別のholdをもう一度commitする。hookがWorklineのprocess自体をcommit中に終了させた中断でも同一。下のmatrixでは、Phase hold、Related maintenance、direct CREATE、STARTのfinalization commit（single-workは `completed`、outerは残りのWorkとintegrationまでside上で実行して `phase_complete`。terminal eventを含むfinalization commitがsideにだけ作られる）、STARTの成果commit、Project開始（初期commitを別branchやdetached HEADの上に作る。pushしないので結果は `initialized`）の、commit記録後・未作成と実commit拒否後の窓で再現した。sideで記録してmainへ切り替えた場合も同じで、`side:side` のpreviewが `=` になる。branch名の変更や記録したbranchの削除では、別branchにcommitを作ってからpushのpreviewが失敗する（`git_error`）。BL-032のprobeで見つかった残る論点(3)。
- Why it matters: 成功を返したoperationの成果が、記録したbranchにもremoteにも無い。`rules/git` の成功条件（remoteありProjectでは必要成果がremoteへ反映されるまで成功扱いにしない）に反し、recordはcompletedで閉じるので回復経路も残らない。STARTでは、BL-031が同じmutationのGit段階で確定させるterminal eventのfinalization commitが別branchにだけ作られる。`validate_project` はworking treeの構造だけを見るので検出せず、記録したbranchへ戻った人にはoperationが無かったように見える。
- Likely scope: `_classify_commit` のHEAD == `base_head` 判定にrecordのbranchと現在のbranchの一致を加えること、branchを持たないrecord（detached HEADで記録したcommit、BL-032より前のimplementationのrecord）の扱い、`rules/git` のCommit / pushとSkillのresume記述。BL-032のD2（HEADが進んだ場合の判定）、message一致の判定（BL-033候補）、schema・`INTENT_VERSION`・effect種別は変えない。
- Cross-project impact: あり（全ProjectのGit段階からのresume）
- Backfill likely: no（branchを持たない既存recordは推測でresumeせず、branchの上では停止する）
- Human confirmation likely: yes（`rules/git` のcommit分類の境界を変える。修正方向は採用時に決定済みで、branchを持たないrecordの扱いは安全側の原則のもとで実測から決めた）
- Self-hosting prerequisite: no
- Evidence class: code inspection
- Resolution: 原因は `_classify_commit` の「HEAD == `base_head` → 未適用」が、BL-032で記録されるようになったbranchを見ていなかったこと。HEADがbaseのままであることは、その上にcommitが作られていないことしか示さず、同じcommitを指す別branchの上にいる状態と区別しない。修正: (1) `_classify_commit` の判定順序（base以降の同じmessageのcommit → 一致、commitする変更が残っていない → 一致、HEAD == base → 未適用、BL-032のD2 → 未適用、それ以外 → 不一致）は変えず、HEAD == baseの判定にだけmodule関数 `_on_recorded_branch` の条件を加えた。recordが `branch` を持つなら、`gitcmd.current_branch_ref` が返す現在のbranchの完全なref名と文字列で一致する時だけ真（短縮名、null、空、文字列でない値、tagのref、存在しないbranch、detached HEAD、Gitが答えない場合は偽）。recordが `branch` を持たないなら、新設の `gitcmd.head_detached`（`git symbolic-ref --quiet HEAD` の終了コード1だけをdetached、0をbranchの上（まだcommitの無いbranchを含む）、それ以外を判定不能とする）がdetachedと答える時だけ真。偽ならD2（`_head_advanced_independently`、変更なし）へ進み、HEADがbaseのままでbranchが違えばそこでも不一致になる。未適用とされたcommitとその後のpushは従来どおり。(2) branchを持たないrecordの扱い: 修正前は、HEAD == baseならどのbranchでも（detachedでも）未適用、HEADが進めば停止だった。branchを持たないrecordを常に停止とする案は、detached HEADの上で行うProject開始（修正前も `initialized`、初期commitはdetached HEADの上に作られ、そのcommit effectはbranchを持たない）が、中断が無くても自分のcommitを最初に分類する時点で `reconcile_required` になり、書き込んだ `.workline` を残して止まるので採らなかった。detached HEADで記録したcommitはdetached HEADの上でだけ作る。BL-032より前のimplementationのrecordとは区別できないので同じ扱いになるが、どのbranchで記録したかは推測しない: HEAD == baseでもbranchの上にいれば、記録時と同じbranchであっても停止する（下記の拒否拡大）。detached HEADの上では、branchを必要とするoperationが従来どおり入口の `detached_head` で先に止まり、分類まで届くのはProject開始だけで、その結果（detached HEADの上に初期commitを作る）は修正前と同じ。(3) 既存testへの影響: `tests/test_mutation.py::MutationControllerTests::test_git_commit_effect_is_idempotent_on_resume` は、branchを渡さない `Effect.git_commit` をmain上で記録・適用しており、最初のapplyが `reconcile_required` になった（(2)の拒否拡大がそのまま現れたもの）。productionのcommit effectは `gitops.finalize_effects` がbranchを入れるので、testのeffectも同じくbranchを記録する形に直した。他の既存testの結果は変わらない。実測（A/B: baseline `f3a3331` と修正後で同じscenarioを実行し、例外の型・code・ID正規化後のmessage、実行したeffect、記録したstage、push dry-runの結果、HEAD / remote mainの移動、recordのbytes、各branchとremoteの履歴、recordしたmessageのcommitがどのrefにあるか、pending recordとeffectの分類、dirty、`validate_project`、stateを比較）: (a) matrix 145 scenario（Phase hold・Related maintenance・direct CREATE・START single-work / outerのfinalization commit × commit記録後・実commit拒否後 × HEADをbaseに残す（そのまま、別branchをcheckout、別branchへ行って戻る、detached）・HEADを進める（同じbranch、別branch）・branchを持たない形へrecordを書き換え（そのbranch、別branch）の80、Phase holdの追加probe 30（branch名の変更、branchの短縮名と同名のtag、baseのamend、detachedでのcommit、remoteだけの前進、recordのpathsへの別内容のcommit、変更して戻した履歴、同じmessageのcommit（main上、別branch上）、branchを持たないrecordでdetached / HEAD前進、recordのbranchを短縮名・null・存在しないbranchに書き換え、baseを短縮ID）、STARTの成果commit 3、sideで記録したholdとSTART（そのまま、mainへ切替、mainへ行って戻る、mainへ切り替えてsideを削除）12、BL-031のcompletion適用後の窓（そのまま、別branch）4、Project開始（既存repository × 2窓 × そのまま・別branch・行って戻る・detached、branchを持たない形でmain上 / detached、まだcommitの無いmainでそのまま・別branch、detached HEADで記録（中断なし、そのまま、mainへ切替、mainへ行ってdetachedへ戻る））16）のうち、82 scenarioは全行一致した（HEADがbaseのまま記録したbranchにいる行と、行って戻った行は両方とも収束。HEADが進んだ行はBL-032どおり（同じbranchで収束、別branch・amend・paths変更・変更して戻した履歴・branchを持たないrecordは停止、remoteだけの前進はcommitの後pushで停止）。同じmessageのcommitはmain上でも別branch上でも両方で `phase_held`（BL-033）。detachedでcommitしたHEAD・branchを持たないrecordのdetachedは両方 `detached_head`。同名tagは両方でcommitの後push previewが曖昧なrefspecで `git_error`。sideで記録したSTARTは成果commitがside上にあるのでmainへ切り替えるとHEADがbaseでなく、両方停止する（sideを削除した行は成果commitのpush previewで両方 `git_error`）。completion適用後の窓は下記の残る論点(2)で両方同じ。Project開始のdetached HEADでの記録は、中断なし・detachedのままの再実行・mainへ行ってdetachedへ戻る再実行のいずれも両方 `initialized` で、branchを持たない形のProject開始のrecordをdetached HEADで再実行した行も両方 `initialized`）。差分は63 scenarioで、すべて次のいずれかだった。(i) 32 scenario: baselineは再実行で記録したbranch以外にcommitを作り、28行は成功（`phase_held` / Related maintenance / CREATEの結果 / `completed` / `phase_complete` / `initialized`。recordのpushはdry-runで `=`、outer STARTは残りのWorkもside上で実行、Project開始はside・detached HEAD・まだcommitの無いside）、4行（branch名の変更、記録したbranchの削除）はcommitを作った後push previewで `git_error`。修正後はすべて再実行が `reconcile_required`（`effect N (git_commit) applied with unexpected result`）で止まり、実行したeffectなし、HEAD・remote・branchの移動なし、recordのbytes不変（direct CREATEだけは従来から再実行ごとに同じwrite scopeを保存し直すので `updated_at` だけが変わる。baselineの停止行でも同じ）、どのrefにもrecordしたmessageのcommitが無く、後続の重なるoperationはpending recordで止まる。記録したbranchへ戻った再実行が同じmutationで収束することはregression testで確認した。(ii) 11 scenario: branchを持たない形のrecordでHEADが記録時のbranchにいる行（5 owner × 2窓とProject開始）。baselineは記録どおりcommit・pushして成功、修正後は同じ条件で停止する。(2)の意図した拒否拡大。(iii) 6 scenario: recordのbranchを短縮名・null・存在しないbranchに書き換えた行。baselineはmain上でcommit・pushして成功、修正後は停止する。Workline自身はこのrecordを書かない。(iv) 1 scenario: detached HEADで記録したProject開始を、同じcommitのmainへ切り替えてから再実行した行。baselineはmain上に初期commitを作って `initialized`、修正後は停止する（(2)の拒否拡大。detachedへ戻れば進む）。(v) 13 scenario: 挙動は両方同じ（11行はdetached HEADで両方入口の `detached_head`、2行はbaseの短縮IDで両方 `reconcile_required`）で、差は観測したcommitの分類（未適用 → 不一致）とharnessが書いた短縮IDだけ。拒否が成功へ変わった行は無い。(b) BL-032のharnessの再実行: 9つのcommit stageのcommit記録後・実commit拒否後の窓の86 scenario（独立なoperation・人のcommit・sweep・amend・remoteだけの前進などの割り込み）とSTART / BL-031 / plan exclusionの66 scenarioは全行一致し、probe 12件のうち差分はbaseでのbranch切替（P16）のcommit記録後・実commit拒否後の2行だけで、baselineの `phase_held`（sideにだけcommit）が修正後は `reconcile_required` になった。(c) BL-033の調査harnessのcommit記録後・実commit拒否後の156 scenario（6 owner × 2窓 × 人のcommitなし・別message・同じmessage（無関係、pathsの一部、別内容、同内容、2回、別branch、branch切替、merge）と、baseより前の同じmessage）は1230行すべて一致し、同じmessageによる偽成功49行はBL-033の調査（`13804d6`）と同じscenarioのまま変わらない。(d) implementationを入れ替えた計測（BL-032より前の `13804d6` がcommit記録後・実commit拒否後に残したrecordを、同じWorkline rootで修正後のimplementationが再実行、HEADはbaseのまま）: main上ではbaselineは成功、修正後は `reconcile_required`（実行effectなし、record・HEAD・remote不変）、別branchではbaselineはsideにだけcommitして成功、修正後は停止、detachedでは両方 `detached_head`。修正後が書いたrecordは、そのまま再実行すれば収束し、別branchでは停止する。regression test（`tests/test_recorded_commit_branch.py`）は12 test / 29 subtestで、同じcommitの別branchで再実行するとPhase hold（2窓）・Related maintenance・direct CREATE・STARTのfinalization commitが何も実行・変更せずに止まり、どのrefにもcommitが無く、記録したbranchへ戻れば同じmutationでcommit・pushして収束すること、branch名の変更、sideで記録したcommitのmainでの再実行と記録したbranchの削除、Project開始のdetached HEADでの再実行が止まり、Roadmap operationは従来どおり入口の `detached_head` で止まること、記録したbranchのまま・別branchへ行って戻った再実行・side上で記録したcommitのside上での再実行・まだcommitの無いbranchでの再実行は進み、まだcommitの無いbranchを切り替えると止まること、recordのbranchが短縮名・null・空・文字列でない・存在しないbranch・同名tagのrefなら止まること、branchを持たないrecordはbranchの上（同じbranch、別branch）では止まり、detached HEADで記録したProject開始は中断なしでも2窓の中断後の再実行でもdetached HEADの上に初期commitを作り、mainへ切り替えると止まってdetachedへ戻れば進むこと、Gitが現在のbranchに答えなければbranchを持つrecordもbranchを持たないrecordも止まること、STARTのfinalization（single-work / outer）がterminal eventを別branchへcommitせずに止まり、mainへ戻れば完了すること、同じmessageのcommitが別branchにあれば従来どおり一致と扱われること（BL-033、変更なし）を固定する。修正前のコードではこのうち23件（subtest 22件を含む）が失敗する。規則の取り違えも8つのmutantで検出されることを確認した: HEAD == baseならbranchを見ない（修正前の規則。23件失敗）、recordのbranchを見ずbranchの上にいれば進む（24件）、短縮名で比較（7件）、detached HEADを記録したbranchと同じとみなす（2件）、branchを持たないrecordを現在のbranchのものとみなす（4件）、branchを持たないrecordを常に停止（5件）、Gitが答えない場合もbranchの上にいないとみなす（1件）、まだcommitの無いbranchではbranchを見ない（1件）。full suiteは727 passed / 2 skipped / 603 subtests（baseline `f3a3331`、BL-032の計測）から739 passed / 2 skipped / 632 subtestsになり、増えたのはこのtestだけである。`rules/git` のCommit / pushに、記録したcommitを未適用とするにはHEADとbaseの関係に加えてrecordのbranchと現在のbranchの整合が要ること、HEAD == baseでも停止する場合、branchを持たないrecordの扱いを追記し、`skills/roadmap`・`skills/start`・`skills/create`・`skills/project-start` のresume記述から参照した。`INTENT_VERSION`、closeしたrecordのtop-level field、git_commitのpayload（BL-032の `branch` をそのまま使う）、effect種別、stage名、予約key、`Mutation` / `MutationController` の公開method、BL-032のD2、message一致の判定は変更しておらず、backfillは不要。残る論点（今回は扱っておらず挙動も変えていない）: (1) base以降に同じmessageのcommitがあるだけで一致と扱う判定（BL-033候補）。branchの判定より前に行われるので、別branch上の同じmessageのcommitも一致と扱われる。(2) Git stageを記録する前の窓（例: STARTのcompletion eventの適用後、finalization commitの記録前）に人がbranchを切り替えると、再実行はそのGit stageを切り替え先のbranchで記録し、commitとpushをそのbranchへ行う（STARTでは前のstageの成果commitとpushはmain、finalizationはside）。記録済みのcommitを別branchで作る問題ではないので対象外で、baselineと修正後で同一の実測。(3) branchの短縮名と同名のtagがあると、commitは記録したbranchに作られた後、push previewのrefspecが曖昧で `git_error` になる（従来どおり）。(4) 修正前の規則ですでに別branchへcommitしてcompletedになったmutationは自動repairしない。branchを持たない既存recordは、HEADがbranchの上にいれば人による照合が要る。(5) plan exclusionの再実行はcommitの分類より前にBL-029の前提条件で止まるため計測していない。BL-029の着地後に再計測する。

### BL-036 Resumed operation can commit its decided effects on another branch before its commit effect is recorded

- ID: BL-036
- Title: Resumed operation can commit its decided effects on another branch before its commit effect is recorded
- Status: RESOLVED
- Kind: implementation, recovery
- Severity: major
- Problem: operationは、Project正本へのeffect（lifecycle event、Roadmap / Phase / Workの登録、Related、Project開始やbootstrap backfill・push-pin maintenanceが書くfile等）を決定してrecordへ記録・適用した後に、それを確定するGit stage（git_commit / git_push）を記録する。git_commitの `branch`（BL-032）はそのGit stageを記録した時のHEADから入り、決定したeffect自身はどのbranchで決定したかを持たなかった。そのため、決定を記録した後・Git stageを記録する前に中断したoperationの間に、人が同じcommitを指す別branchをcheckoutすると、同じrequestの再実行は現在のbranchでGit stageを新しく記録し、決定済みのeffectをそのbranchへcommit・pushして成功を返し、mutationをcompletedにした。記録したbranchとそのremoteには何も届かず、`validate_project` は問題を示さない。BL-035は記録済みのcommitのbranchを比べる修正なので、比べる相手がrecordに無いこの窓は直らない（BL-035の残る論点(2)）。実測（baseline `72514f1`、remoteありProject、monkeypatchなし、Gitの `core.fsmonitor` hookまたは `GIT_TRACE2_EVENT` の監視でWorklineのprocessを実際にkill）: single-work STARTでW1のcompletion eventを適用した直後・finalization commitの記録前にkillし、`git checkout -b side` の後の再実行は `completed` を返した。W1のlifecycle event 4件を含むfinalization commitはsideとremote sideにだけあり、local main・remote mainは成果commitのまま、recordはcompleted、`validate_project` は問題なし。mainへ戻って同じSTARTを実行すると、新しいmutationとしてexecutorを再実行し、同じWorkのcompletionをmainにもう一度作った。outer STARTではW1のfinalizationに続いてW2とintegrationもside上で実行・commit・pushして `phase_complete` を返し、mainへ戻った再実行はPhaseをW1からもう一度実行した。W2のcompletionで中断した場合もW2とintegrationがsideにだけ完了した。Roadmap Phase holdは `phase_held` をsideにだけcommitし、mainでの再実行は別のholdをもう一度commitした。direct CREATEはsideにWorkを登録し、mainでの再実行は同じspecの別Workを登録した。下の319 scenarioのmatrixでは、baselineの偽成功77行のうち72行が決定の記録後・Git stageの記録前（決定を記録して未適用 / 適用済み）の窓で、START（single-workとouterのcompletion、outerの2番目のWork、hold）、Roadmap（Phase hold / cancel / entry / Phase追加）、direct CREATE、Related maintenance、Project開始、bootstrap backfill、push-pin maintenanceのすべてで起きた（同じcommitの別branch 16、独立なcommitのある別branch 14、書き換えた別branch 14、side起点でmain 13、同じ移動を所在の無い旧recordの形で 13、Project開始のdetached HEAD 2）。残る5行はWorkの実行を開いただけ・何も決定していない窓の行。
- Why it matters: 成功を返したoperationの成果が、決定したbranchにもそのremoteにも無い。`rules/git` の成功条件（remoteありProjectでは必要成果がremoteへ反映されるまで成功扱いにしない）と、`skills/start` のTerminal finalization（terminal eventのfinalizationを同じmutationのGit段階で確定する、BL-031）に反する。recordはcompletedで閉じるので回復経路が残らず、元のbranchへ戻った人にはoperationが無かったように見え、同じrequestの再実行がexecutor・登録・lifecycleを二重に実行して、別IDのWork / event / commitがbranchごとに並立する。outer STARTではPhaseの残り全体が別branchで実行される。`validate_project` はworking treeの構造だけを見るので検出しない。
- Likely scope: 決定したeffectの所在（branchとHEADのcommit）をその決定と同じdurable writeで記録すること、確定するcommitを記録するまでreplay・stageの記録・ID予約・note・write scopeの保存を所在の上に限ること、Git stageを記録する時に所在のbranchとcommitの `branch` が一致することを示してBL-032 / BL-035の分類へ引き継ぐこと、所在を持たない旧recordの扱い、STARTのexecutor前後の区別、Project開始のdetached HEAD / まだcommitの無いbranch、`rules/git` のCommit / pushとSkillのresume記述。記録済みcommitの分類（BL-032 / BL-033 / BL-035）、cancelのdurable decision（BL-030）、plan exclusionのresume（BL-029）、bootstrap backfill自身のresume（BL-034）は変えない。
- Cross-project impact: あり（全ProjectのGit stage記録前の中断からのresume）
- Backfill likely: no（既に別branchで確定してcompletedになったmutationは自動repairしない。所在を持たないpending recordは推測でresumeしない）
- Human confirmation likely: yes（`rules/git` のCommit / pushに決定の所在を加える。所在を固定する時点、旧record、outer continuation、Project開始、cancel、Gitが答えない場合の扱いは採用時に決定済み）
- Self-hosting prerequisite: no
- Evidence class: code inspection
- Resolution: 原因は、決定したeffectの所在がdurableにならず、Git stageを記録する時点のHEADがそのままcommitのbranchになっていたこと。Git stageを記録するまでは、再実行がどのbranchにいても、recordから決定の所在を示せなかった。修正は `src/workline/mutation.py` のmodule関数と、`Mutation` の既存methodからの呼び出しだけで、owner（START、Roadmap、CREATE、Project開始、backfill、pin maintenance）のコードは変えていない。(1) 所在の記録: `Mutation.add_effects` は、Project正本のfile effect（`write_file` / `add_relation` / `remove_relation` / `append_event`）を持つstageを記録する時、同じsaveで各file effectに `decided_on: {branch, head}` を付ける。branchは `gitcmd.current_branch_ref` の完全なref名（`gitcmd.head_detached` がdetachedと答えた時だけnull）、headは `gitcmd.head_commit`（まだcommitの無いbranchではnull。git_commitの `base_head` と同じ扱い）。Gitがbranchにもdetachedにも答えなければ、stageを記録せず `git_error` で止まる。STARTがexecutorより前に記録するWork開始のlifecycle（`work_started` / `work_resumed` / `work_target_added` だけのstage）と、Git stageは所在を持たない。STARTの所在は、executorが返した後にcompletion・hold・cancel・派生等を記録した時に初めて決まる。(2) 未確定の決定: recordの最後のgit_commitより後に記録された、決定を持つstageを、確定するcommitをまだ記録していない決定として読む。その間に記録する決定は、新しい所在ではなく同じ所在を持つ。(3) 検査: 未確定の決定がある間、`Mutation.apply`（分類とreplayより前）、`add_effects`（stageの記録より前）、`reserve_id`（新しいIDの予約）、`set_note`、`extend_scope` は、現在HEADが所在と同じbranch（完全なref名。detached HEADはdetachedで決定したものとだけ一致）の上にあり、所在のheadが現在のHEADそのものか祖先である（`gitcmd.descends_from`。独立なcommitで伸びただけの履歴。headがnullならbranchだけ）ことを示せる時だけ進む。示せなければ何もreplay・記録・保存せず `reconcile_required` で止まり、recordのbytesもworking treeも変わらない。Gitが答えない問いは示さない。(4) 引継ぎ: 未確定の決定がある間に記録するgit_commitは、payloadの `branch` が所在のbranchと一致しなければ記録しない（`reconcile_required`）。記録した後は所在を読まず、そのcommitのmessage・branch・base（BL-033 / BL-035 / BL-032）がmutationを扱う。(5) 所在を持たない決定（修正前のimplementationのrecord）は、確定するcommitが記録されるまで、現在のbranchが記録時と同じでも推測せず `reconcile_required` で止まる。所在の形が違う・未確定の決定どうしで所在が違うrecordも止まる。確定するcommitを記録済みの旧recordと、Work開始のlifecycleだけの旧recordは従来どおり進む。`INTENT_VERSION`、closeしたrecordのtop-level field、effect種別、git_commit / git_pushのpayload、invocation、stage名、予約key、`Mutation` / `MutationController` の公開method、BL-032のD2・BL-033のmessage判定・BL-035のbranch判定は変更しておらず、effect recordに `decided_on` keyが加わるだけで、backfillは不要。修正前のimplementationは `decided_on` を読み飛ばして従来どおり動く。実測（A/B: baseline `72514f1` と修正後で、investigationのmatrixを拡張した同じscenarioをmonkeypatchなしのGit操作で実行し、各stepの例外の型・code・ID正規化後のmessage、実行したeffect、記録したstage、record・HEAD・branch・remoteの移動、決定したeffectが着地したref、pending record、dirty、`validate_project`、stateを比較）: 319 scenario（START single-work / outer / hold × 決定前・Work開始後・成果push後 / 決定を記録して未適用 / 適用済み / commit記録後・未作成 / commit後・push前の窓、Roadmap Phase hold、START outerの2番目のWork・single-workのhold・cancel、outerのcancel、Phase cancel・plan exclusion・entry・Phase追加、direct CREATE、Related maintenance、Project開始、bootstrap backfill、push-pin maintenance × 同じbranch、同じcommitの別branch、別branchへ行って戻る、detached HEAD、side起点で同じsideとmain、独立なcommitのある別branch、書き換えた別branch、書き換えた同じbranch、独立なcommitで伸びた同じbranch、Gitが現在のbranchに答えない、所在を取り除いた旧recordの形でmain / side / detached、停止した行は元の場所へ戻して同じ再実行）のうち、191 scenarioは全stepで一致した。baselineの偽成功77行は5行になり、残る5行は決定前の窓（Phase holdのID予約だけ、STARTのexecutor中断、成果commitのpush後）で別branchへ移った行で、決定がその別branchで行われる（採用時の決定どおり変えていない）。差分は128 scenarioで、すべて次のどれかだった。(a) 72行: baselineの偽成功（上記）が、修正後は再実行で何も実行・記録せず `reconcile_required` になり、record・HEAD・branch・remoteは不変。(b) 24行: Gitが現在のbranchに答えない（16）と書き換えた同じbranch（8）で、baselineはbranchの無いGit stageを記録してから、または書き換えた履歴の上にcommitを作ってからpushの拒否で止まっていたのが、修正後は何も変えずに止まる。(c) 5行: 書き換えた同じbranchのSTARTで、baselineも何も変えずに止まっていた行の理由がこの規則になった。(d) 8行: single-work STARTのcancel（決定の後、Git stageの記録前）を別branch等で再実行した行で、baselineはreplayの後に `spec_violation`（cancelでterminalになったWorkの開始の拒否、BL-030の対象）、修正後はreplayの前に `reconcile_required`。どちらも何も変えずに止まり、元のbranchでは従来どおり `spec_violation`（cancelのresumeを新たに可能にしない、採用時の決定）。outer STARTのcancelはBL-031の停止がこの検査より先で、全transitionで不変。(e) 16行と3行: 所在を取り除いた旧recordの形で同じbranchの上（16行）、またはdetached HEADで入口の `detached_head` に止まった後mainへ戻った再実行（3行）で、baselineは進み修正後は止まる（採用時に決定した拒否拡大）。停止した行の再実行: 所在のある93行は、元の場所へ戻る（checkout、Gitが答える、書き換えた履歴を決定時のcommitへ `reset --keep`）と、baselineで移動が無かった場合の再実行と同じ結果（成功なら決定したbranchとremoteに同じeffectが着地）になり、旧recordの形の32行は止まり続ける。同じbranchのまま・行って戻る・side起点のside・独立なcommitで伸びた同じbranch、決定前の窓、commit記録後の窓（BL-035 / BL-032 / BL-033がそのまま扱う）、commit後・push前の窓は全行一致した。exact reproduction（上記、mockなし）は修正後、single-work START・outer START（W1 / W2のcompletion）・Phase hold・direct CREATEのいずれも再実行が `reconcile_required` で何も変えず（direct CREATEの再実行も `updated_at` を含めrecordのbytes不変）、mainへ戻った同じ再実行が同じmutationで、executorを再実行せずにcompletion / hold / 登録をmainとremote mainへ確定した（outerはW2とintegrationをmainで続けた）。commit記録後に中断した対照（BL-035）は両方で同じ。正当なbranchの移動を扱う調査のprobe 13本（executorの中でのbranch移動（single-work、outerの2番目のWork）、executorがdetachしてmainへ戻す、question wait中の移動と再開、mutationの無い状態での移動、detached HEADの入口停止の後にmainで再実行、Project開始のdetached HEAD・まだcommitの無いmain、確定済みholdの後の移動、決定前の窓（ID予約だけ、executorの中断、成果commitのpush後）での移動）は、修正前後で全行一致した。push-pin maintenanceの決定後の別branch、別branchに記録と同じmessageのcommitがある場合の再実行は、偽成功から `reconcile_required` になった。修正前のimplementationが中断したProjectのWorkline rootを修正後のimplementationへ入れ替えて再実行した実測（Phase hold / single-work START × 決定の適用後・commit記録後・question wait × そのまま / 別branch / detached HEAD）では、決定の適用後の旧recordはそのままでも別branchでも `reconcile_required`（実行effectなし、record・HEAD・remote不変）、detached HEADは入口の `detached_head`、commit記録後の旧recordは従来どおり（そのままなら確定、別branchはBL-035で停止）、question waitの旧recordは再開した。修正後が書いたrecordを修正前のimplementationが読むと従来どおりに動く。regression test（`tests/test_decision_branch_binding.py`）は17 test / 52 subtestで、13 ownerのそれぞれで決定を適用した後・commitを記録する前に別branchで再実行すると何も実行・変更せずに止まり、mainへ戻れば同じmutationで確定すること（STARTはexecutorを再実行しない）、決定を記録して未適用の窓では別branchのworking treeへreplayしないこと、独立なcommitのある別branch・書き換えた別branch・書き換えた同じbranch・side起点のmainで止まり戻れば進むこと、同じbranchのまま・行って戻る・独立なcommitで伸びた同じbranchでは確定するcommitを1回だけ作ること、sideで決定したoperationはsideで進むこと、所在が完全なref名とcommitで決定stageのfile effectにだけ記録され、Work開始のlifecycle・成果commit・finalization stageは持たず、commitの `branch` へ引き継がれ、その後はBL-035 / BL-032の規則が扱うこと、commitを記録する前にHEADが移る・commitが別branchを名乗る場合はcommitを記録しないこと、executorの中でのbranch移動（single-work、outerの2番目のWork）・question wait中の移動・executor中断後の移動・ID予約だけのholdの後の移動は、移動先で決定し確定すること、outer STARTが別branchで次のWork・integrationへ進まないこと、cancelの停止（outerはBL-031の停止、single-workは別branchで `reconcile_required`・元のbranchで `spec_violation`）、所在の無い旧record（main / side、Project開始のdetached）が止まりWork開始だけ・commit記録済みの旧recordは進むこと、Gitが答えない場合に再開も決定の記録もしないこと、別branchの同じmessageのcommitを決定の確定とみなさないこと、Project開始をmainで決定してdetachedで・detachedで決定してbranchで・まだcommitの無いbranchを切り替えて再実行すると止まり、決定した所在では1回だけ初期commitを作ること、`core.fsmonitor` hookで実processをkillしたSTARTが別branchで止まりmainで確定すること、`INTENT_VERSION` とeffect種別が変わらないことを固定する。修正前のコードではこのうち13 test（subtest 38件を含む42件）が失敗し、成功する4 testは変えてはいけない挙動（決定したbranchでの再開、sideで決定したoperation、executor・question waitの移動、recordの形）だけである。規則の取り違えも11のmutantで検出されることを確認した: 最初のfile effectでbranchを固定してmutationの最後まで持つ（operation開始時の固定、8件失敗）、STARTのexecutorより前のWork開始lifecycleでbranchを固定（6件）、別branchでの再実行で所在を現在のbranchへ書き換えて進む（34件）、replayしてから停止（5件）、所在の無い旧recordを現在のbranchのものとみなす（5件）、Git stageの記録時に所在を見ない（2件）、outer STARTだけ検査を迂回（2件）、Project開始だけ検査を迂回（5件）、branch名だけで比較して書き換えた履歴を通す（2件）、Gitが答えないbranchをdetached HEADとみなす（1件）、ID予約・note・write scopeの保存を検査しない（1件）。full suiteは751 passed / 2 skipped / 660 subtests（baseline `72514f1`、BL-033の計測）から768 passed / 2 skipped / 712 subtestsになり、増えたのはこのtestだけである。`rules/git` のCommit / pushに、決定したstageが所在を同じdurable writeで記録すること、所在を固定する時点、確定するcommitを記録するまでの条件と停止、commitの `branch` への引継ぎ、所在の無い旧recordとGitが答えない場合を追記し、`skills/start`（Mutation / Git、Terminal finalization、未完了のcancel）・`skills/roadmap`・`skills/create`・`skills/project-start` のresume記述から参照した。残る論点（今回は扱っておらず挙動も変えていない）: (1) commitを作った後・pushの前にsideからmainへ移った再実行は、記録済みのcommitの分類より前に、適用済みの前のeffectをmainのworking treeへreplayしてから `reconcile_required` で止まる（baselineと同一。所在の寿命はcommitの記録までで、記録済みcommitのbranchが検査されるのは分類の時点だけという別の順序の問題）。(2) single-work STARTのholdを適用した後・commitを記録する前の再実行は、同じbranchでもexecutorを再実行する（baselineと同一。BL-031のGit段階からの継続はcompletionだけで、holdには無い）。(3) 決定前の窓（executorの中断、成果commitのpush後、ID予約だけ）で別branchへ移ると、決定とその確定は移動先で行われ、成果commitは元のbranchに残る。outer STARTで前のWorkのcommitを作った後・pushの前に移ると、前のWorkは元のbranchへpushされ、次のWorkは移動先で決定される（採用時の決定どおり）。(4) plan exclusionの再実行はBL-029の前提条件で決定の検査より前に止まるので計測していない。BL-029の着地後に、plan exclusionの決定の所在とcommitへの引継ぎを再計測する。(5) BL-030がcancelのdurable decisionを導入する時は、そのdecisionのstageがfile effectを持てば同じsaveで所在を持つ。(6) 修正前に別branchへ確定してcompletedになったmutationは自動repairしない。所在の無いpending recordは人による照合が要る。(7) direct CREATEの再実行は、commitを記録した後の窓では従来どおり停止の前にwrite scopeを保存し直す（所在の検査は確定するcommitの記録までなので変わらない）。(8) resumeは前のstageで記録済みのpushも再生する（BL-032の残る論点のまま）。

### BL-037 Own applied commit is not recognized when its stored message differs from the recorded message

- ID: BL-037
- Title: Own applied commit is not recognized when its stored message differs from the recorded message
- Status: RESOLVED
- Kind: implementation, recovery
- Severity: major
- Problem: `Mutation.apply` はresumeのたびに、STARTではstageを記録するたびにも、適用済みのgit_commitを含む記録済みeffectをすべて分類し直す。`MutationController._classify_commit` は、recordが適用済みとして持つcommitを、`base_head..HEAD` に記録と同じmessage（前後の空白を除いた全文）のcommitがあることでしか、このmutationのcommitと認識できなかった。しかしGitは `git commit -m` のmessageを既定のcleanupで保存し（各行末の空白・CRを削り、連続空行を1行にする）、`commit-msg` / `prepare-commit-msg` hookはtrailer・prefix・comment行を加え、`commit.cleanup=strip` は `#` 行を消し、Workline自身もgit logをuniversal newlineで読むので、自分が作ったcommitでも記録と同じmessageとして読めないことがある。その後の同じmutationのstage（outer STARTの次のWorkが前のWorkの成果fileを編集する、question waitの前の編集、message書換えhookの下での次のWorkのlifecycle event）や人の編集がそのcommitのpathsを再びdirtyにすると、`applied with unexpected result` → `reconcile_required` になった。実測（investigation `72514f1`、mockなし・中断なし）: outer STARTでW1の成果messageに途中行の行末空白と連続空行、CRLF、lone CRのどれかがある、またはChange-Id型のcommit-msg hookがあると、W2の段階で止まり、同じSTARTの再実行も、ledgerを共有する他のoperationも止まり続けた（`validate_project` は沈黙、false successなし）。hookの下ではWorkどうしが同じfileを触らなくてもouter STARTは2本目のWorkで必ず止まり、single-workのderive継続→完了も止まった。中断窓では、適用済み・push前（W3）とpush後・完了前（W4）に人がpathsを編集した再実行が止まり、W3ではremoteへ届かないまま残った。bootstrap backfillの所有証明（BL-034）も、記録と同じmessageでだけ自分のcommitを示していた。W0〜W2はBL-033のgateでmessageを見ないので影響しない。
- Why it matters: 契約上正当なexecutor message（`skills/start`: executorの文字列をそのまま使う）や一般的なhookで、通常運転のouter STARTが自力で抜けられない停止になる。hookを外してもcommitのmessageは変わらず、手でcommitや破棄をしても次のWorkの再実行で同じ停止に戻り得る。
- Likely scope: 適用済みcommitの同一性をmessageに頼らず、commitを作った時のcommit IDで示すこと（investigationのCandidate Db）。BL-033（他人の同じmessageのcommitを自分のcommitとしない）、BL-032 D2、BL-035、BL-036の判定を変えないこと。messageの正規化で解決しないこと。
- Cross-project impact: あり（全ProjectのGit段階からのresumeと、同じmutationの中の再分類）
- Backfill likely: no（commit IDを持たない既存recordは従来どおり扱う）
- Human confirmation likely: yes（`rules/git` のcommit分類を変える。到達不能なIDの拒否拡大、記録branchの外の扱い、backfillの所有証明は採用時に決定済み）
- Self-hosting prerequisite: no
- Evidence class: code inspection, reproduction
- Resolution: 適用済みcommitの同一性を、commitを作った時に記録するcommit IDで示すようにした。(1) 記録: `Mutation.apply` がgit_commitを未適用と分類して作る時だけ、`mutation.py` のmodule関数 `_make_commit` がcommit前のHEADと、commitが成功した直後のHEADを読み、commit前のHEADを唯一のparentとする新しいcommit（まだcommitの無いbranchではparentなし）であることを `gitcmd.commit_parents` で確かめたcommitのIDを、effect recordの `commit_id`（payloadの外、`applied` の隣）に置き、既存の `applied: true` と同じ `_save()` で書く。示せない場合（post-commit hookが続けてcommitした、Gitが答えない等）はIDを書かない。後から履歴を探索してIDを推測しない。commitの後・保存の前に中断すると、flagもIDも無い（従来どおり）。(2) 分類: `_classify_commit` は最初に、recordが適用済みで `commit_id` を持ち、HEADが記録したbranchの上にある（`_on_recorded_branch`。branchを持たないrecordはdetached HEAD）時、IDのcommitがHEADから到達可能（`gitcmd.descends_from`）なら一致、到達不能・IDの形が違う・Gitが答えないなら不一致（`reconcile_required`）とし、messageの判定へ戻らない。記録したbranchの上にない時と、IDを持たないrecordは、従来の順序（BL-033のmessage判定、pathsにcommitする変更なし、BL-035、BL-032 D2、不一致）のまま。(3) backfill: `bootstrap._unprovable_record` はgit_commitの `commit_id` を、適用済みで完全なcommit IDの時だけ受け入れ、`_refuse_unowned_bootstrap` はIDを持つrecordでは「base以降にbootstrapを変えた唯一のcommitがそのIDで、bootstrapだけを変えた」ことで示してmessageを見ない（IDを持たないrecordは従来のmessage判定）。採用時に決定: 到達不能なIDは拒否する（amend、reset、同じmessageでの再commit、resetしてstagedのまま等は従来は一致として進んだ。拒否拡大）。記録したbranchの外ではIDを一致の根拠にせず従来の規則で扱う（BL-036が認めるexecutorの中のbranch移動、question wait中の移動を変えないため。常に拒否すると `test_decision_branch_binding.py` のouter executor移動のpinが止まることを実測）。backfillの所有証明にもIDを使う。`INTENT_VERSION`、closeしたrecordのtop-level field、effect種別、payload、invocation、`Mutation` / `MutationController` の公開method、commitのmessageとcleanup（`--cleanup` を強制しない）は変えていない。測定: 新しい `tests/test_recorded_commit_identity.py`（18 tests / 27 subtests: 途中行の行末空白と連続空行・CRLF・lone CRの成果messageでW2がW1の成果fileを編集する中断なしのouter START、commit-msgのChange-Id trailerとprepare-commit-msgのbranch prefixの下でのouter STARTとsingle-workのderive継続、CRLF messageのouter question wait再開、Change-Id hookの下でのPhase hold / START finalizationの適用済み・push前とpush後・完了前に人がevent logを編集した再実行、commit IDが適用済みと同じsaveで書かれること、commitの後・flagの前の中断ではIDが無く再実行もIDを作らないこと、post-commit hookが続けてcommitした時とGitがparentを答えない時はIDを書かないこと、reword amend・同じmessageのamend・resetして同じmessageで再commit・resetしてstagedのままでは止まり元のcommitへ戻せば進むこと、Gitが到達可能性に答えない時に止まること、同じcommitを含む別branchでは従来の規則で止まりmainへ戻れば進むこと、同じmessageの他人のcommitはIDを得ず（無関係なpathsなら自分のcommitをその上に作ってそのIDを書く）別内容なら止まり、commit後・flag前の自分のcommitに同じmessageの他人のcommitとpathsの編集が重なれば止まること、IDを取り除いたrecordは従来どおり（clean messageは進み、hookで変わったmessageは止まり、同じmessageのamendは進む）、BL-032の独立なcommitの上に作ったcommitのIDとBL-035の別branchでの停止、backfillのhookの下の3窓・同じmessageのamendでの停止・形の違うID）は修正後すべて成功し、`4d24730` では28件（7 tests、21 subtests）が失敗した（成功したのは従来の契約を変えないことをpinするID無しrecord、同じmessageの他人のcommitの拒否、W2でIDを作らないこと、`KnownResidualTests` 等）。mutant 5本（IDの規則からbranch条件を外す 2件、到達不能でmessageの判定へ戻る 4件、IDを記録しない 4件、parentの確認を外す 2件、backfillの所有証明がIDを見ない 4件）はすべて検出された。直接の回帰: BL-033の `tests/test_recorded_commit_message.py`（12 passed / 28 subtests）、BL-035の `tests/test_recorded_commit_branch.py`（12 / 29）、BL-034の `tests/test_bootstrap_backfill_resume.py`（9 / 27）、BL-036の `test_decision_branch_binding.py` のexecutorのbranch移動とouter continuation（3 / 7）、BL-032の代表（4 / 13）、START finalization resumeの代表（2 / 7）、`tests/test_mutation.py` と公開method・gitcmd名のpin（16 passed）。investigationのexact reproduction（mockなし・中断なし: clean、行末空白と連続空行、CRLF、Change-Id hook、hookでfileの重なりなし）は修正後すべて `phase_complete`、pending recordなし、HEADとremoteが一致した。`validate-registry` はPASS。残る論点: (1) commitを作った直後・flagとIDの保存前に中断したcommitは、再実行でpathsにcommitする変更が無いことから適用済みになるがIDは持たず、その後に同じmutationがpathsを再びdirtyにしてmessageも変わっていれば従来どおり止まる（そのcommitが誰のものかを示せないため）。(2) 修正前のimplementationが書いたpending recordは従来どおり。(3) 記録したbranchの外（executorがbranchを移ったouter START等）では従来の規則なので、message書換えhookの下では従来どおり止まる（`KnownResidualTests` でpin）。(4) clean messageのW3 / W4の後に同じcommitを含む別branchで再実行すると、完了がその別branchで確定し記録したbranchのremoteに届かない既存の挙動は変えていない（BL-036の残る論点と同系、別item）。(5) investigationの別件（U+2028 / U+0085を含む記録文字列でrecordが読めない、`commit.cleanup=strip` で本文が `#` 行だけのmessageのcommit拒否）は対象外。

### BL-038 Resume re-applies a mutation's committed effects on another branch before refusing it

- ID: BL-038
- Title: Resume re-applies a mutation's committed effects on another branch before refusing it
- Status: RESOLVED
- Kind: implementation, recovery
- Severity: major
- Problem: operationは、決定したeffectを記録・適用し、それを確定するgit_commitを記録して作り、pushする。resume（同じrequestの再実行）の `Mutation.apply` は、記録済みeffectを記録順にすべて現在のworking treeと比べて分類し直し、無いものを書く。commitを作った後・pushの前や、pushの後・operationの完了前に中断し、人がそのcommitを履歴に含まないbranchをcheckoutしてから再実行すると、適用済みのeffectはそのworking treeに無いので未適用と分類され、決定していないbranchのworking treeへ書き直された。BL-036の検査は最後に記録したgit_commitより後の決定だけを読み（commitの記録で所在をcommitの `branch` へ引き継ぐ設計）、`_classify_commit` がbranchを見るのは未作成のcommitを作る判断（BL-035 / BL-032）だけなので、作成済みのcommitが確定するeffectのreplayにはbranchの検査が無かった。停止は、書き直しでcommitのpathsに変更が残り、commitの分類が期待値不一致になった副産物だった。実測（investigation baseline `4d24730`、remoteありProject、Gitのhookで実processをkill: pre-pushでcommit作成後・push前、remoteのpost-receiveでpush後・完了前、commit-msgでcommit記録後・未作成）: Phase holdは `phase_held` eventを、single-work / outer STARTはWorkのlifecycle event 4件を、direct CREATEはWork fileとRelatedを別branchのworking treeへ書いてから `reconcile_required` で止まり、STARTはその前に成果stageの記録済みpushを再実行してfinalization commitをremoteへ出した。commitを含まないbranchでの再実行21行のすべてが書き直してから止まり、記録したbranchへ戻るcheckoutをGitが21行すべてで拒否した（書き直し分を捨てれば同じ再実行は収束した）。移動先に記録と同じmessageのcommitがあると、BL-033のmessage判定がそれを拾ってcommitを一致とし、書き直した変更を残したまま `phase_held` / `completed` を返してmutationを閉じた。`validate_project` はどの状態も検出しなかった。BL-037（`d2dadef`）の後もcommit IDは記録したbranchの上でしか使われないため、代表行（commit後・push前とpush後・完了前の別branch、Gitがbranchを答えない場合、同じmessageのcommit、outer STARTの2番目のWork、direct CREATE）は同じ書き直しと停止・成功を再現した。BL-036の残る論点(1)。
- Why it matters: 決定していないbranchのworking treeに正本の変更（event・entity file・relation）が未commitで残り、そのworking treeは「Work completed」「Phase held」を示すのに `validate_project` は何も示さない。BL-035 / BL-036が示す回復手順（記録したbranchへ戻る）をGitが拒否し、停止messageは原因を示さない。停止の前にremoteが変わり、同じmessageのcommitがあれば成功扱いでrecordが閉じる。全ownerの `Mutation.apply` を通るresumeに共通する。
- Likely scope: 適用済みeffectを書き直す前の読み取りだけの検査（investigationのCandidate D′）、書き直しが起きない再実行を変えないこと、BL-032以前のbranchを持たないrecordでbranchを推測しないこと、Gitがbranchを判定できない場合、`rules/git` のCommit / pushとSkillのresume記述。BL-032 D2、BL-033のapplied-message gate、BL-035のcommit branch guard、BL-036の決定の所在、BL-037のcommit IDは変えない。
- Cross-project impact: あり（全Projectのcommit作成後の中断からのresume）
- Backfill likely: no（既に書き直したworking treeやcompletedになったmutationは自動repairしない）
- Human confirmation likely: yes（`rules/git` のCommit / pushに追記。BL-032以前のrecordの扱いとGitがbranchを判定できない場合の扱いは採用時に決定済み）
- Self-hosting prerequisite: no
- Evidence class: code inspection, reproduction
- Resolution: 原因は、commitを記録した後、そのcommitが確定する適用済みeffectのreplayにbranchの検査が無く、作成済みのcommit自身の分類もbranchを見なかったこと。修正は `src/workline/mutation.py` のmodule関数 `_require_finalized_branch` と、`Mutation` の既存methodからの呼び出しだけで、ownerのコードは変えていない。(1) 検査: recordが適用済みとして持つfile effect（`write_file` / `add_relation` / `remove_relation` / `append_event`）のうち、その直後に記録されたgit_commit（それを確定するcommit。BL-036により決定の所在の上でだけ記録される）がpayloadに `branch` を持つものについて、現在HEADのbranch（`gitcmd.current_branch_ref`、完全なref名）と比べる。HEADがそのすべてと同じbranchの上にあれば何もしない（通常のflowで増えるのはGitへの問い1回）。どれかと違う（別branch、detached HEAD、Gitがbranchを答えない）時だけ、`unapplied_effects`（`Mutation.apply` と同じ分類と順序、書かない）でその再実行がそれらのeffectを書き直すことになるかを読み、なるなら `reconcile_required` で止まる。何もreplay・記録・commit・pushせず、recordのbytesもworking treeもindexも変えない。書かずに進むことはしない。messageは書き直すことになるeffect、それを確定するcommitのbranch、HEADの現在地（branch / detached HEAD / Gitが名付けられないbranch）と、そのbranchへ戻れば同じ再実行が進むことを示す。(2) 位置: `_require_decided_branch`（BL-036）と同じ5箇所、`Mutation.apply`（分類とreplayの前）、`add_effects`（stageの記録の前）、新しいIDの `reserve_id`、`set_note`、`extend_scope` で、いずれもBL-036の検査の後。direct CREATEの再実行がreplayの前に `extend_scope` でrecordを保存し直す経路も、保存の前に止まる。(3) 採用時の決定: payloadに `branch` を持たないcommit（BL-032以前のimplementationのrecord、detached HEADで記録したcommit）が確定するeffectは検査せず、branchを推測しない（別branchでは従来どおり書き直してから期待値不一致で止まる）。Gitが現在のbranchを答えられず書き直しが必要な時は `reconcile_required`、書き直しが要らなければ従来どおり進む。BL-037のcommit IDをbranchの所在の代わりには使わない。(4) 変えないもの: 書き直しが起きない再実行（記録したbranchのまま、行って戻る、BL-032の独立なcommitで伸びた同じbranch、commitを履歴に含む別branchでの記録branchへのpushとouter continuation、Gitがbranchを答えなくても正本がある場合）、W3でcheckoutが未commitの変更を運ぶ場合のBL-035の停止と文言、中断なしのexecutorのbranch移動、BL-036の決定の所在、BL-037のcommit IDによる記録branch上の分類。`INTENT_VERSION`、effect種別・payload・effect recordのkey、closeしたrecordのtop-level field、invocation、`Mutation` / `MutationController` の公開methodは変えていない。測定: 新しい `tests/test_finalized_effect_branch.py`（14 tests / 23 subtests: Phase hold・single-work START・outer START・direct CREATEのcommit後・push前と、Phase hold・STARTのpush後・完了前に、commitを含まないbranchで再実行すると何も実行・変更せずに止まり、何も捨てずにmainへ戻れて同じmutationで確定すること（STARTはexecutorを再実行せず、outerはPhaseを完了する）、direct CREATEがwrite scopeを保存する前に止まりrecordのbytesが変わらないこと、移動先の同じmessageのcommitで成功しないこと、outer STARTの2番目のWorkの窓で前のWorkの記録済みpushを再実行しないこと、sideで決定したoperationをmainで再実行すると止まりsideで確定すること、commitが作れなかった後に適用済みの変更をstashして移った場合も書く前に止まること、記録したbranchのまま・行って戻る・独立なcommitで伸びた同じbranchでは従来どおりpushだけを実行すること、commitを含むbranchでは記録branchへpushしouterはそのbranchでPhaseを完了すること、中断なしのouter STARTでexecutorがbranchを移っても（成果fileあり / なし）完了すること、Gitがbranchを答えない時は書き直しが必要なら止まり不要なら進むこと、branchを持たない旧recordは従来どおり（別branchでは書き直してから期待値不一致、同じbranchでは進む）で、履歴を書き換えた記録branchはBL-037の規則が扱うこと、pre-push hookで実processをkillしたSTARTが別branchで止まりmainで確定すること）は修正後すべて成功し、`d2dadef` では9 tests（うち4 testsは9 subtestsの失敗）が失敗した（成功したのは変えてはいけない挙動と既知の残る論点をpinする5 tests）。mutant 4本（検査しない 14件、書き直しの有無を見ずにbranchだけで止める 6件（commitを含むbranch 3、中断なしのexecutorのbranch移動 2、Gitが答えないが正本がある 1）、replayの前だけで保存の前に検査しない 2件、Gitが答えないbranchを記録したbranchとみなす 1件）はすべて検出された。直接の回帰（BL-037の `d2dadef` の上の同じ変更で計測）: BL-037の `tests/test_recorded_commit_identity.py`（18 passed / 27 subtests）、BL-036の `tests/test_decision_branch_binding.py`（17 / 52）、BL-035の `tests/test_recorded_commit_branch.py`（12 / 29）、BL-033の `tests/test_recorded_commit_message.py`（12 / 28）、START finalization resumeの `tests/test_terminal_finalization_resume.py`（21 / 13）、`tests/test_create.py`（6 passed）、`tests/test_mutation.py` と公開methodのpin（15 passed）。investigationのharnessで代表23行を `d2dadef` と修正後で実行した: 対象9行（commit後・push前とpush後・完了前の別branch、Gitが答えないbranch、同じmessageのcommit、outer STARTの2番目のWork、direct CREATE）は書き直し・push・recordの保存なしに止まり、記録したbranchへ戻ると、修正前に書き直し分を捨ててから戻った場合と同じ最終状態に収束した。残る14行（同じbranch、独立なcommit、書き換えた記録branch、Gitが答えない同じbranch、commitを含むbranch（hold / START / outer / CREATE）、branchを持たない旧record、commit記録後・未作成、中断なしのexecutorのbranch移動2本）は観測が完全に一致した。`validate-registry` はPASS。`rules/git` のCommit / pushに規則を追記し、`skills/start`・`skills/roadmap`・`skills/create`・`skills/project-start` のresume記述から参照した。残る論点: (1) branchを持たない旧recordは、別branchでは書き直してから止まる（採用時の決定）。(2) 履歴を書き換えた記録branch（reset等）では、BL-037の到達不能なIDで止まる前に、同じbranchへ適用済みeffectを書き直す（branchの所在の問題ではない）。(3) 書き直しが起きない再実行では、前のstageの記録済みpushが記録branchにある人の未push commitも公開する（BL-032の残る論点のまま）。(4) Gitがbranchを答えない場合に到達するのは、入口の `ensure_git_ready` でbranchを判定しないProject開始と、`symbolic-ref --quiet` だけが答えない場合に限られる。

### BL-039 Recovery record becomes unreadable when recorded text contains Unicode line separators

- ID: BL-039
- Title: Recovery record becomes unreadable when recorded text contains Unicode line separators
- Status: RESOLVED
- Kind: implementation, recovery
- Severity: major
- Problem: recovery record・relation file・entity frontmatterが使うYAML subset（`yamlish`）は、誤読されうる文字列をJSONのdouble-quoted scalarとして書く。writerの `json.dumps(ensure_ascii=False)` はU+0085（NEL）・U+2028（LINE SEPARATOR）・U+2029（PARAGRAPH SEPARATOR）を文字列の中にraw文字のまま残し（JSONとしては正しい）、readerは `str.splitlines()` で物理行に分けていた。`str.splitlines()` はこの3文字でも行を終えるので、quoted scalarが途中で切れて `bad double-quoted scalar`（keyなら `expected 'key: value'`）になり、recordは書けても読めなかった。書込みは成功し、書いたoperationはin-memoryのrecordで最後まで進むので、次にrecordを読む時に初めて表面化する。LF / CR / CRLF / VT / FF / FS / GS / RSはJSONがescapeするので影響しなかった。`MutationController.list_records` は状態を問わず全recordを読むので、該当するrecordが1件残ると、本人のresumeも、write scopeが重ならないoperation・backfill-bootstrap・pin maintenance・Project開始の再実行も `reconcile_required` になり、`validate_project` は沈黙した。実測（investigation `d2dadef`、mockなし）: outer STARTの成果messageに3文字のどれかがありW2がquestion waitした後、push拒否で中断した直接CREATEのWork名、payload validationで拒否された直接CREATE（abandoned recordが残る）、CLIの `create-work --name` で再現した。Related targetに入ると `related.yaml` 自体も読めなくなった（commit前のpostcheckで止まり、worktreeに残る）。raw文字を手でescapeしてresumeしても、resume中のsaveがraw文字を書き戻し、resumeしたrecordは削除されないので再び止まった。`events/events.jsonl` もwriter（`ensure_ascii=False`）とreader（`splitlines`）が同じ形だった（現行APIに利用者の文字列が入る経路は無い）。BL-030のcancel決定はこの読み戻しを確かめるので、3文字を含む理由や新Workの文字列を、recordが保てない値として記録前に `validation_failed` で拒否していた。
- Why it matters: 契約上正当なexecutor message、Work名・desired state・Related等の合法な入力で、Project全体が自力で抜けられない停止になる。3文字はWeb・PDF・DTPからのcopy（強制改行）やcp1252の誤decode等で現実に混入する。
- Likely scope: 入力を拒否せず、writerの表現とreaderの行分けを直す（investigationのCandidate A + B4）。LFだけで行を分けるreader（Candidate B）は手編集されたyamlishの既存の意味を広く変えるので採らない。record形式・`INTENT_VERSION`・入力validationは変えない。既存fileの一括書き換えはしない。
- Cross-project impact: あり（全Projectのrecovery record・canonical fileの読み書き）
- Backfill likely: no（raw文字を持つ既存のrecord / fileはそのまま読み、そのfileを書く通常のoperationでescape表現になる）
- Human confirmation likely: yes（`rules/git` のstructured textの表現と、BL-030の保てない値の範囲を変える。修正方針、入力を拒否しないこと、events.jsonlのrecordをLFで区切ること、related.yamlを一括で書き換えないことは採用時に決定済み）
- Self-hosting prerequisite: no
- Evidence class: code inspection, reproduction
- Resolution: (1) writer: `yamlish.escape_line_separators` がJSON textの中のU+0085 / U+2028 / U+2029をJSONのUnicode escapeにし、`yamlish._scalar`（recovery record・relation file・project.yaml・frontmatter）と、event logの2つのwriter（`store.render_event_line`、`MutationController.apply_effect` のappend_event）が使う。JSONはこの3文字を文字列の外に持たないので、変わるのは3文字を含む文字列だけで、他の文字列の出力bytesは同じ。(2) reader: `yamlish._prepare` の行分けを `_lines` にした。3文字を含まないtextは従来どおり `str.splitlines()` で分ける。含む場合も同じ境界で分け、3文字のどれかで終わる行が、writerがdouble-quoted scalarを置く位置（indentまたは `- ` の直後に始まるkey / item、keyの `: ` の直後に始まるvalue）で開いたままのJSON文字列の途中にある時だけ、その文字を行に残して続ける（`_inside_quoted_scalar`。文字列の終わりは `_split_key` と共通の `_quoted_end` で判定）。comment、quoteを含むplain scalar / key、閉じた文字列の後、space以外の空白の後のquoteでは従来どおり行を終える。(3) event log: `ProjectStore.read_events` はrecordをLF（`read_text` がCRLF / CRをLFにした後）だけで分ける。JSON文字列の外にLF以外の行境界を置いた手編集のlog（Workline自身は書かない）は、従来は別々のrecordとして読めたが、malformedとして拒否される（採用時に決定したLF区切りによる変化）。(4) BL-030との統合: cancel決定の読み戻し（`start._read_back`）は変えず、3文字を含む値が保てるようになったので、その理由や新Workの文字列を持つcancelは記録され、中断後も継続する。`start._cancel_decision` のdocstringとSTART Skillの保てない値の例、`tests/test_cancel_decision_resume.py` の保てない文字列のpinを、行区切りからUTF-8で書けないlone surrogateに替え、3文字を含む理由は保てる値のpinへ移した。(5) `rules/git` のMutation Controllerに、structured textの文字列の中のUnicode line separatorを行・recordの区切りとしないこと、3文字は合法なtextで入力として拒否しないこと、writerの表現、readerが既存のraw表現も読み通常のsaveでescape表現になることを追記した。`INTENT_VERSION`、record / effect / payload / invocationの形、`Mutation` / `MutationController` の公開method、入力validationは変えていない。raw文字を持つ既存recordは次のsaveで、`related.yaml` はそのfileを書く通常のoperationでescape表現になり、それ以外のfileを修復のためだけに書き換えない。測定: 新しい `tests/test_record_line_separators.py`（15 tests / 27 subtests: 3文字それぞれのvalue・key・sequence item・nested value・frontmatterでの往復と出力にraw文字が無いこと、LF / CR / CRLF / VT・FF・FS / 日本語 / quoteとbackslash / backslashで始まるescape風の文字列の出力bytesが修正前と同じこと、raw文字をquoted scalarに持つ修正前の形のrecordを読むこと、手編集の形（3文字でentryを区切る、3文字の後にLF、quoteを含むplain scalar、comment、閉じた文字列の後、NBSPの後のquote、2つのspaceの後のquote、未終端）とVT / FF / FS / GS / RS / CR / CRLFの読み方が修正前と同じで、変わるのはquoted scalarの中の3文字だけであること、event logの2つのwriterがraw文字を書かず、修正前のraw行を読み、CRLFと空行は従来どおりで、LF以外の行境界はrecordを終えないこと、outer STARTのLSを含む成果messageでquestion waitし別processでresumeして `phase_complete`（commitにもmessageが入る）、修正前の形に書き戻したquestion waitのrecordが読め、重なるholdは通常のoverlapで止まり、backfillは進み、resumeの最初のsaveでescape表現になること、push拒否で中断したNELを含む名前のCREATEのrecordを修正前の形にしても重ならないPhase holdが進みresumeが収束すること、PSを含む名前のCREATEがpayload validationで拒否されたabandoned recordを修正前の形にしても後続が止まらないこと、LSを含むRelated targetがescapeで書かれ、修正前の形でcommitされた `related.yaml` が読め、無関係なholdはそのfileを書き換えず、related maintenanceが書く時にescape表現になること、CLIの `create-work --name` のpush拒否・再実行・`validate-project` PASS、理由と新Workのname・desired state・derivation detail・Related targetに3文字を持つcancelが決定を記録してcancel適用後の中断から `cancelled` まで継続すること）は修正後すべて成功し、`1ca695f` では20件（11 tests、9 subtests）が失敗した（成功したのは修正前と同じ出力bytesのpinと、VT〜CRLFの読み方のpin）。`tests/test_cancel_decision_resume.py` の保てる値に加えた3文字の理由は `1ca695f` では3件とも `cannot be kept exactly by the recovery record` で拒否され、修正後は記録・継続する。mutant 4本（writerのescapeを外す: 10件、readerの救済を外す（yamlishとevent logの行分けを戻す）: 14件、U+2029を対象から外す: 6件、yamlishをLFだけで行分けする: 手編集の形のpin 8件）はすべて検出した（BL-030を含まない `9be0c5b` 上の14 testで測定。変更したfileは `1ca695f` と同一）。修正前後のyamlish / event logのreaderとwriterを20万件の生成入力で比較し、yamlishの読み方が変わるのは修正前が読めなかった入力（45,444件）だけで、全60万件で修正後の読み方は「修正後が行に残した3文字をescapeしたtextを修正前が読んだもの」と一致し、3文字を含まないtext（290,828件）の読み方と、3文字を含まない値の出力bytes（47,393件）は同一、修正後の出力は修正前のreaderでも、修正前の出力は修正後のreaderでも読めた。event logでは、修正前が読めなかったJSON文字列の中の3文字（16,839件）が読めるようになり、JSON文字列の外にLF以外の行境界がある手編集の形（994件）だけがmalformedになり、2つの異なる読み方になる入力は0件。直接の回帰: `tests/test_cancel_decision_resume.py`（BL-030）（28 passed / 69 subtests）、`tests/test_yamlish_durable.py`・`tests/test_mutation.py`・`tests/test_registry.py`・`tests/test_historical_related.py`（70 passed / 1 skipped / 23 subtests）。`validate-registry` はPASS。残る論点: (1) Work名に行境界（LFを含む）があると `Entity.name` が本文の読み戻しで切り詰められる（本件のframingとは別の、名前の表現の問題）。(2) `related.yaml` が読めない時にbackfill / pin maintenanceが `not_a_project` と報告する（既存の誤報）。(3) lone surrogateを含む文字列はwrite時に `UnicodeEncodeError` になる（有効なtextではなく別系統。BL-030は記録前に拒否する）。(4) 3文字をyamlishの行区切りとして使った手編集ファイルは従来どおり読み、quoted scalarの中だけが変わる。event logでLF以外の行境界をrecordの区切りに使った手編集は、Resolutionの(3)のとおりmalformedになる。

### BL-040 Cancel resume cannot reconcile a relation added earlier in the same START and removed by the cancel decision

- ID: BL-040
- Title: Cancel resume cannot reconcile a relation added earlier in the same START and removed by the cancel decision
- Status: RESOLVED
- Kind: implementation, recovery
- Severity: major
- Problem: STARTのDerive（HumanNGの派生を含む）は、派生するWorkとそのrelationを同じSTARTのmutationのstage（`<Work>:derive:<n>`）として記録・適用する。同じSTARTの後のcancel（BL-030の記録済みの決定）がそのrelation（R）を削除すると、recordには適用済みのRの追加と、後のstage（`<Work>:cancel:<n>:remove`）の同じrelation（同じID・同じrecord）の削除が並ぶ。Mutation Controllerは各effectを現在の正本とだけ比べて分類し（`MutationController.classify`）、`add_relation` はIDがfileに無ければ未適用とし、recordの `applied` も同じrecordの後の削除も見なかった。そのためRの削除を書いた時点から、(1) 中断した同じSTARTの再実行は、cancelの証明（BL-029から共有する `ops._recorded_progress`）が「Rの追加が未適用なのに後のeffectが適用済み」として `reconcile_required` にし、削除の途中・削除後・commit記録後・commit後push前・push後・完了前のどの窓でも、何度再実行しても同じで、pending recordがevent logとrelation fileを書く後続operationを止め続けた。(2) 中断が無くても、cancelのcommit stageの `Mutation.apply` がRを書き戻して削除し直し（Rの追加・削除が各2回）、その間に行う前のstageのpushの分類（`git push --dry-run`）が一時的に失敗するかkillされると、Rが書き戻されたまま同じ `reconcile_required` から抜けられなかった。single-work（Derive自身が追加し、cancelで削除が構造上必須になるreturn_toを含む）、outer（同じWork、および前のWorkが派生したWorkのcancel）、question waitをまたいで同じmutationを再開した場合で同じ。
- Why it matters: BL-030のdurableな決定があるのに、Deriveの後に同じSTARTでcancelするという通常の流れが、cancelの削除を書いた後の中断から回復できず、中断が無くても一時的なnetwork失敗やkillだけで永久に止まる。書き戻されたRは構造違反（`return_to_unreplanned` / `dependency_unreplanned`）としてworking treeに残り、人がRを記録どおりに戻してもresumeできない。
- Likely scope: Mutation Controllerの分類規則として、同じrecordの適用済みの追加と後のstageの同じrelationの削除の対を示せる時だけ追加を適用済み・一致とし、`Mutation.apply`・`unapplied_effects`・`ops._recorded_progress` が共有する（investigationのCandidate B）。START側の証明だけを変えること（Candidate A / C）と、決定へownership情報を足すこと（Candidate D）は採らない。BL-030の決定・証明・view、BL-029のhelper、BL-038のbranch検査、`INTENT_VERSION`・effect種別・recordの形は変えない。
- Cross-project impact: あり（全ProjectのSTARTで、Deriveの後に同じSTARTのcancelが派生relationを削除する場合の回復と、中断の無いcancelの書き込み）
- Backfill likely: no（止まっていたpending recordは、同じSTARTの再実行でそのまま継続する。自動repairはしない）
- Human confirmation likely: yes（`rules/git` のMulti-write mutationに追記。削除自体の適用済みを条件にしないこと、削除を記録した後に人がRを消した場合の帰結、成功へ昇格させないものは採用時に決定済み）
- Self-hosting prerequisite: no
- Evidence class: code inspection, reproduction
- Resolution: 原因は、Mutation Controllerの分類が各effectを単独で現在の正本と比べ、同じmutationが先に追加して後のstageで自分で削除したrelationを、正本に無いという理由だけで未適用としたこと。BL-030の決定・`_own_effects_free_view`・削除snapshotの照合は正しく働いていた。修正は `src/workline/mutation.py` のmodule関数 `_classify_recorded` と、3つの分類箇所のその呼び出しへの置き換えだけで、ownerのコードと `MutationController.classify` は変えていない。(1) 規則: `classify` が未適用とした `add_relation` は、recordがそれを `applied: true` として持ち、かつrecordでそれより後にあり別のstageにある `remove_relation` のpayload（relation fileと、IDとすべてのfieldを含むrecord）が追加のpayloadと一致する場合だけ、適用済み・一致とする。削除は正本がそのrelationを記録どおりに持つ時にしか記録されない（`validate_effect`）ので、その時点で追加は書かれており、relationが無いのはその削除による。削除自体の `applied` は条件にしない（採用時の決定。削除を書いた後、適用済みを保存する前の中断も収束する）。規則が読むのは、すべての記録済みeffectが持つstage・kind・payload・`applied` と記録の順序だけで、新しいfieldは使わない。(2) 共有: `Mutation.apply` の分類、`unapplied_effects`（BL-038の `_require_finalized_branch` と、CREATE / Phase CREATEの記録前の構造投影が使う）、`ops._recorded_progress`（BL-029のplan exclusionとBL-030のSTART cancelの証明が使う）が同じ関数で分類する。START専用の分岐やBL-030専用のhelperは足していない。(3) 結果: 削除を書いた後の中断（削除の途中、削除を書いて適用済みを保存する前、削除後、commit記録後、commit後push前、push後、完了前）からの再実行は、executorを呼ばず残りのeffectだけを実行して `cancelled` になり、Rを書き戻さない。中断の無いcancelはRの追加・削除を各1回だけ実行する。cancelのcommit stageで前のstageのpushの分類が一時的に失敗してもRは書き戻されず、次の再実行で収束し、再実行中に同じ失敗が起きても次の再実行で収束する。(4) 採用時の決定の帰結: 削除を記録した後・適用する前に人がRを記録どおりに消した場合は、削除を書いて適用済みを保存する前の中断とrecordでもProjectでも区別できず、同じく `cancelled` になる（開始前からあるrelationを削除する同形のcancelでも従来から同じ）。(5) 成功へ昇格させないもの（いずれも何も書かずに停止し、recordを変えない）: 同じtype / from / toで別IDのrelationを人が足した場合は、START precheckの `structure_invalid`（重複edge）で止まりそのrelationを残す（開始前からあるrelationを削除する同形のcancelと同じ。修正前は証明の `reconcile_required` で、どちらも停止）。同じIDを別typeで戻した場合、削除が記録される前にRを消した場合、cancelが削除しない派生relationを消した場合、Rを記録どおりに戻した場合、追加を適用済みと記録していないrecord、削除のsnapshotが追加と違うrecord、決定を持たない旧recordは、修正前と同じ `reconcile_required`。Mutation Controllerの規則としても、別IDの削除、snapshotの違う削除、追加より前や同じstageの削除、適用済みと記録していない追加は、従来どおり未適用に分類する。(6) 他のBLとの関係: BL-030の決定・証明・view、executorを再実行しないことは変えていない（`_own_effects_free_view` にはcancel自身のstageの適用済みeffectだけを渡すので、Rは削除snapshotから戻る）。BL-029のplan exclusionのrecordは同じrelationの追加と削除を持たないので規則は発火しない。BL-038の検査は `unapplied_effects` を通じて同じ規則を使い、書き戻さない追加を書き直すeffectと数えない。commitを含まないbranchでの再実行は、他の適用済みeffect（event等）を書き直すことになるので従来どおり何も書かずに止まり（STARTのcancelはその前に証明で止まる）、記録したbranchへ戻れば収束する。`INTENT_VERSION`、effect種別・payload・effect recordのkey、closeしたrecordのfield、invocation、`Mutation` / `MutationController` の公開methodは変えていない。測定: BL-038 landing後の `3f6a916` で、investigationのharnessの代表18 jobと実processのkill 2件を再実行し、raise位置の行番号（BL-038の追加行）とharness側の読み取りだけの観測1項目を除いてinvestigation時と一致した（欠陥は残っていた）。新しい `tests/test_added_then_removed_relation.py`（16 tests / 42 subtests: 中断の無いcancelがRを1回ずつ書くこと（single-work / return_to / outer / 派生Workのcancel）、cancelのcommit記録後のpush分類の一時失敗でRが書き戻されず次の再実行で収束すること、削除の途中から完了前までの7窓とreturn_to・outer・派生Workのcancelの再実行が残りだけを実行して収束すること、削除を書いて適用済みを保存する前の中断、再実行中のpush分類の失敗の後の収束、commitを含まないbranchで何も書かず止まりmainで収束すること、削除記録後に人がRを消した場合が同形のcancelと同じく収束すること、別IDの同形relation・同じIDの別type・recordで説明できない人の変更4種・適用済みでない追加・snapshotの違う削除・決定の無い旧recordが何も書かず止まること、対の無い対照（開始前からのrelationの削除、残す派生relation、別mutationが追加したrelation）が従来どおり収束すること、Mutation Controllerの規則の境界（削除のflag未保存、既存のfieldだけのrecord、適用済みでない追加、別snapshot、別ID、同じstage、追加より前の削除）と、BL-038の検査がRではなく書き直す他のeffectで止まること）は修正後すべて成功し、`3f6a916` では27件（4 tests、23 subtests）が失敗した（成功したのは変えてはいけない停止・対照・規則の境界のpin）。規則を1つずつ壊したmutant 4本はすべて検出した（後の削除を見ない: 7 subtests（残す派生relationを人が消した場合とsnapshotの違うrecordが成功する等）、削除の `applied` を要求する: 4件、snapshotを比べずIDだけで対にする: 2 subtests（snapshotの違うrecordが成功する等）、順序とstageを見ない: 2 subtests）。productionのA/B（investigationのharness、`3f6a916` と修正後、38 job）: 対照・同形のcancel・人の変更・旧recordの13 jobは全stepの観測が一致。16 jobは `reconcile_required` から `cancelled` へ変わった（削除の途中から完了前までの窓、中断の無いpush分類の失敗、再実行中の二重障害、return_to、outer、派生Workのcancel、question waitをまたぐ同じmutation、採用時に決めた削除記録後の人の削除）。5 jobは両方成功し、Rの（派生Workのcancelでは2つの派生relationの）重複した書き込みだけが無くなった。2 jobは両方停止で理由が `reconcile_required` から `structure_invalid` に移り（別IDの同形relation）、2 jobは同じ停止で1回目の実行の重複した書き込みだけが無くなった。実processのkill: 削除後（fsmonitor hook）とcancelのcommit後push前（pre-push hook）は再実行で `cancelled`。cancelのcommit stageが前のstageのpushを分類する瞬間（pre-push hook）のkillは、修正前はRが書き戻されたまま再実行が `reconcile_required`、修正後はRが無く再実行で `cancelled`（対照は両方収束）。直接の回帰（同じ `src` で計測）: `tests/test_plan_exclusion_resume.py`（BL-029、26 passed / 50 subtests）、`tests/test_finalized_effect_branch.py`（BL-038、14 passed / 23 subtests）、`tests/test_registration_projection.py`（23 / 36）、`tests/test_terminal_finalization_resume.py`（21 / 13）、`tests/test_replan_registration.py`（18 / 14）、`tests/test_project_start_recovery.py`（公開methodのpinを含む、37 passed / 1 skipped / 107 subtests）、`tests/test_mutation.py`（14 passed）。`validate-registry` はPASS。`rules/git` のMulti-write mutationに規則を追記し、`skills/start` のcancel再実行の記述から参照した。残る論点: (1) 削除を記録した後・適用する前に人がRを消した場合は収束する（採用時の決定）。(2) 同じrelationの追加と削除をまとめて `_own_effects_free_view` に渡すと、削除snapshotからRを戻してしまう（現在の呼び出しはcancel自身のstageだけを渡し、plan exclusionのrecordは対を持たないので到達しない）。(3) 対とそのcommitだけを持ち、他に書き直すeffectの無いrecordを、commitを含まないbranchで再実行すると、書き直しが無いのでBL-038の検査は止めない（Workline自身はこの形のrecordを書かない。STARTの対は常に同じcommitが確定するevent・Work file等と一緒に記録される）。(4) recordとProjectを整合的に書き換えた人の編集は検出しない（BL-030と同じ）。

### BL-041 Operation performs effects before refusing a pre-existing dirty overlap, leaving an unrecoverable mutation

- ID: BL-041
- Title: Operation performs effects before refusing a pre-existing dirty overlap, leaving an unrecoverable mutation
- Status: RESOLVED
- Kind: implementation, recovery
- Severity: major
- Problem: START・Roadmapのlifecycle決定（hold / resume / cancel / 達成）・plan exclusion（Phase / Work / standalone Work）は、開始時にdirtyだったpathの名前だけを `preexisting_dirty` noteとしてmutationごとに1回記録し、それとcommit対象の重なりをGit段階（`gitops.finalize` の `ensure_separable`）で初めて検査していた。そのため開始前から `events/events.jsonl` に人の未commit変更（合法なevent行・空行・書式変更）があると、中断なしの1回目の実行でも、STARTはWork開始のlifecycle eventを追記し、executorを実行し、成果commitをpushし、完了eventを追記してから `dirty_overlap` で停止し、Phase holdはeventを追記してから、plan exclusionはeventとreplan（新Work file・relation）を適用してから停止した。noteはpath名だけで以後更新されないので、人が変更を戻しても、event logを捨ててreplayさせても、自分の変更だけを別にcommitしても、同じ再実行は必ず同じ `dirty_overlap` になり（収束したのはrecordのnoteを手で書き換えた診断だけで、それは人の行を横取りしてcommitした）、recordを消すだけでは次のoperationに同じ停止が移った。stuck recordとwrite scopeが重なる無関係なSTART・Phase hold・CREATEは `reconcile_required` で止まり、`validate_project` とCLIの `validate-project` はPASSのままだった。Hold outcomeでは再実行ごとにexecutorが再実行され、event 4件とrecordのeffect 4件が増え続けた（4→8→12→16→20）。replanが書く `roadmap.yaml` / `related.yaml` に開始前の変更があるplan exclusionも、eventと登録を適用してから同じ停止をした。実測（investigation `a6783df`、36 scenario / 407観測、実process kill（`core.fsmonitor` hook）を含む。BL-038 landing後の `ae0466d` で代表20 jobを再測定し、investigationと共通の154 stepは正規化した観測が完全に一致し、plan exclusion（Roadmap-owned / START-owned、event log / roadmap.yaml）の遅い停止を新たに実測した）。一方、開始前の変更を含む作業ツリーの上でoperationが判断しているので、人の変更を残して自分の分だけをcommitすると、自分の前提条件が禁じる履歴を公開しうる（人が手で追記したPhase resumeの上でWorkを完了した場合、分離commitは「Phase held・Work completed」を公開した）。停止そのものではなく、停止の位置と解けないrecordが欠陥だった。
- Why it matters: 中断すら無い通常の実行で、executorの実行・成果commitのpush・terminal eventの適用という戻せないeffectの後に止まり、どの作業ツリー操作でも解けないmutationが残ってledgerを書く全operationを塞ぎ、validateは何も示さない。引き金は人の手編集のほか、旧implementationのfalse successが残した未commit eventや、stuck recordを消した後の残骸でも生じる。
- Likely scope: 開始前の変更がoperationのcommit対象と重なり分離できない場合に、最初のdomain effectより前に拒否する（investigationのCandidate D）。START・lifecycle決定・plan exclusionを対象とし、同じ遅い検査を使う他のownerの総当たりはしない。hunk分離による救済、開始時の内容snapshotの追加、既存stuck recordの自動救済はしない。
- Cross-project impact: あり（全Projectの、開始前に未commit変更があるoperation）
- Backfill likely: no（既に停止しているrecordは自動repairせず、従来どおりGit段階で停止する）
- Human confirmation likely: yes（`rules/git` のCommit / pushとSTART・Roadmap Skillに追記。対象範囲、legacy recordを推測でresumeしないこと、開始後の他人の変更（F0）を含めないこと、hunk分離をしないことは採用時に決定済み）
- Self-hosting prerequisite: no
- Evidence class: code inspection, reproduction
- Resolution: (1) 検査: `src/workline/gitops.py` の `ensure_separable_before_effects(mutation, paths)`。effectを1件も記録していないmutationについて、Git段階と同じ開始時snapshot（`record_preexisting_dirty` が記録したnote）と `paths` を `ensure_separable` で比べ、重なればGit段階と同じ `dirty_overlap`（同じmessage）で停止し、そのmutationをabandonする。event・executor・正本write・commit・pushは何も行わず、pending recordを残さない。effectを記録済みのmutationには何もしない。(2) 位置と `paths`: STARTは `_Session.run_work` で、前提条件（terminal Work、Phase / Roadmapのlifecycle、dependency、read target、戻せないread target）を判定した後、write scopeの記録とWork開始のlifecycle eventより前に、event logで検査する（STARTはexecutorが何を返してもevent logをcommitし、question waitはそのcommitを後に回すだけのため）。lifecycle決定は `roadmap._record_lifecycle` でeventを記録する前にevent logで検査する（hold / resume / cancel / 達成の共通経路）。plan exclusion（`roadmap._plan_exclude_locked`、`start._plan_exclude_standalone_locked`）は、replanのID予約と投影検査の後、eventを記録する前に、`ops._plan_exclusion_ledgers` がreplanから決めるledger（event log、relationを追加・削除するならroadmap.yaml、新WorkがRelatedを持つならrelated.yaml）で検査する。新Workと派生detailのfileは予約したIDで作るので、開始前の変更と重ならない。各前提条件のSTOPは従来のcodeのまま先に出る。(3) 採用時の決定: noteに内容やhashを足さない（新しいoperationは開始前に拒否するので不要）。同じfileの人の変更をhunk分離しない。effectを記録済みの未完了mutation（修正前のimplementationが残したstuck recordを含む）は推測でresumeせず、従来どおりGit段階で `dirty_overlap` とする。開始時はcleanで開始後に他人がevent logへ書いた変更がcommitに入る件（F0）は扱わない。noteを保存しeffectを記録する前に中断したrecordは、再実行がそのnoteで拒否してabandonする（再開するeffectが無いため。noteの保存と検査の間で中断しても、effectを持つstuck recordは作られない）。(4) 変えないもの: 開始前の変更がcommit対象の外にある場合（STARTでのroadmap.yaml / related.yaml / 無関係なfile / Work file、lifecycle決定でのrelation file、plan exclusionでreplanが書かないledger）、cleanなoperation、resume（BL-031のterminal finalization、BL-030のcancel、BL-029のplan exclusion、BL-038の別branchでの停止）、Git段階の分離検査と `ops._refuse_before_replay`、Project開始・backfill・push pin、同じ遅い検査のままのPhase entry・Roadmap作成・Phase追加・Related maintenance・direct CREATE。`INTENT_VERSION`、record / effect / payload / invocation / noteの形、`Mutation` / `MutationController` の公開methodは変えていない。(5) 狭めた挙動（採用時に予測・開示済み）: event logに開始前の変更があるSTARTで、従来1回目だけ返していた `question_wait`（そのresumeは必ず `dirty_overlap`）と、executorの後の拒否（completion precheck等）は、executorを呼ぶ前の `dirty_overlap` になる。stuck recordが無くなるので、それに止められていた無関係なoperationは自分自身の判定に従う（event logを書くものは開始前の `dirty_overlap`、書かないdirect CREATEは成功）。測定: 新しい `tests/test_preexisting_change_preflight.py`（21 tests / 9 subtests: event logに開始前の合法なevent行・空行があるSTARTが、effect・executor・変更・pending recordなしに拒否され、再実行もrecordを残さず、人がその変更をcommit / 破棄すると完了すること、hold・question wait・cancel・outerのどのoutcomeもexecutorより前に拒否されること、terminal Work・held Phase・未完了dependencyの拒否が従来のcodeで先に出ること、STARTのcommit対象外（無関係なfile・roadmap.yaml・related.yaml）とcleanは従来どおり完了して人の変更をそのまま残すこと、Phase holdがevent logの変更で拒否されrelation fileの変更では進むこと、plan exclusionがevent log（Roadmap-owned）・replanが書くroadmap.yaml（START-owned）・Relatedを持つ置き換えのrelated.yamlの変更で拒否され、replanが書かないledgerの変更では進むこと、修正前の動作で停止したSTARTとPhase holdのrecordは、人が変更を戻しても従来どおりGit段階で停止しrecordを変えないこと、noteだけを保存して中断したrecordは再実行でabandonされ、変更をcommitすると完了すること、BL-031のfinalization・BL-030のcancelの中断と、BL-038の別branchでのresumeが従来どおりであること、既知の残る論点（executorが返す成果path、修正前のHold recordの増殖、開始後の変更F0）のpin）は修正後すべて成功し、`ae0466d` では8 tests（うち1 testは4 subtests）が失敗した（成功した13 testsは変えてはいけない挙動と残る論点のpin）。mutant 4本（検査を外しGit段階だけにする: 11件、STARTだけ直しlifecycle決定とplan exclusionを外す: 4件、commit対象との重なりを見ずに開始前の変更があれば拒否する: 5件（commit対象外の変更のpin 4件と成果pathのpin）、STARTの判定をWork開始のeventとexecutorの後にする: 9件）はすべて検出した。既存pinの更新: BL-031の `tests/test_terminal_finalization_resume.py`、BL-030の `tests/test_cancel_decision_resume.py`、BL-029の `tests/test_plan_exclusion_resume.py` で、開始前の変更の上を1回目が進んで作ったrecordを前提にしていた3 testは、1回目がeffectより前に拒否されrecordを残さないことを加えたうえで、同じrecordを修正前の動作（この検査を外す）で作り、従来どおりGit段階・replayの前で停止することをpinする。investigationのharnessの代表20 job（START完了・cancel・question wait・Hold、Phase hold、plan exclusion 3種、成果path、Deriveのroadmap.yaml、commit対象外の変更4種、clean control、実process kill 2本）を `ae0466d` と修正後で実行した: 対象行は、effectを実行した後の `dirty_overlap`（question waitは、そのresumeが必ず行き着く `dirty_overlap`）から、何も実行せずpending recordを残さない `dirty_overlap` になり、人が変更を戻す・破棄する・自分の変更だけをcommitすると同じoperationが完了し、Hold再実行のeffect増殖（4→20）は起きず、stuck recordに止められていた無関係なSTART・Phase holdは自分の開始前の判定で止まり、direct CREATEは成功した。clean control・commit対象外の変更・成果path・Deriveのroadmap.yaml・cleanな実process killの68 stepは観測が完全に一致した。入力境界のA/B（16 operation × 開始前の変更7種 = 112行、1行1 Project）: 80行は完全一致（commit対象外の変更での成功74、前提条件が先に出る拒否2、executorが決めるpathと対象外ownerの遅い停止4）、32行が対象（effectの後の `dirty_overlap` 30行と `question_wait` 2行が、何もしない `dirty_overlap` になった）、それ以外の差分は0行。full suite: 最終tree（BL-040の `ea6c697` と本変更）で1 test file 1 processに分けて実行し、916 collected、915 passed・1 skipped（file symlink）、986 subtests passed・1 subtest skipped、失敗0（全41 fileのexit 0、実行の前後でtreeが同一）。BL-040を含まない `70ff58d` と `3b9e563` 上の同じ変更でも、900 collected・899 passed・1 skipped・944 subtests passed・失敗0だった。 `validate-registry` はPASS。`rules/git` のCommit / pushに、開始前の変更がcommit対象と重なる場合に最初のdomain effectより前に停止すること、hunk分離で救済しないこと、effectを記録済みのmutationに後から適用しないことを追記し、START Skillの「安全にhunk分離できる時だけ分離する」を同じ規則に揃え、Roadmap Skillから参照した。残る論点: (1) executorが返す成果path（開始前の人のfileをexecutorが上書きした場合）と、executorのDerive / cancelが書くrelation fileは、executorが返すまで分からないので、従来どおりその時点（成果commit・派生commitより前）で停止し、Work開始のlifecycle eventを適用済みのrecordは再実行ごとにexecutorを再実行して止まる。(2) 開始時clean・開始後に他人がevent logへ書いた変更は、STARTのcommitに入ってpushされる（F0。開始時snapshotの範囲外）。(3) 修正前に停止したHold outcomeのrecordは、再実行ごとにexecutorを再実行しeffectが4件ずつ増える（自動repairしない）。(4) Phase entry・Roadmap作成・Phase追加・Related maintenance・direct CREATEは、Git段階の遅い検査のまま（対象外）。(5) `dirty_overlap` のmessageは、何もせずに止まった場合とGit段階で止まった場合で同じ。

### BL-042 START destroys a pre-existing dirty result path before refusing it, then leaves a mutation that cannot converge

- ID: BL-042
- Title: START destroys a pre-existing dirty result path before refusing it, then leaves a mutation that cannot converge
- Status: RESOLVED
- Kind: implementation, recovery
- Problem: STARTの成果path・削除pathと、executorが返した派生の登録が書くrelation fileは、executorが返すまで分からないため、BL-041が最初のdomain effectより前に行う検査（event log）の対象外だった。STARTはWork開始のlifecycle eventを適用したうえでexecutorを実行し、executorは開始前から未commitだったそのpathを上書き（またはtracked fileを削除）してから `dirty_overlap` で停止する。停止時点でmutationはeffectを持つためabandonできず、開始時snapshotはmutationの生存中固定されるので、人がそのpathをcommitしても破棄してもworking treeを完全にcleanにしても同じ拒否が続き、再実行のたびにexecutorがその人のfileをもう一度上書きし、派生ではWork・派生detail・relation・effectが再実行ごとに増え、そのProjectでledgerを書く全operationが `reconcile_required` で止まる。`validate` は何も示さない。
- Why it matters: 中断の無い通常の実行で、人の未commitの作業がWorkline都合で消える（`rules/git` 基本原則の「既存untrackedファイルの編集・上書き・削除は人間確認へ返す」に反する）。しかもその後どう片付けても同じoperationを終えられず、Project全体が塞がる。
- Likely scope: executor実行前に開始時snapshotのpathの内容を読み、成果path・削除path・派生が書くrelation fileと重なった場合は成果・派生の適用より前に、その内容を戻してから停止する。派生はWork file・派生detail・relationを1件も記録・適用する前に判定する。開始時snapshotは広げないが、runが何かを書くより前に、現在は変更されていないpathを落とす。停止時にexecutorが返した成果と重なったpathの内容をrecordへ保存し、再実行ではexecutorを再実行しない。
- Cross-project impact: あり（全Projectの、開始前に未commit変更があるWork実行）
- Backfill likely: no（開始前の変更と自分の変更が同じpathにある記録済みmutationは推測で直さない）
- Human confirmation likely: yes（`rules/git` のCommit / pushとSTART Skillに追記）
- Self-hosting prerequisite: no
- Evidence class: code inspection, reproduction
- Resolution: (1) 保護と復元: `start._Session._protect_preexisting` が、そのsessionのexecutorを1つも動かす前に、開始時snapshotが名指すpathの内容とmodeを1度だけ読む（存在しないpathは「存在しない」として読む。directory・link・読めなかったpathは持たない）。成果path・削除path、または派生の登録が書くrelation fileがsnapshotと重なった場合、`_refuse_overlapping_result` が、重なったpathだけをその内容へ戻してから同じ `dirty_overlap` で停止する。戻したpath・戻せなかったpathはmessageに出す。working tree全体や他の変更は戻さない。(2) 派生の判定位置: `_derive` は `_derive_ledgers`（relationがあれば roadmap.yaml、新WorkがRelatedを持てば related.yaml。Work fileと派生detailは予約IDなので重ならない）で `register_works` より前に判定し、重なればWork file・派生detail・relationを1件も記録・適用しない。(3) 収束: `gitops.narrow_preexisting_dirty` が、STARTのrunが何かを書くより前に、開始時snapshotから現在は変更されていないpathを落として保存する。落とすだけで足さないので、executor自身の出力が開始前の変更に昇格することはない。mutation自身が書いたpathであることを理由には落とさない（snapshotはpathだけを持ち、その中の人の変更と自分の変更を区別できないため）。(4) 成果の再利用: 停止時に、executorが返した成果（completed / derive）と、重なったpathにexecutorが残していた内容を `refused_result` noteへ保存する。再実行は、重なったpathがまだ変更されていれば何も書かずに同じ `dirty_overlap` で停止し、人がcommit / 破棄していれば保存した内容を書き戻して同じ成果で続け、executorを再実行しない。保存した成果は、その成果commitまたは派生の登録が済んだ時点で捨てる。recordがそのまま保ちきれない成果（合計1 MiB超、読めない、read-backが一致しない）は保存せず、従来どおり再実行でexecutorへ問い直す。noteが読み戻せない形なら何もせず `reconcile_required` で停止する。(5) 変えないもの: BL-041の最初のdomain effectより前の拒否（event logの変更はexecutorを1度も呼ばず、recordも残さない）、BL-030のcancel（成果pathがdirtyでもcancelは成功し、replanのledgerがdirtyなら従来どおりGit段階で停止してexecutorを再実行しない）、成果pathがdirtyでも成功するHold、BL-020（`work_started` / `work_target_added` を取り消さない。F2は不採用）、開始時snapshotのpathを触らないexecutorの成果、`INTENT_VERSION`・record / effect / payload / invocationの形、`Mutation` / `MutationController` の公開method。開始前の変更と自分の変更が同じpathにある記録済みmutationは、修復も再開もせず、成長もしない。(6) 測定: 新しい `tests/test_result_path_protection.py`（26 tests）は、人のuntracked draft・tracked modification・未commitの削除・削除成果の対象が停止後もそのままであること、触られなかった変更がそのまま残ること、cleanな成果が従来どおりcommitされること、人がcommit / 破棄 / checkoutした後に同じSTARTが完了すること、working treeが完全にcleanでも古いpath名だけでは停止しないこと、片付ける前の再実行は何も書かずに停止し人のbytesがそのままであること、executorが拒否・再実行・完了を通じて1度しか呼ばれないこと、commitされた成果がexecutorの返したものそのものであること、outer modeで完了済みWorkのexecutorが呼ばれないこと、派生が拒否前にWork・relation・effectを1件も増やさず3回の再実行でも増えないこと、人がcommitした後に保存した成果で続きexecutorを呼び直さないこと、cleanな派生が従来どおり登録されること、Hold・Cancel・BL-041・BL-020・executorが成果を書いて失敗した後の再開が従来どおりであること、修正前に停止したrecordが修復も成長もせず人の変更をcommit / pushしないことを固定する。mutant 5本（復元を外す、snapshotを固定のままにする、snapshotを現在のdirtyから取り直す、派生の判定を外す、保存した成果を使わない）はそれぞれ別のtestが検出した。`validate-registry` はPASS。`rules/git` のCommit / pushとSTART Skillへ反映した。(7) 残る論点: 成果が1 MiBを超える場合と、executorが成果を書いた後にprocessが落ちた場合（復元はrefusal時に行うため）は、従来どおり再実行でexecutorを呼び直す。成果pathの重なり以外の理由（completion precheck等）でexecutorの後に停止した場合、そのexecutorの書き込みは従来どおりそのまま残る。開始前の変更と自分の変更が同じpathにある修正前のrecordは、人がそのfileをcommitすると、その時点から続きが進む（その記録済みの登録は取り消さない）。

### BL-043 Operation commits, erases or adopts a change another subject makes to an owned path after it begins

- ID: BL-043
- Title: Operation commits, erases or adopts a change another subject makes to an owned path after it begins
- Status: RESOLVED
- Kind: implementation, recovery / ownership
- Severity: major
- Problem: operationは、自分のeffectがそのfileのどこかへ書いたというだけでpathをowned扱いし、Git段階（`mutation.apply_effect` の `git_commit` → `gitcmd.add_paths` + `gitcmd.commit_only`）は渡されたpathにworking treeが持つものをそのままcommitしていた。`ops._owned_paths` はpath名だけを持ち、`start._commit` はそれを `changed_against_head`（HEADと違うか）で絞るだけで、「その違いが自分のものか」は問わない。唯一のdirty機構である `preexisting_dirty` は開始時に1回だけ取るpath名のsnapshotで、BL-041はそれを最初のeffectより前へ移しただけだった。そのため、開始時cleanだったpathへoperation開始後に他者・別AIが書いた変更は、中断が1つも無い1回目の実行で、operation自身のcommitへ入り、承認済みremoteへpushされ、成功として返り、`validate_project` もcleanと報告した。investigation（`f2bb8f5` 上、`D:\AIproject\workline-core-bl042-inv`）の実測: 開始eventの直後・executorの最中・完了eventの直後・commit記録後のどの窓でもcontaminateし（F0a–F0d）、outer STARTでは先行Workのcommitより後に入った変更を後続Workのcommitが吸収した（F0e）。path種別もevent log・executor成果file・relation ledgerで同じで、非STARTのRoadmap Phase holdも同じだった（G1。共有する `gitops.finalize` のGit段階が原因）。最悪の3つ: **P1** executorが返してから成果commitまでの間に成果fileが書き換えられると、他者のbytesが「成果commit」としてcommit・pushされ、Workline自身の成果内容はそのcommitから失われたまま、STARTは `completed` を返す。**E1** ledgerの再renderが再現できない他者の行を黙って消し、その削除を自分のcommitとしてpushする。**E2b** 妥当なULIDを持つ他者のrelation recordを再renderが自分のrelation setへ取り込む。さらにF0d×不正行はcommit・pushの後に `events_invalid` になり、remoteのcloneが `structure_unreadable` で壊れた。他者の `work_held` 1行がremoteのcloneで `unstarted` → `held` を正史にした。
- Why it matters: 中断の無い通常の実行で、人・別AIがoperation中に触れたfileがWorklineの成果としてcommit・pushされ、消され、または自分のstateとして取り込まれる。`rules/git` 基本原則の「他者・別AIの変更をWorkline都合で失わせない」「現在operationが責任を持つ変更だけをstage / commitする」「ownership不明変更の破棄は自動実行しない」と、START Skillの「commit直前にdiffを再確認し、START-owned changeだけをstageする」のすべてに反し、何の警告も出ない。P1は他者のbytesの混入にとどまらず、operation自身の成果についてのfalse successである。BL-041が採用時にF0として明示的に範囲外とした残件。
- Likely scope: Worklineが自分で書いたbytesのcontent identityをdurableに持ち、stage / commitの直前に現在のbytesと比較し、一致しなければstage・commitせずwrite-freeの `reconcile_required` で停止する（investigationのCandidate A-digest）。同じfileの中の他者の変更と自分の変更をhunk等で分けて自分の分だけcommitするCandidate BはBL-041が書いた `rules/git` の規定で禁止。event logだけを対象にするCandidate Dは狭すぎる（P1を残す）。既にpush済みの履歴の書き換えと、既にcontaminateしたrecordの自動repairはしない。
- Cross-project impact: あり（全Projectの、operation実行中にworking treeへ触れられたoperation）
- Backfill likely: no（既に公開されたcommitは履歴であり書き換えない）
- Human confirmation likely: yes（`rules/git` のCommit / pushとSTART / Roadmap Skillに追記。path ownershipがcommit ownershipではないこと、書込み前とcommit前の両方で確認すること、hunk分離をしないこと、内容の記録を持たないlegacy recordを推測で拒否しないこと、同一bytesは区別しないことは採用時に決定済み）
- Self-hosting prerequisite: no
- Evidence class: code inspection, reproduction
- Resolution: (1) 書いた内容の記録: `src/workline/mutation.py`。Project正本fileを書くeffect（`write_file` / `add_relation` / `remove_relation` / `append_event`）は、stageを記録する同じdurable write（`Mutation.add_effects`）でそのpathが書く前に持っていた内容 `held_before` を記録し、適用する時（`Mutation.apply`）は書く直前のdurable writeでこれから書く内容 `wrote` を記録してから書く。内容はworking treeのbytesのSHA-256（`_content_digest` / `_text_digest`。linkは指す先、無いpathは `absent`）で、`durable_write_text` がUTF-8・改行変換なしで書いたbytesと同じlayerで比較するので、`core.autocrlf` や `.gitattributes` の変換がfalse positiveを作らない。書く内容は `MutationController._planned_write` が1箇所で作り、`apply_effect` はそれをそのまま書くので、記録した内容と書いたbytesが別々のrenderingで食い違うことはない。effectが書かないのにcommitするpathは `declare_own_content(mutation, paths)` がmutation note `own_content` へ記録する（effectが書くpathは飛ばし、読めないpathは `unreadable` として後でどの状態とも一致させず、記録が変わらない時は保存もしないので、そのまま停止するrunはrecordを1 byteも変えない）。(2) 書く直前の確認: `_require_own_bytes_before_write`。未適用のfile effectを書く前に、そのfileが現在もこのmutationが最後にそこへ書いた内容（そのpathへの最初の書込みの前はそのstageの `held_before`）を持つことを確認する。自分が書いた内容がsnapshotより優先されるので、他者の変更の後に取ったsnapshotが自分のbytesの代わりになることはない。commit直前の確認だけでは足りないのは、event logへの追記もledgerの再renderも、fileに既にある他者の変更を取り込んでしまい（parseできるrecordは自分のrelation setへ取り込み（E2b）、再現できない行は消す（E1））、その後はfileが期待どおりになってしまうためである。(3) commit直前の確認: `_require_own_bytes_committed`。`_make_commit` の直前に、そのcommitがHEADと違う状態で運ぶpathのそれぞれについて、`wrote`（なければ `own_content`）と現在の内容を比べる。HEADと同じpathは何も運ばないので確認しない。自分が書いたのでもなく記録も無いのにHEADと違うpathも同じく拒否する（このoperationがしていない変更のため）。(4) 停止の仕方: 一致しなければ `ReconcileRequired`（`reconcile_required`）で `add_paths` / `commit_only` の前に停止する。stage・commit・pushをせず、その変更を書き戻しも削除もせず、recordを変えない。hunk分離はしない（BL-041が `rules/git` へ書いた規定）。人がその変更をcommit / 破棄するか、pathをoperation自身が書いた内容へ戻せば、同じoperationの再実行がそこから進む。他者が自分と同一のbytesを書いた場合は内容として区別できないので成功する（契約として明記）。(5) ownerの記録: STARTの `_complete` は、executorが返した直後（成果path・削除pathが分かる最初の瞬間、`completion_precheck` より前）に成果path・削除pathの内容を記録する。Project開始と bootstrap backfillは、bootstrap Skill fileがownedでどのeffectも書かない場合（既にそのoperationが書くはずの内容そのものを持つuntracked file）に同じく記録する。`ops._owned_paths` は新しい `mutation.effect_path` を使い、operationが「自分が書くpath」と言うものとMutation Controllerが「自分が書いた」と示すpathが1箇所から出るようにした。(6) legacy: 内容の記録を持たないrecord（この変更より前のimplementationのもの）と、そのpathを書いたeffectが内容を記録していないpathは、推測せず従来どおり扱う（BL-035 / BL-036 / BL-037と同じ方針）。`INTENT_VERSION` は上げていない（上げると既存のpending recordがすべて読めなくなるため）。(7) 変えないもの: 開始前からの変更に対するBL-041の入口での拒否（`dirty_overlap`、同じcode・同じmessage・同じ位置）、BL-042の成果pathの保護（人のbytesを戻す拒否、`refused_result` として保存した成果の再利用、開始時snapshotの縮小）、BL-044の記録済みholdの読み戻し、BL-031のterminal finalization resume、BL-030のcancel、BL-029のplan exclusion、BL-040の「自分が足して自分が消したrelationを足し直さない」分類（人がそのrelationをledgerから手で消した場合だけ、書き込む前に止まるようになった。(8)）、BL-035 / BL-036 / BL-037 / BL-038のbranch・commit IDの判定、`INTENT_VERSION`、record / payload / invocation / stageの形、`Mutation` と `MutationController` の公開method（新しい `_planned_write` は非公開）。加えたのはfile effectの任意key `held_before` / `wrote` と、mutation note `own_content` だけで、どちらも持たないrecordも同じように続けられる。既に公開されたcontaminated commitの書き換えも、既にcontaminateしたrecordの自動repairもしない。(8) 狭めた挙動（開示）: operation開始後に他者・別AIがowned pathへ書いた変更があると、従来 `completed` / `cancelled` / `phase_held` / `phase_complete` を返してその変更をcommit・pushしていた実行が `reconcile_required` になる。それが本件の目的だが、従来acceptされていた入力に対する新しい拒否なので明示する。同じく、effectが書く直前にfileが自分の書いた内容でなければ、eventも成果もrelationも書かずに停止する（従来はその変更を取り込むか消していた）。人がledgerから、そのmutationがちょうど消すつもりだったrelationを手で消した場合も同じで、従来はその状態を取り込んで同じ終状態へ収束していた（BL-040の `DeletedByHandTests`）が、消したのがoperationではない以上、書き込む前に停止する。人がledgerをそのmutationが書いた内容へ戻せば、同じ再実行がそのrelationを自分で消して終わる（そのpinをそう書き換えた）。逆に、他者の変更が自分のcommit対象の外にある場合、自分のcommitより後に入った場合、自分が書いた内容へ戻された場合、自分と同一のbytesが書かれた場合は従来どおり成功する。(9) 測定: 新しい `tests/test_post_start_foreign_change.py`（23 tests）は、F0aの合法なevent追記と既存行の書換え、F0dの不正な行、P1のexecutor復帰後の成果書換え、P4のledgerへの追記、F0eのouter STARTで後続Workのcommitが吸収する経路、G1のRoadmap Phase hold、R1のcommit後・push前の再実行、E1の再renderが再現できない行、E2bの妥当なULIDを持つ他者のrelation recordを固定し、それぞれで（a）そのbytesがcommitにもremoteにも入らないこと、（b）消されずworking treeにそのまま残ること、（c）Worklineのstateへ取り込まれないこと（ledgerは1 byteも再renderされない）を確認する。変えてはいけない挙動として、他者の変更が無いSTARTの完了・commit・push・`validate` clean、commit前に取り消された変更、commit対象外のpath、自分のcommitより後に入った変更（成果pathの書換えを含む）、同一bytesの再write、自分が書いた内容へ戻した場合、拒否がrecordをbyte単位で変えずexecutorを再実行しないこと、内容の記録を持たないlegacy recordが従来どおり振る舞うこと、BL-041の `dirty_overlap`、BL-042の成果path保護と人のcommit後の完了、BL-031のterminal finalization resume、BL-044の記録済みhold resumeを固定する。A/B: `dc05950` に同じtest fileを置いて実行すると23件中10件が失敗し（F0a×2・F0d・F0e・P1・P4・G1・R1・E1・E2b）、13件は修正前後で同じに成功する。mutant 5本（commit直前の確認を外す: 4件、書込み直前の確認を外す: 6件、成果の記録を外す: 14件、stageのsnapshotを自分が書いた内容より優先する: 19件、stageのsnapshotを使わず自分が書いた内容だけにする: 2件（E1とE2bのみ））はすべて検出され、生き残りは無い。直接関係するsuite（`test_mutation`・`test_preexisting_change_preflight`・`test_result_path_protection`・`test_recorded_hold_resume`・`test_start`・`test_terminal_finalization_resume`・`test_cancel_decision_resume`・`test_plan_exclusion_resume`・`test_lifecycle_resume`・`test_roadmap`・`test_project_start`・`test_project_start_recovery`・`test_bootstrap`・`test_bootstrap_backfill_resume`・`test_create`・`test_registration_projection`・`test_replan_registration`・`test_related_maintenance`）は全件成功する。更新した既存pin 4件: BL-041の `KnownResidualTests::test_a_change_made_after_start_began_is_still_committed_with_it`（F0の残件をpinしていたもの）は、その変更がこの内容の証明で拒否されBL-041自身のsnapshotでは拒否されないことをpinする形へ移した。`test_project_start_recovery` の「Project開始が書くnote」のpinに `own_content` を加えた（bootstrap Skill fileを書くeffectが無い場合の記録）。BL-040の `test_added_then_removed_relation.py` の `DeletedByHandTests` は、人がledgerから手で消した場合に収束することをpinしていたので、書き込む前に `reconcile_required` で止まりledgerもrecordも1 byteも変わらないこと、人が書いた内容を戻せば同じ再実行が同じ終状態（relationは1度だけ消え、足し直されない）へ進むことをpinする形へ書き換えた。`MutationController` の公開methodのpinはそのままにするため、新しい書込み計算は非公開の `_planned_write` にした。`validate-registry` はPASS。full suite: 最終tree（この変更を含む）で1 test file 1 processに分けて実行し、全fileがexit 0・失敗0（実行の前後でHEAD・tree・working treeが同一。件数はこの変更のcommit messageに記録）。並行して先にlandした2件の上へ、意味で統合した: BL-045（記録済みregistration stageの証明からdisplay番号を外す）は `create._registration_effects` の引数と `ops._registration_matches` / `ops._recorded_displays` を足し `ops._prove_plan_exclusion` / `start._prove_cancel` の証明箇所を差し替え、BL-046（記録済みの派生を決め直さない）は `start.py` の派生resume経路を足す。本変更が触るのは `ops._owned_paths` の中身と `.mutation` からのimport、`start` の `.mutation` importと `_complete` の1箇所だけで、同じfileの別hunkである。BL-046が同じ `.mutation` importを複数行へ書き換えていたので、そこは行を作り直さず `declare_own_content` を1行足す形で統合した。BL-045が比べるのはeffectの `payload` で、本変更が足す `held_before` / `wrote` はeffect recordのtop-level key、`own_content` はmutationのnoteなので、`_as_recorded` にも `_recorded_displays` にも入らない。BL-046が読み戻す派生の決定は `<Work>:derive:<n>` のstageとそのcommitで、成果pathのnoteとは別の記録である。両者が足した `tests/test_replan_display_numbering.py` と `tests/test_recorded_derivation_resume.py` は本変更の上で全件成功する。 (10) 残る論点: (a) `_require_own_bytes_committed` と `git add` の間の窓は閉じきれない（Gitは自分が動く時にworking treeを読むため。push destinationの再解決と同じ性質で、その呼び出しの中へ狭めてある）。(b) 内容だけを見るので、bytesを変えないmode変更（実行bit等）は検出しない。(c) 他者が自分と同一のbytesを書いた場合は区別しない（契約として明記）。(d) operation開始からそのpathへの最初の書込みまでの窓に入った変更は、そのstageが記録する `held_before` に取り込まれる（BL-041の開始時snapshotは名前だけを持つため）。STARTでは最初のlifecycle eventが入口の直後に記録されるので窓は狭い。(e) 他者がfileをHEADと同じ内容へ戻して自分の書込みを消した場合、そのcommitは「commitする変更が残っていない」として一致扱いになり、postcheckが `postcheck_failed` で止める（他者のbytesは公開されないが、停止のcodeは本件のものではない）。(f) 不正なJSON行のようにreaderが先に拒否するpayloadは、ownershipが問われる前に `events_invalid` で止まる（結果は同じく未commit・未pushだが、ownershipの証明ではない）。(g) 既にcontaminated commitを公開したProjectの履歴は書き換えない。

### BL-044 START resume re-decides a recorded hold and grows duplicate lifecycle effects on every retry

- ID: BL-044
- Title: START resume re-decides a recorded hold and grows duplicate lifecycle effects on every retry
- Status: RESOLVED
- Kind: implementation, recovery
- Severity: major
- Problem: STARTのhold（executorが `Hold` を返した中断）は `work_target_removed` / `work_held` の2 lifecycle eventと、それを載せるcommitとして同じmutationに記録される。しかしresumeがrecordから読み戻していた決定は `_cancel_to_finish`（BL-030のcancel）と `_completion_to_finish`（BL-031、判定keyは `WORK_TERMINAL_EVENTS`）だけで、`work_held` はterminalでないため対象外だった。そのため中断後の再実行は `session.run_work` に落ち、自分自身の未commit eventが作ったstate（held = 後のSTARTが正当にresumeできるWork）を外界として読み直して `work_resumed` / `work_target_added` を追加し、`attempt` を1に戻してexecutorを呼び直し、返ってきたoutcomeを新しい決定として記録した。`ops.stage_name` は既存stage数から必ず新しいstage名を作るので、`Mutation.add_effects` の唯一の重複防止（`stage_duplicate`）も働かない。effect identityは `reserved_ids[f"{stage}:event:{index}"]` とstageに従属しており、同じeffectを指す手段自体が無かった。recordは既にそのstageを決定として扱い、決定したbranch（`decided_on`）まで刻んでいたのに、resumeがそれを読まなかった。
- Why it matters: 中断が無くても起きる。remoteが一時的に落ちてholdのpushだけ失敗したrecordを再実行すると、実際には起きていない「resume → hold」1 cycleがcommit + pushされて正史になり、STARTは `held` を返すので人にはエラーが見えない（実測: canonical event 4 → 8、commit 4 → 5、executor 1 → 2）。BL-041以前に作られたstuck recordでは `preexisting_dirty` noteが凍結されているためGit段階が永久に `dirty_overlap` で止まり、再実行のたびにeffect +4 / stage +2 / canonical event +4 / executor +1 / record 約 +1,679 Bが上限なく増える（実測: 4 → 8 → 12 …、100回で effect 404 / record 171,615 B、replayは O(N²)）。人が自分の変更をcommitすればその偽のcycleごとpushされ、破棄すればreplayが全部書き戻す。その間はledgerを書く全operationが `reconcile_required` で止まり、`validate_project` は何も報告せず、supported commandにpending recordを片付ける手段が無い。再実行でexecutorが `Completed` を返せば成果commitをpushまでするので、「stuck recordの再実行は外へ出さない」も成り立たない。
- Likely scope: 記録済みのhold決定をrecordから読み戻し、証明できる場合だけ残りのGit段階（commit / push）を進める（investigationのCandidate B、BL-031 / BL-030と同系）。証明できないrecordはrecordを変更しないまま `reconcile_required`。effectを持つresumeを一律に拒否するCandidate Aは採らない（question waitのresumeは、effectを持つ同じrecordでexecutorを正当に再実行する経路であり、それを壊す）。legacy recordの自動repair / migrationはしない（BL-041の方針を維持）。
- Cross-project impact: あり（全Projectの、holdを記録した後に中断・push失敗したSTARTの再実行と、BL-041以前に残ったstuck record）
- Backfill likely: no（既に停止しているrecordは自動repairせず、従来どおりGit段階で停止する。増えなくなるだけ）
- Human confirmation likely: yes（START Skillのtrue hold節に追記。holdの理由をrecordへ載せること、recordが保てない理由は記録せずhold自体は拒否しないこと、証明できないrecordを `reconcile_required` とすること、legacy recordを推測repairしないことは採用時に決定済み）
- Self-hosting prerequisite: no
- Evidence class: code inspection, reproduction
- Resolution: (1) 読み戻し: `src/workline/start.py` の `_hold_to_finish(mutation, work_id, mode)`。`_start_locked` で、cancelの読み戻し（BL-030 の `_cancel_to_finish`）とterminal finalizationの読み戻し（BL-031 の `_completion_to_finish`）の直後、`mutation.apply()` より前・`run_work` より前に置く。recordが `work_held` を持たなければ `None` を返し、STARTは従来どおり進む（executorがまだholdを返していないrecordはこれに当たり、question waitのresumeもここを素通りする）。持つ場合は、そのholdがこのSTART自身のものだとrecordだけで示せた時に `_Session.finish_hold` が残りのGit段階（commit / push、既に記録済みならreplayだけ）を行い、記録した理由とともに `held` を返す。executorを呼ばず、eventを追加せず、新しいstageを作らず、outerでも後続Workを選ばない。示せなければ `reconcile_required` で停止し、record・Project・Gitのどれも触らない。(2) 証明の条件: `work_held` がちょうど1件。effectがすべて読める形で、同じstageが2箇所に記録されていない（`ops._recorded_stages`）。対象entityがWork IDで、single-workでは指定Work自身。stage名が対象Workの次のlifecycle stage `<Work>:lifecycle:<n>`（nはそれ以前の同Work lifecycle stage数）。そのstageがちょうど `append_event` 2件で、event recordのfieldがidentity（id / type / entity / at）だけの空でない文字列、種別と順序が `work_target_removed` → `work_held` で両方とも対象Work宛、`{stage}:event:<index>` の予約IDと一致。payloadが `record` だけ（`work_held` のeffectだけは `record` と理由）。理由を持つならSTARTが記録する形（version 1 と reason のみ）。holdより前に記録された決定（`mutation._decides`）がそれぞれ自分のcommitで確定済み。holdの後に記録されたstageが無いか、そのholdのcommit `commit:<n>` ちょうど1つで、そのstageが `git_commit`（+ `git_push`）だけ・messageが `chore(workline): hold <display>`・pathsがこのmutationの書くpathの部分集合。そのcommitが未記録なら、HEADのevent logがまだそのeventを持たないこと。決定した所在（`decided_on`）はMutation Controllerの担当で、replayも記録も決定したbranchの上でだけ行われる（BL-036 / BL-038、`rules/git` のCommit / push）。(3) 理由: `_Session._record_hold` が `_hold_decision(outcome.reason)`（`{version: 1, reason: executorが渡した値}` を `_read_back` で書き戻せる場合だけ）を `work_held` のeffect payloadへ載せ、lifecycle eventと同じdurable writeで記録する。理由はSTARTの呼び出し元への言葉でProject正本の内容にならないので、書き戻せない値（小数・object・surrogateを含む文字列等）は記録しないだけで、holdは従来どおり成功する（新しいSTOPを作らない）。resumeは記録があればその理由とともに、無ければ理由なしで `held` を返す。(4) legacy: BL-041以前のstuck recordは証明を通って `finish_hold` → `_commit` → `gitops.finalize` → `ensure_separable` へ進み、凍結されたnoteと重なるので従来と同じcode・同じmessageの `dirty_overlap` で止まる。自動repairもmigrationもしない。(5) 変えないもの: question waitのresume（開始lifecycleだけのstageは決定ではない）、BL-031のcompletion、BL-030のcancel、BL-029のplan exclusion、BL-041の開始前拒否、Derive / HumanNG / 成果commit、holdが完了した後にheld Workを普通にSTARTし直す経路。`INTENT_VERSION`、record / note / invocation / 公開API・公開methodは変えていない。加えたのは `work_held` のeffect payloadの任意key `hold` だけで、それを持たないrecordも同じように続けられる。(6) 測定: 新しい `tests/test_recorded_hold_resume.py`（27 tests / 6 subtests: 3つの中断窓からのresumeがGit段階だけを実行しevent・stage・commitを増やさないこと、outer holdがそこでSTARTを終えること、question waitの後のholdも同じであること、理由の読み戻しと理由を持たないrecord・保てない理由、実際にremoteを退避させたpush失敗からのresumeが余分なcommitを作らないこととその後に他Workが進めること、BL-041以前のstuck recordが4回再実行してもeffect 4 / stage 2 / record byte / event log / Git / 他のrecordまで完全に不変であること、人がその変更をcommitまたは破棄した場合、証明できないrecord 8種が `reconcile_required` でrecordを変えないこと、中断の無いhold・held Workの再START・question wait・BL-031・BL-030・BL-041の非回帰）は修正後すべて成功し、`f2bb8f5` では24件（18 tests + 6 subtests）が失敗した。更新した既存pin: BL-041の `tests/test_preexisting_change_preflight.py` の `KnownResidualTests::test_a_held_start_stuck_before_this_change_still_runs_again_on_every_retry`（effect `[8, 12]` を期待していた）を `LegacyRecordTests::test_a_stuck_hold_keeps_stopping_at_its_git_stage_and_grows_nothing` へ移し、effect 4のまま・executor再実行なし・record byteとProject不変をpinする（`f2bb8f5` では失敗する）。mutant 4本（holdを読み戻さない: 17件、予約IDの照合を外す: 1件、holdの後のstageを何でもそのcommitとみなす: 1件、HEADのevent logの確認を外す: 2件）はすべて検出した。実測A/B: BL-041以前のstuck record（`ea6c697` の実装で作り、recordは書き換えていない凍結fixture）を6回再実行すると、`f2bb8f5` では effect 8 → 12 → 16 → 20 → 24 → 28、stage 4 → 14、event log 9 → 29行、record 3,914 → 12,324 B、executor毎回+1だったものが、修正後は effect 4 / stage 2 / event 5行 / record 2,239 B / executor +0 のまま、毎回Projectの全fileがbyte単位で不変になった（100回でeffect 404だった増殖が停止）。実際にremote directoryを退避させてpushだけ失敗させたhold（Workline codeは無patch）は、`f2bb8f5` では再実行がexecutorを2回目に呼び、起きていない resume → hold 1 cycleをcommit + push して canonical event 4 → 8・commit 4 → 5 にしていたが、修正後はexecutor 1回のまま、canonical event 4・commit 4 で `held` を返し、その後に同じPhaseの別Workが普通に完了する。入力境界のA/B（mode 2 × executorのoutcome 7種 × 中断窓7種 = 98行、1行1 Project、各行を3回実行）: 90行が完全一致、8行が対象（すべて `|hold|` 行）、それ以外の差分は0行。対象8行の内訳は、single-workの4行がexecutor 2 → 1回・canonical event 8 → 4・commit 5 → 4、outerの4行が `stopped`（未commitのeventを残してmutationを閉じていた）→ `held`、うち1行は3回目が `dirty_overlap`（自分が残した未commit eventに引っかかっていた）→ `held` になった。question wait・completed・cancel・Derive・Derive(move)は全窓で完全一致で、investigationが隣接面として挙げたDeriveの重複登録には触れていない（別件）。BL-030 / BL-031 / BL-029 / BL-025 / BL-036の既存resume testとSTART本体（`tests/test_cancel_decision_resume.py`、`test_terminal_finalization_resume.py`、`test_plan_exclusion_resume.py`、`test_lifecycle_resume.py`、`test_start.py`、`test_decision_branch_binding.py`）は137 tests / 222 subtestsすべて成功。full suite: 最終tree（`f2bb8f5` の上）で1 test file 1 processに分けて実行し、1 test file 1 processで全file実行し、全fileがexit 0・失敗0（実行の前後でtreeのhashが同一。件数はこの変更のcommit messageに記録）。`validate-registry` はPASS。START Skillのtrue hold節に、holdの決定とその理由をrecordへ記録すること、resumeが証明できる時だけexecutorを呼ばずGit段階だけを進めること、示せない場合の `reconcile_required`、理由を記録していない旧record、開始前の変更で止まるrecordを増やさないことを追記し、outer continuationからも参照した。(7) 並行して先にlandしたBL-042（成果pathの開始前変更を消さずに拒否し、拒否した成果をrecordへ保存して再実行で問い直さない）の上へ、意味で統合した: BL-042はmutationのnote（`refused_result`）と `gitops.narrow_preexisting_dirty` を足し、本変更は `work_held` のeffect payloadと `_start_locked` のresume経路だけを触るので、同じ関数の別hunkであり衝突しない（`f2bb8f5` 上で作った変更の各fileのpatchは、`cc559bf` 上でも1文字も変えずに当たる）。保存された成果は `run_work` のexecutor呼び出し位置で読まれ、hold決定より前の拒否でしか作られないので、記録済みholdを持つrecordとは同時に存在しない。BL-042が加えたsnapshotの縮小は `run_work` の中で行われ、`finish_hold` は通らないが、その時点でholdのcommit対象は必ずこのmutation自身の適用済みeffectでdirtyなので縮小されることはなく、結果は同じ `dirty_overlap` になる（人がその変更をcommitした場合は、HEADのevent logがそのeventを持つので本変更の証明が先に `reconcile_required` で止める）。BL-042の `tests/test_result_path_protection.py` は本変更の上で全件成功する。(8) 狭めた / 変えた挙動（開示）: 記録済みholdを持つrecordのresumeで、形を証明できないものは今日 `held` を返していたが `reconcile_required` になる（測定した8種すべてrecord・Project・Git不変）。BL-041以前のstuck recordで人が自分の変更をcommitした場合、HEADのevent logが既にそのholdのeventを持つため `dirty_overlap` ではなく `reconcile_required` になる（どちらもSTOPで、recordは不変。従来はそこからも増殖し続けていた）。outerで記録済みholdを持つrecordのresumeは `stopped` ではなく `held` を返す（従来は未commitのeventを残したままmutationを閉じていた）。修正によって未commitのeventが残らなくなるため、従来その残骸で `dirty_overlap` になっていた後続の再実行は普通に進む。(9) 残る論点: 中断したDeriveの再実行が同じfix Workを二重登録する隣接面（root causeは同じだがhold以外の決定にはこの読み戻しを広げていない）、BL-041が挙げた成果pathと開始後の変更（F0）、pending recordを人が安全に放棄するsupported commandが無いこと（BL-041の調査で確定、本件では扱わない）。

### BL-045 Replan resume refuses after another Work changes only the recomputed display numbering

- ID: BL-045
- Title: Replan resume refuses after another Work changes only the recomputed display numbering
- Status: RESOLVED
- Kind: implementation, recovery
- Severity: major
- Problem: STARTのcancel（BL-030の記録済みの決定）とplan exclusion（BL-029。RoadmapのPhase / Phase Work、STARTのstandalone Work）は、記録済みのregistration stageを同じ入力から組み立て直し、recordが持つものと完全一致するかで「このrequest / 決定が記録したstageである」ことを示す。その組み立て直しが、新Workに書くdisplay番号を「今Projectが持つWork数 − このmutation自身の適用済みの新Work」から採番し直していた（`src/workline/start.py` の `_prove_cancel`、`src/workline/ops.py` の `_prove_plan_exclusion`）。mutationがpendingの間に他のWorkが1件増えると（別cloneが登録してpull / mergeで届いたもの、人が置いたもの。pendingの間はどのWorkline ownerも同じledgerを要求するので、operationとしては登録できない）、recordの `display: W-04` に対して組み立て直しは `display: W-05` となり、stageの他のすべて（effectのkindとpath、stable Work ID、display以外のfrontmatter、body、derivation detail、予約したrelation / derivation ID、relation payload、記録順序）が一致していても `reconcile required` になった。同じ他人のWorkがregistration stageの記録より前に届いた場合は普通に成功するため、同じ中断・同じ計画でも他人のWorkが届いたwindowだけで結果が変わっていた。
- Why it matters: 並行する別clone / 別sessionや人の作業でWorkが1件増えるだけで、中断したcancel / plan exclusionが回復できなくなる。止まったrecordは適用済みのeventとreplanの一部をworking treeに残し、`validate_project` は構造違反（`dependency_unreplanned` 等）を示し続け、event logを書く全operationがそのpending mutationに塞がれる。displayはProjectがWorkをどう見せるかであって同一性ではないので、この停止は同一性の欠落を示していない。
- Likely scope: registration stageのequality proofからdisplayを外し、実際に書く値はrecordが持つdisplayのままにする（investigationのCandidate B）。displayをlive stateから採番し直さない。STARTのcancelとplan exclusionの両ownerを直し、STARTだけのspecial-caseにしない。schema・`INTENT_VERSION`・display allocationの式・uniqueness enforcement・Project全体のrenumber・migrationは変えない。
- Cross-project impact: あり（全Projectの、中断したcancel / plan exclusionのresume）
- Backfill likely: no（止まっていたpending recordは、同じrequestの再実行がそのまま継続する。自動repairしない）
- Human confirmation likely: yes（`rules/git` の記録済みstageの判定に追記。displayを同一性に含めないこと、recordのdisplayをそのまま書くこと、欠番 / 重複を許すこと、uniqueness ruleを足さないことは採用時に決定済み）
- Self-hosting prerequisite: no
- Evidence class: code inspection, reproduction
- Resolution: (1) 原因: 記録済みのregistration stageを組み立て直す際に、新Workのdisplay番号を現在Projectが持つWork数から採番し直していたこと。stageの他のすべてと、recordのfile内容・予約ID・relation membership・記録順序は一致していた。BL-030のdurableな決定、BL-029のrequest同一性とown-effects-freeの再判定は正しく働いていた。(2) 修正: `src/workline/create.py` の `_registration_effects` は `base_number: int` の代わりに、書くdisplayそのもの（`displays`、specのkeyごと）を受け取る。いま記録するstageは `_allocated_displays(specs, base_number)`（従来と同じ `W-{n:02d}` の式、同じ宣言順）で採番する。`src/workline/ops.py` の新しいprivate helper `_registration_matches(recorded, specs, work_ids, relations, related_ids, derivation_ids)` は、recordが各新Workのentity writeに持つdisplayを `_recorded_displays` で読み出し、それで組み立て直して従来どおり `_as_recorded` の完全一致で比べる。STARTのcancel（`start.py` の `_prove_cancel`）とplan exclusion（`ops.py` の `_prove_plan_exclusion`）の両ownerが同じhelperを使い、STARTだけのspecial-caseにしていない。まだ記録していないregistrationを先に検査する `_refuse_before_replay` は、そのstageのdisplayをこれから採番するので `_allocated_displays` のまま。(3) 同一性として比べるもの: effectのkind、file path、stable Work ID、display以外のfrontmatter全体、body全体、derivation detailの内容、予約したrelation / derivation ID、roadmap / related relationのpayload、stageが記録したeffectの列そのもの（意図した登録集合とその順序）。displayだけをnon-identity fieldとして扱う。displayがWorkの同一性でないことはinvestigationで確認済み（非emptyの検査しかない、uniqueness ruleが無い、lookup keyでない、重複displayでも `validate_project` のproblemsは0、projectionは `W-??` で成立する）。(4) 書く値: resumeは自分のstageが記録したdisplayをそのまま書く（`register_works` はstageが記録済みならeffectを組み立て直さず記録済みeffectをreplayするので、書く値はrecordのまま）。現在の件数で採番し直さない。欠番や重複が残ることは現在の契約で妥当で、新しいuniqueness ruleは足さない。(5) timing依存の解消: 他人のWorkがregistration stageの記録より前に届いた場合と後に届いた場合で、同じ中断・同じ計画が同じ答え（そのまま継続）になる。display番号だけは、そのstageが採番した時点の値が残る。(6) 変えないもの: schema、`INTENT_VERSION`、display allocationの式、Project全体のrenumber、migration、record / effect / payload / invocationの形、`Mutation` / `MutationController` の公開method、BL-030のdurableな決定・executor再実行なし・recorded reconstruction・cancel proof、BL-029のrequest同一性・own-effects-freeの再判定・recorded registrations / removals・nativeな拒否、BL-038の別branchでの停止、BL-040の追加→削除の分類（`mutation._classify_recorded`）、BL-041の開始前変更のpreflight、他人のWorkの扱い（上書き・stage・commit・relation編入・ID奪取をしない）。(7) 広げた範囲（採用時に決定済み・開示）: recordが持つdisplayが、いま採番するはずの値と違っても受け入れ、そのまま書く。他人のWorkによるずれ以外に、recordのdisplayだけを書き換えた場合（欠番 `W-99`、2件の新Work間でのdisplayの入れ替え）も継続する。displayは同一性ではないので結果のProjectは妥当（`validate_project` のproblemsは0）。stageがそのWorkのwriteを持たない・読めない・displayが非emptyのstringでない場合は従来どおり停止する。新しく拒否するようになった入力は無い（修正前に一致した記録は、そのdisplayを読み出せるので必ず一致する）。(8) 測定: focused: 新しい `tests/test_replan_display_numbering.py`（21 tests / 69 subtests）は修正後すべて成功し、`f2bb8f5` では41件が失敗した（成功した18件は変えてはいけない挙動のpin: displayが同一性でないこと、stageの記録より前に他人のWorkが届いた場合の成功、中断の無い採番、squat / tamper / 順序の停止、BL-041のpreflight）。3 ownerを各case独立のProjectで通す: STARTのcancel、RoadmapのPhase Work plan exclusion、STARTのstandalone Work plan exclusion。他人のWorkの形は6通り（standalone、同じPhase、新Workと同名で別ID、別名で別ID、向こうでcommit済み、こちらでuntracked）を3 ownerで確認し、stageの記録直後とその適用直後の両windowで継続すること、他人のWorkがbyte単位で変わらず（上書き・stage・commit・relation編入・ID奪取なし）、このoperationのcommitがそれを運ばないこと、書かれるdisplayがrecordの値のままで採番し直されないこと、新Workが2件でも記録どおりの連番が残ることを含む。directly affected 3 file（`test_cancel_decision_resume` / `test_plan_exclusion_resume` / `test_replan_registration`）は72 passed・133 subtests passedでinvestigation baselineと同数。入力境界のA/B（3 owner × 21状況 = 63行、1行1 Project）を `f2bb8f5` と修正後で実行: 48行が完全一致、15行が変化し、変化はすべて `reconcile_required` → 完了（`valid=0`・pendingなし・他人のWorkはbyte不変）で、逆向き（新たに拒否）は0行。変化した15行は、3 owner × 〈他人のWorkがstage記録後にuntracked / commit済み / stage適用後〉と、開示済みの2件〈recordのdisplayだけを `W-99` に書き換え〉〈新Work 2件のdisplayを入れ替え〉。他人のWorkが予約IDを占有した場合、name / stable ID / path / relation type / 非emptyでないdisplay / 読めないfrontmatter / 余分なeffect / Workのwrite削除 / 記録順序の入れ替えの改竄、別branch（BL-038）、開始前の変更（BL-041、`dirty_overlap`）は修正前後で同じ停止のまま。mutant 5本: displayを同一性へ戻す（41件検出、`f2bb8f5` と同じ内訳）、stable IDも比較から外す（`test_a_stage_registering_another_work_still_stops` だけが検出）、relation payloadも比較から外す（`test_a_stage_whose_relations_are_not_the_ones_this_request_adds_still_stops` だけが検出）、recordのdisplayを採番し直して書く（`test_a_replan_resumes_when_a_foreign_work_arrived_after_its_registration_was_recorded` だけが検出）、displayに規則を課さない（`test_a_display_the_project_would_refuse_to_hold_still_stops` だけが検出）。`register_works` の中でrecordを採番し直すmutantは、継続するreplanが `apply_replan` で記録済みstageを読み戻し `register_works` に入らないため到達せず、これ自体が「書く値がrecordのままである」ことの構造的な根拠になっている。full suite: 最終tree（本変更）で1 test file 1 processに分け3 shardで実行し、42 file全てexit 0、937 collected、936 passed・1 skipped（file symlink）・失敗0、1055 subtests passed・1 subtest skipped、実行の前後でtreeは同一。`validate-registry` はPASS。 (9) 残る論点: (a) `start.py` の cancel finalization は、記録済みcommitのmessageを現在のProjectから読んだ「cancelされたWorkのdisplay」（`chore(workline): cancel <display>`）と比べる。この照合は今回変えていない。cancelされたWork自身のfileのdisplayが変われば同じ形の停止が起きるので、別candidateとして残す（他人のWorkが増えてもcancelされたWork自身のdisplayは変わらないため、BL-045の条件では起きない）。(b) Phase / Roadmapのdisplay（`phase_create` の `P-<n>`）は同じ採番をするが、registration stageの証明で組み立て直していないので対象外。(c) 欠番や重複のdisplayは残ったままになる。Project全体のrenumberはしない。

### BL-046 START resume re-decides a recorded derivation and registers the same fix Work again

- ID: BL-046
- Title: START resume re-decides a recorded derivation and registers the same fix Work again
- Status: RESOLVED
- Kind: implementation, recovery
- Severity: major
- Problem: executorが返した派生（`Derive` / `HumanNG`）は、STARTがそのcycleで下した決定であり、registration stage（fix Work file・派生detail・relation）と、それを確定するcommitとして同じmutationに記録される。`_bind_decision` はそのstageへ決定したbranch（`decided_on`）まで刻んでいた。しかしresumeがrecordから読み戻していた決定は `_cancel_to_finish`（BL-030）・`_completion_to_finish`（BL-031）・`_hold_to_finish`（BL-044）だけで、派生は対象外だった。そのため再実行は `session.run_work` に落ち、`attempt` を1から振り直してexecutorを呼び直し、返ってきたものを新しい決定として記録した。`ops.stage_name` は既存stage数から必ず新しいstage名（`<Work>:derive:1`）を作り、予約IDはstage名に従属する（`create.register_works` の `f"{stage}:work:{key}"` 等）ので、2回目の登録は別のstable Work ID・別の派生detail・別のrelation 2件になり、`Mutation.add_effects` の唯一の重複防止（`stage_duplicate`）も働かなかった。派生がtargetを外す場合（temporary move / human_confirmation NG）は、Workが「targetを持たないin_progress」になるため再実行が `work_target_added` から開き直し、lifecycle eventまで二重になった。
- Why it matters: 中断が必要条件ではない。派生の直後のquestion waitはrecordをpendingのままSTARTを終える正規の流れで、回答して再実行すると同じcycleが最初から数え直されるため、`derive → 質問 → 回答 → 続行` だけで同じfix Workが2件登録され、両方がcommit + pushされて正史になり、STARTは `completed` を返すので人には何も見えない（実測: Work 3 → 4、relation 3 → 5、派生detail 1 → 2、executor 2 → 4回、remoteのWork file 3 → 4）。中断からの再実行では、決定を記録した後のどの窓（registration記録後・1 effect適用後・全適用後・commit記録後・commit済みpush未）でも同じことが起き、中断のたびに上限なく増えた（実測: 3回の中断で Work 3 → 6、relation 3 → 9、派生detail 1 → 4）。`validate_project` は1件も報告せず（重複Work・重複 `derived`・重複 `requires_completion` を見るruleが無い）、outerでは重複したfix Work同士が同格になって `stopped`（`2 startable Works are equally planned`）で継続不能になり、duplicateの `requires_completion` がintegrationを塞ぐ。safe RRではなくfalse successであり、正本にWorkが増え、remoteへ出る。
- Likely scope: 記録済みの派生の決定をrecordから読み戻し、証明できる場合だけそのcycleがそれを登録し直す（investigationのCandidate A、BL-044 / BL-031 / BL-030と同系）。executorを呼ばない。証明できないrecordはrecordを変更しないまま `reconcile_required`。1 run内で正当に複数回派生するcycleを壊さない（「派生は1回だけ」というglobal dedupeは採らない）。legacy recordの自動repair / migrationはしない（BL-041 / BL-044の方針を維持）。
- Cross-project impact: あり（全Projectの、派生を記録した後に中断・push失敗・question waitを挟んだSTARTの再実行）
- Backfill likely: no（既に二重登録されたProjectは自動修復しない。増えなくなるだけ。決定を記録していない旧recordは従来どおりexecutorへ問い直す）
- Human confirmation likely: yes（START Skillの `Derived / fix Work` 節に追記。決定をstageより前にrecordへ記録すること、証明できる場合だけexecutorを呼ばず登録し直すこと、示せない場合の `reconcile_required`、targetを外した派生をGit段階だけで終えること、新しいcycleは自分の派生を決めることは採用時に決定済み）
- Self-hosting prerequisite: no
- Evidence class: code inspection, reproduction
- Resolution: (1) 決定の保存: `src/workline/start.py` の `_Session._record_derivation` が、executorが返した派生を `_recorded_outcome`（BL-042が入れた `Derive` の往復。works / integration / relations / move / related / confirmation_target / derivation_detail まで検証付き）の形でmutation note `derivations` へ、cycleが決めた順に、**それを登録するstageを記録する前に**保存する。`_read_back` を通らない値は保存せず `complete: false` を立てるだけで、派生自体は従来どおり成功する（新しいSTOPを作らない）。stageより前に保存するので、記録済みのregistrationが決定を伴わないrecordは生まれない。registrationがstageを記録する前に拒否した場合は `_forget_derivation` がその決定を捨てる: Work file・派生detail・relationのどれも書かれていない（`skills/create` の登録前の構造検査）ので決定は実行されておらず、再実行は従来どおりexecutorへ問い直す（`tests/test_registration_projection.py` の`test_a_derived_work_requiring_an_excluded_work_is_not_registered` が要求する挙動）。記録済みのstageを持つ決定は、その後に拒否されても残る。processの停止（kill）はこの経路を通らないので、決定を保存した直後に落ちたrecordはその決定を使う。BL-042の拒否（開始前から変更されていたrelation ledger）はこの保存より前で、`refused_result` noteの経路は変わらない。(2) 読み戻し: `_derivations_to_reuse(mutation, work_id, mode)` を `_start_locked` で、cancel / completion / holdの読み戻しの直後、`mutation.apply()` より前・`run_work` より前に置く。noteが無い、または `complete: false` のrecordは `()` を返して従来どおり進む。証明の条件は、各Workの派生が `<Work>:derive:<n>` として決定順に記録され、未記録が末尾1件まで（決定を保存した直後の中断）、記録済みstageが `decided_on` を持ち、派生と次の派生の間に記録されたstageがその派生のcommit（`commit:<n>`、`git_commit`（+ `git_push`）だけ）1つ、targetを外した派生がそのWorkの派生の最後、single-workでは指定Workの派生だけ、noteが `{version, complete, derivations[{work_id, stage, outcome, moved_by}]}` として読み戻せ `outcome.kind == "derive"` であること。示せない場合は何もreplayせず `reconcile_required`。(3) 登録し直し: `run_work` は、cycleが続いている場合（Workが自分のtargetを持ったまま）だけ、記録済みの派生をそのcycleのattemptへ順に割り当てる（`_cycle_derivations`。そのWorkのcycleを開いたlifecycle stageより後に記録されたderive stageだけがそのcycleのもの。ここでWorkを開き直した場合は新しいcycleなので何も割り当てない）。割り当てがあるattemptはexecutorを呼ばず、記録済みのoutcomeをそのまま使う。`_derive` は記録済みのstage名をそのまま使い、そのstageがrecordにある場合は `register_works` を呼ばずに `_recorded_registration` で読み戻す（今のProjectに存在するWorkを検証し直し、記録済みのintegrationを二重に数えるのを避ける。`ops.apply_replan` が記録済みのreplan registrationに対して行っているのと同じ）。stageがそのWorkの決定の作る登録であることは `ops._registration_matches`（BL-045）で示す: effectのkindとpath、stable Work ID、display以外のfrontmatterとbody全体、派生detail、予約したrelation / derivation ID、relation payload、記録順序を完全一致で比べ、displayはstage自身が記録した値を使う（他のownerがWorkを登録していても採番し直さない）。予約IDが1つでも欠けていれば `reconcile_required`。派生自身のrule（integration invariant / 再統合）は、そのstageの適用済みeffectを除いた `ops._own_effects_free_view` の上で判定する（resumeは判定の前にそのstageを適用済みにしているため。1 run内で「integrationを作る派生」と「作らない派生」は相互排他なので、そのstage自身を除けば足りる）。(4) targetを外した派生: `_derive_move_to_finish` が、記録済みの派生の最後がtargetを外すもので、その `work_target_removed`（対象Work宛・予約IDどおり・payloadは `record` だけ）が次のstageとして記録済みで、その後がその派生のcommitだけ（messageは決定のもの: 派生なら `chore(workline): branch from <display>`、NGなら `chore(workline): <display> NG; fix planned`、pathsはこのmutationの書くpathの部分集合）の場合に `_Session.finish_derive_move` へ渡す。残りのGit段階（commit / push、記録済みならreplayだけ）だけを行い `moved` を返す: executorを呼ばず、Workを開き直さず、lifecycle eventもstageも追加しない。commitが未記録ならHEADのevent logがまだそのeventを持たないことも要る。target削除が未記録なら `None` を返し、そのcycleが従来どおり記録する。mutationがその先へ進んでいる場合（outerが次のWorkを選んだ場合）も `None` で、その後そのWorkが新しいcycleとして開かれれば、そのcycleが自分の派生を決める。どちらの決定が動かしたかは note の `moved_by` が持つ（`HumanNG` は `move=True` の `Derive` として `_derive` に入るため、outcomeだけでは区別できない）。(5) 変えないもの: BL-042（`refused_result` の保存と再利用、`narrow_preexisting_dirty`、成果pathの保護、派生のledger事前拒否）、BL-044のhold、BL-031のcompletion、BL-030のcancel、BL-029のplan exclusion、BL-045のdisplay（新しいregistrationは従来どおり `_allocated_displays` で採番し、読み戻しはstageのdisplayを使う。live stateからの再採番を復活させていない）、BL-041の開始前拒否、BL-036 / BL-038のbranch拘束、question waitのresume（開始lifecycleだけのstageは決定ではない）、`INTENT_VERSION`・record / effect / payload / invocationの形・公開API・`Mutation` / `MutationController` の公開method・`registry.md`。加えたのはmutation noteの `derivations` だけで、それを持たないrecordも同じように続けられる。(6) 測定: 新しい `tests/test_recorded_derivation_resume.py`（41 tests / 28 subtests）は修正後すべて成功し、`2694c699` では48件（25 tests + 23 subtests）が失敗した。`2694c699` でも成功する8件は変えてはいけない挙動のpin（中断の無い派生 / move / NG、BL-030のcancel、BL-031のcompletion、BL-044のhold、BL-042の `refused_result` 再利用、派生より前のquestion wait）。窓は決定を保存する前・保存後stage未記録（killが残す状態をrecordから組み立てる。raiseはregistrationの拒否と同じ経路なので決定を捨てる側に入る）・stage記録後未適用・1 effect適用後・全適用後・commit記録後commit未・commit済みpush未の7つ。mutant 5本（読み戻しを無効化: 19 tests + 23 subtests、`decided_on` の検査を外す: 1 test、registration同一性の検査を外す: 2 subtests、記録済みstageを読み戻さず採番し直す: 9 tests + 11 subtests、targetを外した派生をGit段階で終えない: 4 tests + 3 subtests）はすべて検出し、`decided_on` と同一性の2本はそれぞれ専用のtestだけで落ちた（`test_a_registration_recorded_without_its_branch_is_refused`、`test_a_relation_payload_other_than_the_decision_makes_is_refused` / `test_a_work_body_other_than_the_decision_makes_is_refused`）。実測A/B（investigationのharnessを `2694c699` と本変更で実行、1行1 Project）: 中断直後の観測はすべての窓で完全一致（本変更が足すのはnoteだけで、effect・stage・event・commitを足さない）。再実行後は、決定を保存した後の全窓で Work 4 → 3・relation 5 → 3・派生detail 2 → 1・executor 3 → 2回・remoteのWork file 4 → 3 になり、中断の無い実行と同一の形に収束した。`derive → question wait → 回答` は Work 4 → 3・executor 4 → 3回。実際にremote directoryを退避させてpushだけ失敗させた行（Workline無patch）も同じ。3回の中断は Work 6 → 3・relation 9 → 3・派生detail 4 → 1 で、executorは最初のcycleの1回だけになった。`Derive(move=True)` は Work 4 → 3・event 5 → 3 になり、中断の無い実行と完全一致。outerは `stopped`（`2 startable Works are equally planned`）→ `phase_complete`。1 run内で2回派生する行（正当）は修正前後で完全一致（Work 4・派生detail 2）で、その2つ目のcommit記録後に中断した行は Work 6 → 4 になった。decision保存前の中断は修正前後で完全一致（executorへ問い直す従来の契約）。更新した既存pin: BL-042の `tests/test_result_path_protection.py` の `LegacyRecordTests::test_a_derivation_stuck_before_this_change_is_left_exactly_as_it_is` は、止まったlegacy recordの再実行がexecutorを2回目に呼ぶこと（`ran == [w1] * 2`）を期待していた。本変更ではその記録済みの派生をrecordから登録し直すので、executorは最初のcycleの1回だけになる（`ran == [w1]`）。止まるcode（`dirty_overlap`）、repairしないこと、増えないこと、人の変更をcommit / pushしないことは同じ。directly affected 13 file（`test_start` / `test_result_path_protection` / `test_recorded_hold_resume` / `test_terminal_finalization_resume` / `test_plan_exclusion_resume` / `test_replan_display_numbering` / `test_replan_registration` / `test_startable_selection` / `test_decided_content_binding` / `test_create` / `test_registration_projection` / `test_declared_write_scope` / `test_added_then_removed_relation`）はすべて成功。full suite: 最終treeで1 test file 1 processに分けて全file実行し、全fileがexit 0・失敗0（実行の前後でtreeのhashが同一。件数はこの変更のcommit messageに記録）。`validate-registry` はPASS。(7) 狭めた / 変えた挙動（開示）: 記録済みの派生を持つrecordのresumeで、形を証明できないものは今日 `completed` を返していたが `reconcile_required` になる（測定した13種すべてrecord・HEAD・remote不変、二重登録なし）。outerで記録済みの派生を持つrecordのresumeは、重複で同格になって `stopped` になっていたものが `phase_complete` まで進む。記録済みの派生を決定どおり登録するので、再実行時にexecutorが別の内容を返しても、登録されるのは最初の決定である。(8) 残る論点: (a) recordのregistration payloadだけを書き換えた場合、そのstageが未適用ならMutation Controllerが自分の記録済みeffectを先にreplayしてからこの検査が `reconcile_required` で止める（record・HEAD・remoteは不変、二重登録なし、working treeにはそのstageの内容が残る）。同一性の検査はregistrationを登録し直す位置にあり、`apply()` より前ではない。(b) `HumanNG` を、そのtarget削除を記録する前の窓で中断したrecordは、resumeが `run_work` のprecheckで、そのNG自身が登録した再統合への `requires_completion` を未充足として `dependency_unsatisfied` で止まる（修正前と同じ。今回の変更で増えも減りもしていない別面）。(c) recordが保てない派生（`_read_back` を通らない値）は従来どおりexecutorへ問い直すので、そのrecordでは二重登録が残る。(d) 決定を記録していない旧recordは自動repairせず、従来どおりexecutorへ問い直す。

### BL-020 Correction of mis-recorded historical facts

- ID: BL-020
- Title: Correction of mis-recorded historical facts
- Status: DEFERRED
- Kind: spec, design
- Problem: 正しい手順で記録されたevent、derived relation、origin等の「起きた事実」が、後から内容として誤りだったと分かった場合の正式な訂正方式がない。これらは書換え・物理削除をしない原則で保護されている。
- Why it matters: 誤記録が見つかった時に、書換え禁止を守ったまま現在の解釈を正す手段がなく、手編集の誘惑や、誤った履歴を前提にした判断が残る。
- Likely scope: 訂正eventや注記の方式、generated stateへの反映、`rules/ai-decision`（起きた事実は現在計画に合わせて書き換えない）との整合。設計段階で「実例が出るまで保留」とした論点であり、実例が出た時点で設計する。
- Tracked requirements (BL-009由来): BL-009でterminal Workのhistorical Relatedを「当時の証拠」として保持すると確定した。その記録自体が誤りだった場合（例: そもそもそのWorkはそのfileを読む必要がなかった）の訂正はここで扱い、最低限次を満たすこと。
  - 元の記録を物理削除・書換えしない
  - 訂正自体を正式な記録として残す
  - downstream readerが訂正の存在を機械的に判定できる（人間向けのREADMEメモだけでは不足）
  - superseded / invalidatedなhistorical factを現在の真実として扱わない（後続WorkやAIが元記録だけを事実として再利用しない）
- Cross-project impact: あり
- Backfill likely: no
- Human confirmation likely: yes（共通ルール / event schemaの変更）
- Self-hosting prerequisite: no
- Evidence class: design deferral

### BL-021 Concurrent operation exclusion

- ID: BL-021
- Title: Concurrent operation exclusion
- Status: RESOLVED
- Kind: implementation, design
- Problem: Mutation Controllerは、pending mutationの検出とwrite scopeのoverlap判定で競合を防ぐが、複数のprocessが同じProjectで同時にoperationを開始した場合の排他（lock / mutex / queue等）は実装されていない。pending mutationの検出からrecovery recordの作成までの間や、ledgerファイルの読み込みから書き戻しまでの間に、別processが割り込む余地がある。
- Why it matters: 複数のsessionやagentが同じProjectを並行して操作すると、互いのpending mutationを検出できないまま同じ正本を更新し、更新の取りこぼしが起き得る。現時点で発生は確認していない。
- Likely scope: 排他の要否と方式（single-writer前提の明文化で足りるか、lock等を入れるか）、設計段階で実装時に決めるとした排他技術の論点、BL-017との関係
- Cross-project impact: あり
- Backfill likely: no
- Human confirmation likely: no（前提の明文化や実装内の排他だけの場合。共通ルールを変える場合はyes）
- Self-hosting prerequisite: no
- Evidence class: design deferral, code inspection
- Resolution: 成立済みProjectにProject単位のactive execution lock（OS管理の排他file lock、one active writer per Project）を導入した。operationはlock取得後にpending mutation・Project state・push destinationを読み、他processが実行中なら待たずに `project_operation_busy` でSTOPして何も書かない。QuestionWaitではlockを解放しpending mutationを維持する。crash時はOSがlockを解放し、既存のpending mutation recoveryで再開する。Mutation ControllerはProject開始以外のownerについてlockなしのmutationを拒否する。initial Project開始の同時実行は対象外（非サポート）。`rules/git` とcanonical Skillsへ反映済み。

### BL-022 ProjectSTART abandoned pre-effect recovery

- ID: BL-022
- Title: ProjectSTART abandoned pre-effect recovery
- Status: RESOLVED
- Kind: implementation, spec
- Problem: Project開始がrecovery intentを作成した後、domain effectを適用する前にSTOPすると（例: bootstrap pathがGitにignoreされている、既存の未commit変更と重なる）、intentはabandonedとして残り、`.workline/` にはruntime recordだけが存在する状態になる。原因を解消して再実行しても、pending mutationがなく成立済みProjectでもないため `partial_workline` としてSTOPし、正式な経路では再開も再初期化もできない。
- Why it matters: 一時的な前提条件の不備だけで、そのfolderをWorkline Projectとして開始できなくなる。回復には手作業での削除が必要になり、推測修復を禁じる規則とも衝突する。使い捨てのrepositoryで再現を確認している。
- Likely scope: Project開始の「partial .workline」判定（canonical fileを含まずruntime領域だけがある状態の扱い）、abandonedになったProject開始intentの扱い、STOP時の孤立生成物cleanup（`rules/git` のcleanup定義との整合）、`skills/project-start` の記述とtest
- Cross-project impact: あり（Project開始の経路）
- Backfill likely: no
- Human confirmation likely: yes（Project開始の判定規則とcanonical Skillの変更）
- Self-hosting prerequisite: no
- Evidence class: code inspection
- Resolution: 新しいProject開始は、git initを含むpre-effect検査（Git boundary、bootstrapのcommit可否、pre-existing dirtyの分離可能性）をrecovery intentの作成より前に終え、そこでのSTOPでは `.workline` を作らないようにした（`.git` だけが残り得るが削除せず、次回は既存repositoryとして扱う）。dirty snapshotは検査時にcaptureし、intent作成直後・最初のeffectより前に同じsnapshotを記録する。`.workline` が、そのfolderに対するProject開始がeffectを1件も記録する前にabandonedになったrecovery recordだけから成ると既存のfieldから機械的に証明できる場合は、それらのrecordを変更・削除・resumeせずに残したまま、新しいmutationとしてやり直す。それ以外のpartial `.workline` は `partial_workline`、ownershipを確認できないrecordは `reconcile_required` のままSTOPする。pending Project開始のresumeは変更していない。intent version・record schema・project.yaml・bootstrap・eventは変更せず、recordのcleanup・保持期間・runtime ignore（BL-019）とProject開始の同時実行（BL-021）は扱っていない。`skills/project-start` へ反映済み。
